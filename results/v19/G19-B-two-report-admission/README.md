# Two retrospective reports: timing-blocked

The partial kernel passes 38 direct Bayes and future-bound controls. Full-support timing in three complete 32-row batches per structure/density projects 12,854.69 inclusive CPU seconds against a 3,600-second cap. A coarse single-row timing is retained separately. No native handler or scientific job is admitted; all native outcomes remain unconsumed. Scientific/evaluator admission evidence only.
