# Prior information: retained dispatch failure

The initial frozen plan named an unregistered handler. Dispatch failed before any scientific calculation; both unstarted replays are retired with their immutable plans retained. Failed CPU is charged. A new immutable plan corrects only the handler name; scientific source, inputs and design remain identical. This is an engineering failure, not a scientific null. No reader inputs.
