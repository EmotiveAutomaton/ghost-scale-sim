# Exact forecast collisions: retained instrument failure

The original stopped before scoring when a native marginal rounded to 1.0000000000000002. The strict component upper bound contradicted the admitted simplex tolerance. No scientific outcome is accepted. The original source, partial reader/evaluator exports and charge are retained; unstarted replays are disposed. A replacement permits 1e-12 upper-bound roundoff without clipping, normalization or changes to exact equality or scoring.
