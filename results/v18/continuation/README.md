# V18.1 current results

The separately commissioned continuation remains active. See its [unchanged
commission](../../../docs/versions/v18-selective-acquisition/continuation/CODING_PACKAGE.md),
[interim results](../../../docs/versions/v18-selective-acquisition/continuation/RESULTS.md),
[branch ledger](BRANCH_LEDGER.json), [bridge](../../../docs/versions/v18-selective-acquisition/continuation/BRIDGE.md)
and [illustrations](../../../docs/versions/v18-selective-acquisition/continuation/ILLUSTRATIONS.md).

Each completed branch directory contains its frozen plan/admission, complete
comparisons, independent reconstruction, and bounded extracted-source replay proof.
`FIRST_WAVE.json` binds the first 1,721,352 checked rows. Raw blocks and source ZIPs
stay in outer `.local/v18-1/`; public proof implementations are preserved by hash
in `proof-sources/`. G0 original cases are exposed; larger/context comparisons are
descriptive, not confirmed. `g4-history/SUPPORT_CORRECTION.json` records 16 physical
contexts among 18 sampling clusters, retaining all 72 original blind cases unchanged.
The first raw benchmark/report revisions remain local and are not overwritten.

The new parent/child CPU ledger includes failed attempts. Ongoing dependency
transfer and the existing Stitch rival have frozen inputs and source revisions;
they are not included in completed-row counts before verification.

`VALIDATION.json` records 41 passing integration checks without skips.
`TARGET_VERIFICATION.json` binds the independent target and detour checks.
`g4-history/SUPPORT_SCORE_CHECK.json` verifies that all 486 reported mean fields
are exactly unchanged by the corrected support grouping. `TIME_ACCOUNT.json`
is an interim parent/child account with its unmeasured setup limits explicit.
`structural-direct/PLAN.json` is admitted next work on reused inputs, with no
scientific result yet. Frozen plans are preserved unchanged.
