# Independent support and holdout verification

The completed support/holdout experiment asks whether learned transition
composition helps on full queries absent from training, with identical final
probability smoothing across all arms. Execution and two complete replays do not
replace numerical adjudication. This checker adds no scientific population or
tiny-model setting. It shares the existing four-hour support card and F/global
ceilings, including its controls and retained failed attempts.

Freeze all original inputs, outputs, source and plan. Independently reconstruct
every native development trajectory and probability, selected training counts,
withheld indices, transition and direct tables, nearest-query ties, explicit
three-step propagation, all nested sampled forecasts, support diagnostics and
proper scores. Check complete denominators and all five declared strata. Preserve
empty strata explicitly. Regroup by paired coefficient lineage, averaging the
two training draws within each lineage; report draw variation separately. Use
10,000 bootstrap samples and seed 190501. No model selection or target refitting.

Known-answer, missing-support, common-smoothing, undo-buffer and corrupted-forecast
controls precede real outcome access. Isolated controls include a complete native
fixture and explicit summation over every possible intermediate artifact path.
The checker imports no producer fitting, forecasting, retrieval or scoring code.
Its native path ruler is the separately implemented crossed-rule verifier.

Public queries declare skill/belief mechanics bits and proposed operations. These
are privileged forward-prediction inputs, not artifact-only historical evidence.
Native paths, training selections, hidden goals, exact endpoints, support labels,
fitted tables and score arrays belong to scientific/evaluator exports. All final
conclusions wait for the completed checker's event review.

Two independent prepared alternatives remain available under the
[crossed-rule protocol](CROSSED_REVIEW_PROTOCOL.md): prior routine versus changed
request with revision, and a transient-state mixture versus periodic resets.
Neither is an implemented or admitted job; each still needs its own fixed
schedule, controls and source admission. They do not require a primary-learning
win or another tiny-model setting. No exhaustion or week closeout is claimed.
