# Retained filter cost and storage: prepared independent alternative

What does the unknown-time/type filter's predictive improvement cost in retained
state, likelihood work and repeated queries? This is a finite B-family alternative;
no implementation or admission is claimed. It does not require provenance
restoration or a neural reader to win.

Use the completed unknown-time/type, timing and source-omission packets, keeping
their scientific estimands separate. Read every retained execution timing from
original and both replays, plan and complete hypothesis roster. Do not rerun the
scientific forecasts. Count float64 hypothesis weights, retained supplied source
records, unique likelihood factors, current-maker projections and endpoint queries
at each original checkpoint. Preserve lengths 32/128 and all five methods.

Freeze query counts 1/4/16/64 per checkpoint. Report work expressions and measured
whole-filter CPU distributions separately; the existing timings do not isolate
each primitive operation and cannot justify a measured query-time break-even.
Compute a symbolic break-even surface over query-to-update unit-cost ratios
1/4, 1 and 4, labeling it an accounting scenario. Never treat the scenarios as
benchmarks. Pair existing score contrasts with the cost record without selecting
only favorable cells or fitting costs to the observed ranking.

Controls: static state has sixteen weights; the declared unknown-time/type roster
has sixteen times one plus twice the allowed time count; duplicate source factors
are counted once only in identity-aware filtering; reset retains at most sixteen
supplied sources. Reconcile all measured timing records and exclude verification
or source-copy administration from scientific filter-call timing while keeping
them in campaign accounting. Test failure and missing timing are recorded gaps.

Export operation-count tables and timing summaries as scientific records, with
source and input hashes. No evaluator data becomes a reader input. This can
establish conditional cost tradeoffs, not a learned memory advantage or human
intent. Cap one CPU hour including controls and review within B/global limits.
Changed-primitive feedback remains a separate prepared experimental alternative.
