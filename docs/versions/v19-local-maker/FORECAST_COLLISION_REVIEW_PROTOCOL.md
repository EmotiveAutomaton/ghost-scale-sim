# Independent exact forecast-collision review

Can every exact forecast group and its uncertainty decomposition be rebuilt
from retained public forecasts and separately held native targets? Reconstruct
all 640 law/frame group bundles using sorted exact binary64 keys, scalar accurate
sums and directly centered within-law errors. Verify complete membership, zero
mass handling, every group mean and its three simplexes, all four decomposition
terms and the 5,120 accepted parent identities. Saved binary64 marginals define
exact equality only after independent scalar projection checks; no rounding merges
or clipping are permitted.

Rebuild pooled target means inside each of the frozen 10,000 paired sixteen-law
resamples, using seed 191022. Retain native and equal-frame populations, both
training draws and five paired seeds. Independently reconstruct all 2,304 estimates,
1,728 budget contrasts and 432 normalized log-budget areas from original and
reconstructed moments. An independently coded equal-mass quadratic expansion is
checked against direct scalar regrouping for every small law multiplicity. A third
regroup is required before final acceptance; full replay agreement is separate.

Known answers include opposing laws, opposing frames, identical targets, singleton
groups, one-bit differences, unequal and zero weights and permutations. Corruption
controls cover groups, membership, masses, target sums, losses, parent identities,
rosters and summary estimates. Complete synthetic handler execution must pass.
Charge tests, source freezing and this review to the existing inclusive 1,800-second
collision cap. Admit only after a full-size synthetic benchmark leaves room for
third regroup, export and closeout. The original source and outputs stay frozen.

Reader inputs contain only inherited public packets. All native targets, group
means and reconstruction arrays remain evaluator/scientific exports. This is a
constructed-method verification, not historical process correspondence or human
intent. Retrospective updating and full joint-goal forecast partitions remain
independent prepared alternatives, each requiring its own admission.
