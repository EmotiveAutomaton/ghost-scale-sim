# Independent future-schedule quotient review

Reconstruct the complete original roster, every remaining schedule and every
membership using direct bit transitions and Python tuple grouping. No producer
transition, roster, grouping, forecast or summary routine is imported. Rebuild
each saved grouped weight with accurate scalar summation; project all original
weights directly to sixteen maker states and contract the supplied endpoint law.
Check every saved future vector, source pairing, parent row mapping, population,
zero-weight group, unavailable checkpoint and summary count. Absolute numerical
tolerance remains 1e-12. Deliberate weight, forecast, shape and error corruption,
stationary identity, known deterministic forecasts and uniform laws must pass
isolated controls before admission.

Report per-posterior float64 weight bytes separately from shared int32 schedules
and membership maps. Give their combined size for the actual saved population,
including separately the metadata for unavailable requested checkpoints. Record
the strict byte break-even posterior count against original weights alone;
common parent metadata is excluded from both sides. Actual archive/JSON/NPZ
sizes are separate and do not establish workspace cost or execution speed.

Use the existing serial queue and original one-hour card ceiling, including its
tests, original run, replays and this review. Parent likelihood validity is
inherited from its bound independent reviews. No fit, new observation, protected
lineage or new scientific owner is admitted. Bounded primitive feedback and
retrospective evidence updating remain prepared independent alternatives.
