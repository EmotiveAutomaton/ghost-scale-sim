# Dependence among goals with witnessed operations: prepared alternative

Can removing dependence among inferred local goals improve a forecast after
complete public operation witnesses are enforced? The unconditioned temporal
comparison changes probability on both allowed and incompatible operations.
This diagnostic isolates goal-sequence uncertainty given the witnessed process.
It does not require the within-step comparison to win or fail.

Use only the complete-witness tier of the frozen original joint-readout roster,
all sixteen development lineages, both draws, five seeds, four budgets and four
readouts. Reuse the accepted public-mechanics restriction to the witnessed operation
sequence, normalize the original forecast on that support, and compare its joint
27-way goal distribution with the product of its three goal marginals. Retain the
unrestricted original and public-support forecast as explicitly distinct references.
Native conditional posteriors are evaluator rulers, never reader inputs. Preserve
unknown-tail expansion before restriction and report restriction normalizers.

Primary contrast: factored conditional-goal loss minus joint conditional-goal loss,
with all original proper scores, candidate coverage/size, compatible mass and each
step's goal accuracy. Preserve zero-mass rejection; do not silently add a native
support mask or new smoothing. Use 10,000 paired sixteen-lineage resamples,
seed 191008, with draws/seeds inside lineage, budget contrasts and normalized
log-budget area. Retain separate draw/seed summaries. No new fitting or sampling.

Controls: point and independent-goal identity, correlated goals with fixed
operations, opposite goal sequences with equal marginals, time reversal, zero
normalizer, wrong witness, unknown-tail contribution, candidate ties and complete
synthetic scoring. Independent scalar membership and scoring must reconstruct
all distributions, metrics, original support-decoder results and paired summaries.
Reader allowlists, source/input hashes, adjacent and extracted-source replay remain
mandatory. Expected original inputs are reused without consuming protected lineages.

Prepared and unimplemented. Admission requires tractability, frozen source and
controls within a separate inclusive 1,800 CPU-second A-family cap. It is a
constructed-method diagnostic of conditional forecast structure, not learned
historical goals, causal access, human intention or a primary-contrast promotion.
