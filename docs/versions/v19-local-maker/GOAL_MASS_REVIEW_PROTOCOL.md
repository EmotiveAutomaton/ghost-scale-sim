# Independent conditional-goal alphabet-mass review

Does redistributing training-alphabet mass explain the witnessed-goal product's
gain? This checker reconstructs the frozen comparison without fitting or sampling.
The original producer and its two replay sources remain unchanged.

Bind every input, source, environment, plan, output and separate timing record.
Use all 704 anonymous complete-witness packets, 16 development lineages, two
training draws, five feature seeds, four budgets and four inherited readouts.
Native targets and 48 reference score rows are inherited accepted evaluator
inputs; independently reproduce their joint entropy as an additional check.

Independent integer labels and accurate scalar sums reconstruct the 27 goal
probabilities conditional on public operations, three goal marginals and their
product. Partition by inherited training-alphabet membership, never truth.
Reconstruct both group masses and the mass-matched forecast preserving each
original group conditional. Verify all saved binary64 normalizers, marginals and
group masses within 1e-12 before their use in executed tie decisions. Also verify
normalized probabilities, so tiny absolute masses cannot hide relative errors.
Zero groups must preserve exact support; positive requested mass without an
original conditional is impossible. Empty groups have explicit identity behavior.

Reconstruct every score in all 7,680 strata, reproduce all 5,120 accepted restricted
and goal-product rows within 1e-10, and check the scalar logarithmic-loss identity:
the mass-matched minus restricted loss is the native weighted negative logarithm
of the product-to-original group-mass ratio. Check all 112,640 learned frame
forecasts, not a sample. Prediction, compatibility, candidate coverage, abstention
and per-step accuracy remain separate; native truth is never a reader input.

Independently regroup reconstructed and original rows. Use scalar within-lineage
means and bootstrap multiplicity counts, 10,000 paired resamples with seed 191010,
keeping two draws and five feature seeds within each of 16 lineages. Preserve
32 budget contrasts, eight normalized log-budget areas and 48 metric means,
including separate draw and seed variation. Intervals condition on retained fits.

Require known group identities, scalar loss decomposition, uniform/point masses,
empty/zero/tiny groups, candidate ties, full synthetic execution and deliberate
score, mass, marginal, operation, reference, reader, summary and roster corruption.
Runtime and portable dispatch are tested in an isolated source checkout. Synthetic
timing must fit the inclusive 1,800 CPU-second A card, including prior execution,
replays, tests, failures, review and operating overhead. Keep the 96-hour ceiling
and 16-hour reserve. The existing serial queue owns execution and its completion
event owns numerical adjudication. Retrospective updating and goal-decision
decoding remain prepared independent alternatives. No confirmation or historical
goal, human-intent or primary-bank claim follows merely from lower forecast loss.
