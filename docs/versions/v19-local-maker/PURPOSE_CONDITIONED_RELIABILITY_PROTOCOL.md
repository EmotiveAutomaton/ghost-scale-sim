# Declared-purpose reliability: prepared independent alternative

Does calibration averaged over public task requests conceal opposing errors within
each request? Requested purpose is an observed input, not proof the maker adopted it.
This finite D-family diagnostic is independent of operation and endpoint grouping:
the same endpoint can occur under either request. It is prepared, unimplemented and
unadmitted; no registry entry asserts an executable handler.

Use the accepted goal-decision forecasts with the unchanged sixteen development laws,
two draws, five seeds, four budgets, four readouts, two forecasts, three positions,
three goal classes and both native and equal-frame populations. Group only by the
two legal public requested-purpose values, retaining both even if empty. Freeze ten
integer-tenth bins. Preserve joint group/bin mass, forecast and native-target sums,
conditional signed errors and empty means. Report absolute discrepancy before and
after pooling the two requests. Do not condition on hidden governing purpose, actual
goals, correctness or an outcome-selected subgroup. No new fit or samples.

Seed 191018 fixes 10,000 paired law resamples, keeping all draws and seeds inside
each law. Preserve all means, within-readout gaps, budget contrasts and log-budget
areas without adding a practical threshold or architecture ranking. Required controls
include native self, opposite and identical group errors, one-group identity, empty
groups, exhaustive public request assignment, target-field rejection, permutations,
bin edges and mass conservation. Independently rebuild every group/bin array and
parent identity before numerical acceptance. Both complete replays are required.

Freeze an executable roster and source only after synthetic timing admits tests,
failures, original execution, both replays and independent review within an inclusive
1,800 CPU-second D card. No family extension is implied. Retrospective updating is
the independent B alternative. Reader inputs remain separate from scientific/evaluator
forecasts and targets. This diagnoses constructed calibration across task contexts;
it establishes neither historical adoption of a request nor human intent.
