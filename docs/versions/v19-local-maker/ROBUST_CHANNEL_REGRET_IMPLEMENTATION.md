# Robust cue-channel regret: executable contract

Can a fixed contingent storage policy protect against uncertainty about cue
reliability? Keep the 28 admitted mask libraries and 15 quarter-grid source
mixtures. Enumerate every three-label deterministic policy. The source mixture
is correctly supplied; cue reliability is unknown in the closed interval from
one-third to one. Each selected mask must fit its own byte budget.

For each policy retain its exact rational score line in reliability. Minimize
the maximum gap from channel-informed optimal expected coverage. The gap is
convex piecewise affine, so its maximum is attained at an endpoint. Preserve
every tied optimum and choose the lexicographically largest policy. Compare the
optimal fixed mask and the policy optimal at two-thirds correctness. Retain
endpoint and two-thirds scores, expected regret, expected bytes, maximum realized
bytes and maximum realized coverage loss against the best mask for each latent
prior. Expected regret and realized loss are different quantities.

Validate endpoint reduction independently at every pairwise score-line
intersection and endpoint. Controls include a known compromise, identical-prior
null, point interval, label/prior permutation, interval widening, fixed-policy
inclusion, exact tie sets and corrupted charges. Preserve all original 57,344
roster/budget rows, joining to 15 mixtures for 860,160 evaluations. These joins
are repeated structural questions, not new independent observations.

Freeze source, inputs and environment before the original and two full replays.
Independent acceptance must reconstruct ancestor rosters/costs/prior optima and
all 420 robust problems, verify every policy/tie/score/charge, and preserve paired
and equal-law summaries. All work must fit the inclusive 1,800 CPU-second cap.

No fitting, new observations, protected lineages, fee or reader input. This is
finite supplied-law policy robustness, not attained forecast accuracy, learned
access or process correspondence. Precision-choice stability and randomized
channel-regret protection are two prepared alternatives.
