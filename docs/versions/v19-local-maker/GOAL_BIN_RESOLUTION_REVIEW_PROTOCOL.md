# Independent fixed-bin resolution review

Can probability-bin resolution hide opposing local-goal forecast errors? Rebuild
the frozen five-, ten- and twenty-bin diagnostic from the accepted goal-decision
forecasts. Use ordered scalar comparisons against integer-ratio binary64 edges,
then accurate scalar sums independently of producer indexed accumulation. Keep
probability one in the last bin. Do not clip, renormalize, select new bins or fit.

Check every saved probability and bin membership, all packed bin weights,
forecast and native-correctness sums, NaN means exactly at zero mass, refinement
increments and unbinned squared errors. Independently verify coarsening of sums
and mass conservation. Finer partitions cannot lower absolute discrepancy beyond
the frozen negative-roundoff tolerance. Native self-forecasts must calibrate
exactly. Reconstruct native forecast arrays and native binned arrays as well.

Verify goal marginals using scalar base-three sums. Recreate the native matrix
projection and check it against scalar sums before exact bin assignment, preserving
the producer's binary64 boundaries. This is not an independent numerical-library
audit. Witnessed operations constrain native support but do not choose bins.
Reconstruct all 5,120 parent fine-loss and modal-goal-accuracy identities.

The population remains sixteen development laws, two training draws, five fit
seeds, four budgets, four readouts and two forecasts. Native and equal-frame
weighting, three positions and three goals remain separate: 92,160 metric rows,
320 array bundles, 225,280 frame forecasts and 704 reader packets. No new samples,
protected lineages or shared model settings are consumed.

Regroup original and reconstructed rows separately using accurate scalar law
means and bootstrap multiplicities. Seed 191019 fixes 10,000 paired resamples
of sixteen laws, retaining both draws and five seeds within each law. Reproduce
576 means, 1,152 within-readout refinement estimates, 2,592 budget contrasts and
648 normalized log-budget areas. A third direct-index regroup of original rows
remains required at final numerical acceptance. Intervals condition on retained
fits; no practical-impact threshold or architecture winner is added.

Before source admission, test opposing and identical errors, native self, all
exact and adjacent bin boundaries, probability one, empty means, permutations,
mass and coarsening conservation, faulty assignments, full synthetic execution,
roster failures and corrupted inputs, arrays and summaries. Time full-size
synthetic reconstruction and preserve failures. Tests, original, replays, checker
and operating work share the existing inclusive 1,800 CPU-second D card.

Use the existing serial queue, CPU only, below-normal priority and one numerical
thread. Original sources remain frozen. Preserve reconstructed arrays and scores
separately from reader packets. Execution and replay acceptance is distinct from
numerical acceptance. This is an exploratory constructed-method diagnostic,
miniature — architecture untested beyond the declared roster. Historical goal
correspondence and human intent remain unestablished. Retrospective updating and
squared-error decomposition remain prepared, unimplemented independent alternatives.
