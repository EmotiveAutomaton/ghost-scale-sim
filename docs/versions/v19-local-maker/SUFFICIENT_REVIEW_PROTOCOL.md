# Independent joint retrospective state review

Reconstruct every native retained posterior's joint future-group/past-state mass
without importing producer transition, grouping or Bayes kernels. Decode maker
bits independently, reconstruct complete future paths, and validate every saved
support and membership. Scalar indexed accumulation reconstructs group and joint
masses, including zeros. Direct full-hypothesis matrix products independently
condition mixed groups; singleton identities are verified exactly. Separate
past-state marginals reconstruct every report denominator.

Reconstruct the saved-table update for all reports and compare with the independent
full-hypothesis result. Check every impossible report, every original discrepancy,
all storage counts, and complete timing coverage. Binary64 discrepancies from
different summation orders are checked against the frozen 1e-12 tolerance; they
are not required or claimed to be bit-identical. The all-future probability bound
is total variation of updated group weights, not a measured forecast maximum.
Small controls expand every updated group and future coordinate independently.

Keep all eight laws, both source conditions, both observation draws, and every
available checkpoint. Preserve the unavailable 128-horizon checkpoints explicitly.
Regroup the complete 128 rows per law/source/checkpoint/draw before combining
draws within laws. The finite sufficiency/storage census needs no sampling interval.
The timing record covers construction and all queries in a fixed 32-row batch;
it does not measure individual query latency or include file serialization.

All evidence is scientific/evaluator data. No reader input, fit, new observation,
protected lineage or service is added. Source-admit and time the full synthetic
structural roster within the existing 3,600-second inclusive card cap and B-family
budget. Preserve both complete producer replays. A separate event-owned regroup
and scientific write-through are required before numerical acceptance.
