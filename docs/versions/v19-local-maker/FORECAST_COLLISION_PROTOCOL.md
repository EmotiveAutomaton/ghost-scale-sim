# Exact forecast collisions: prepared independent alternative

Does one frozen learned goal-probability vector represent different native
goal distributions, even when it reports a precise answer? This unimplemented,
unadmitted D-family diagnostic is independent of the absolute-error envelope
and exhaustive retrospective updating. It asks about representational ambiguity
under unknown law, rather than how a chosen bin partition averages residuals.

Use the existing sixteen-law, two-draw, five-seed, four-budget, four-readout,
two-forecast roster and all 704 complete-witness frames. Group samples by the
entire three-position, three-goal forecast vector using exact stored binary64
equality. Each draw, seed, budget, readout and forecast is a separate comparison;
never group across them. Count multiplicities and singleton groups. Keep native
and equal-frame weighting separate, giving each law equal prior weight.

Within every exact forecast group, retain total mass, complete law/frame
membership and the weighted native target vector. The squared-error identity
decomposes prediction error into squared distance from the group's mean native
target plus native-target variation inside the group. The mean target is an
evaluator-only oracle reference; no learned forecast or reader input is replaced.
Report all groups, not a selected collision, and explicitly distinguish variation
across laws from variation among frames within a law. Check full probability
simplex preservation separately at each position. Numerical near-equality must
not merge groups; report no tolerance-based collision claim.

Controls: identical native targets; identical forecasts with opposing native
targets; one-bit-distinct forecasts; unequal and zero weights; permutations;
singleton groups; group/ungroup squared-error conservation; and deliberate faulty
merges. Require independent scalar reconstruction, complete replays and a third
regroup. Freeze seed 191022 and 10,000 paired sixteen-law resamples before any
outcomes. Rebuild groups within each resample using its law multiplicities,
retaining draw/seed pairs; do not bootstrap already-pooled group means as laws.

Freeze the executable grouping and source only after a synthetic full-roster
benchmark establishes the inclusive 1,800 CPU-second cap for tests, failures,
replays, regrouping and export. If it cannot fit, record the scoped tractability
limit without sampling or changing equality. No fit, tiny setting, protected
lineage, new owner or family-budget extension is authorized by this design.
This supplied-reference constructed-method result cannot establish usable learned
access, historical process correspondence or human intent.
