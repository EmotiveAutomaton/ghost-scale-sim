# Report context disclosure: prepared independent alternative

Does disclosing the query context of an unknown-source past report improve
future forecasts without identifying its source? Keep the retained complete
posteriors, distinct source rosters, eight laws, both evidence conditions and
draws. Compare the endpoint-only report with a report containing its source's
public context and endpoint. Context is one of four existing query contexts,
not a new latent label. Uniform source selection and the five fixed copying
probabilities precede outcomes. Sources within a context remain marginalized.

Enumerate all eight endpoint and all 32 context/endpoint reports. Preserve exact
supports and all future-time/context multiplicities. Measure expected future
multiclass squared-loss reduction, conditional source entropy and report
alphabet size. A refinement cannot worsen expected proper loss. Require direct
scalar Bayes, one-hot loss, constant-context identity, distinct-context positive,
sparse-law and permutation controls. No learned provenance or historical
correspondence follows from supplying this additional evidence.

Prepared only: implementation, full-support synthetic timing, controls and
source-frozen admission remain required. Inclusive prospective cap is 3,600 CPU
seconds within the existing B/global limits, including failures, tests, both
complete replays and independent reconstruction. No source sampling, new
observations, protected lineages or fits. Two-report dependence remains the
other prepared alternative and requires its own complete-support admission.
