# Independent review of finite reply uncertainty

Reconstruct all 31,744 field/reply sets from the verified parent's native masses,
legal completions and independently implemented endpoint mechanics. Scalar Bayes
sums at the three fixed accuracies must reproduce every saved candidate,
modeled reply mass, impossible-candidate mask, pairwise distance and endpoint bound.
Conventional fallback forecasts remain stored and excluded from compatible sets.
Check all 384 population cells, complete reader allowlists and all denominators.
Exact binary64 equality is checked on the stored representation separately from
scalar reconstruction within 1e-12; numerical tolerance is not exact ambiguity.

Report 336 mean estimates using 10,000 paired resamples of the eight development
laws with fixed seed 190982. Independently regroup the original summary cells
before numerical acceptance. Preserve native versus equal-query populations;
do not average the three actual reliabilities into a reader prior. Complete
adjacent and extracted-source replays, source/input/output hashes and separate
reader/evaluator exports remain required. Corruption controls cover candidates,
masks, distances, bounds, summary values, omissions and hidden reader fields.

The checker shares the original 1,800-second card ceiling, including its failed
attempt and repairs. It consumes no new samples, model settings or protected
lineages. A successful reconstruction validates a supplied-law method in this
finite population, not learned access or historical process correspondence.
