# Independent forecast-horizon reconstruction

Reconstruct the admitted current, near and far forecast errors without importing
the producer evaluator, selector, span slices, summarizer or transition operator.
Select source-time ranks with exact rational midpoints; reconstruct bit schedules,
joint group/report Bayes numerators and every future time/context/endpoint.
Explicit expected one-hot squared losses independently check probability-distance
regret. Source mass, original-prior remainder and copy counts remain complete.

Classify each future offset directly: zero is current, positive offsets at most
half the count of all forecast times are near, and the remainder is far. Retain
all three disjoint spans. Empty spans are raw NaN and JSON null, never zero-valued
observations. Count-weighted span regret must reproduce whole-future regret.
Maximum errors have no corresponding averaging identity. Verify each support mask,
all retained source identities, histograms, group total variation and storage.
Recount mixed joint cells with structural sets; deterministic cells reuse group
weights. Reference execution timing does not establish compact-state latency.

Bind every source, input, environment, plan, deterministic output and separate
timing. Preserve all 28,672 posteriors, 505,856 sources, 143,360 summary rows and
1,146,880 shared report queries. Eight laws, both evidence conditions, two draws,
seven structural cells and five copy probabilities stay paired. Each of 1,120
draw strata contains all 128 Cartesian identities; all 48 unavailable checkpoint
strata remain absent. Reconstruct all 83 metrics. Separate completion-event review
must regroup ORIGINAL rows into 560 law strata and 70 population cells, with both
draws inside each equally weighted law and both spaced-minus-recent contrasts.
Missing span values must be uniformly missing within their applicable strata.

Controls cover dense/sparse/constant/disjoint laws, source permutation, full and
certain-copy identities, rational selection, independent span coverage through
129 forecast times, empty/one-step futures, weighted recombination, corruption of
every raw field and NaN mask, summary/roster/timing/input faults, native integration
and portable dispatch. Full-support synthetic timing covers all seven structures
and both source densities, including decompression and complete 32-row batches.
The original 3,600 CPU-second inclusive limit covers prior attempts, three
executions, controls, reconstruction, separate regroup and operations. No fit,
new observations or protected lineages. No practical margin or confirmatory
interval is frozen. Numerical acceptance requires completion and regroup.

This is a supplied-law constructed-method test. Learned provenance, historical
process correspondence and human intent remain unestablished. Highest-probability
source selection and query-specific retention remain prepared independent
alternatives; their own implementation and admission are still required.
