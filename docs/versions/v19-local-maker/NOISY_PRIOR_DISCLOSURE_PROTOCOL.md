# Noisy prior disclosure: prepared finite alternative

Does an imperfect cue about source-prior identity retain the value of truthful
prior disclosure? Use the existing 28 mask libraries and 15 mixtures in quarters.
Compare a symmetric three-label cue with correctness probabilities 1/3, 2/3 and
1. Incorrect labels split the remaining probability equally. These are declared
channels, not learned reliability or calibrated human reports.

For every cue label, choose the mask with greatest posterior expected retained
mass. Preserve rational joint and conditional weights, all scores and ties,
largest-mask choices, zero-probability labels, expected coverage and individually
feasible bytes. Compare no cue, the correct channel-aware reader, and a reader
that treats the noisy label as certain. Score the latter under the actual channel.
This separates information value from damage caused by misinterpreting a cue.

Controls: uninformative-cue identity, perfect-cue identity, identical-prior null,
known misleading-certainty case, prior/label permutation, exhaustive policy
enumeration and malformed-channel rejection. Retain all original population
joins, paired/equal-law summaries, source/input/environment bindings and two full
replays. No new sampling, fitting, protected lineages, acquisition fee or reader
inputs. The gross-coverage result cannot establish forecast or process accuracy.

Prepared, unimplemented and unadmitted. Full-support timing and frozen source are
required before admission. A prospective inclusive 1,800 CPU-second cap competes
inside the existing family/global budget. Precision-choice stability remains a
separate prepared alternative. Neither protocol is an implemented queue job.
