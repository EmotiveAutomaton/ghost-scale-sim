# Independent source identity omission review

The frozen comparison requires complete reconstruction before numerical acceptance.
Rebuild native path probabilities, endpoint laws and original midpoint/stationary
streams. Independently reproduce the identity-aware parent posteriors, forecasts,
scores and cells. Replace only source identity by presentation position, retaining
content and both timestamps. The independent checker never calls the producer's
filtering, scoring, stream generation or identity transformation functions.

Use scaled likelihood products to verify all five hypothesis rosters, priors,
source-time likelihood assignments, query-time state projections, float64 joint
arrays, row mappings, type masses, forecasts and scores. Check every true and
supplied source count, plus the distinct true sources actually retained by the
last-sixteen-supplied-source reset. Independent streams must retain exactly the
parent's stored forecasts and scores, beyond numerical reconstruction tolerance.
Reader inputs contain only context, endpoints, supplied identities and timestamps.

Average sixteen makers equally within draw, then two draws within each of eight
paired coefficient lineages. Retain every length, reference factor, actual change,
source condition and checkpoint. Report 440 arm means with five measures, every
within-omission arm contrast and every same-arm omission-minus-identity-aware
contrast: 6,600 metric contrasts. Preserve draw means, paired lineage values and
95% percentile intervals from 10,000 resamples with seed 190501. The 0.02-nat
practical margin applies only to logarithmic losses. No-change reference-factor
cells are paired controls, never added as independent evidence.

Known-answer controls cover neutral laws, counted-twice likelihood ratios, direct
scalar products, a copy spanning the possible change, conflicting provenance,
impossible evidence, paired strata, a complete native fixture with both draws,
reset source counts, reader truth rejection, parent-score and joint-map corruption.
Both complete replays, source/input/environment bindings and scientific/reader
exports must verify. This checker shares the source-omission card's four-hour cap,
including tests, failures, replays and administration, within B/global limits.

This is supplied-law constructed-method verification, miniature — architecture
untested. Shared timestamps may reveal dependence to another reader; these fixed
filters use supplied identity. Prediction and current-state coverage do not prove
historical correspondence, learned provenance detection or human intent. Bounded
changed-primitive feedback and partial provenance restoration remain independent
prepared alternatives, each requiring separate implementation and admission.
