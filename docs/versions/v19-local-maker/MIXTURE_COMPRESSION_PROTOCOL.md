# Bounded retained-mixture compression: prepared independent alternative

Can a smaller set of complete maker/change hypotheses preserve useful prediction
and uncertainty at a fixed checkpoint? This B-family alternative is prepared;
no handler, admission or outcome is claimed. It is independent of likelihood
tempering and changed-primitive feedback.

Reuse the completed identity-aware unknown-time/type mixture and the source-omitted
counterpart: all eight development lineages, two saved draws, sixteen makers,
lengths 32/128 and original checkpoints. Preserve the complete hypothesis roster
and compare retained counts 1/16/64/256/all. Sort by saved probability, breaking
exact ties by the original roster index; keep the selected weights and renormalize.
Use this deterministic selection only, with no outcome-based capacity choice.
The one-hypothesis condition is a coherent point account, not independent marginals.

Score endpoint logarithmic loss, current-maker logarithmic loss, true-state mass,
90% tie-inclusive set inclusion and set size. Preserve removed probability mass,
number of complete hypotheses and weight/index bytes separately. Average makers
within draw and draws within lineage. Pair each size against the complete mixture
and against its identity-aware counterpart. Use the existing 10,000 paired
eight-lineage bootstrap, seed 190501, and 0.02-nat loss margin; retain draw means,
all strata and stationary aliases. Do not present reused streams as replication.

This tests checkpoint compression only. Retaining the exact sixteen-state current
maker marginal is an exact fresh-endpoint forecast control, but it does not retain
all uncertainty about past/future changes. Do not claim recursive sufficiency,
historical process identity, learned memory, or human intent from that identity.

Controls before execution: all-hypothesis identity; normalized nonnegative weights;
known two-hypothesis ambiguity where point selection loses coverage; deterministic
ties and index relabeling sensitivity; independent scalar projection and scores;
zero-weight removal; unchanged current-marginal endpoint predictions. Pin parent
receipts, source, complete rosters, arrays and all outputs. Reader packets remain
the unchanged parent inputs; hypotheses, selection, truth and scores stay evaluator
or scientific evidence.

Cap two CPU hours including controls, replays and independent review, within the
B-family/global allowance. No new fits, tiny settings, protected lineages, or
observations. Source admission and available family budget are required before
execution. Primitive feedback is the other independent prepared alternative.
