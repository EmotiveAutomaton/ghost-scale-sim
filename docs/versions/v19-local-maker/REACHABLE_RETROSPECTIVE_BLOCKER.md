# Reachable prior-conditional retrospective comparison: timing blocker

The complete prior-conditional quotient rival remains scientifically useful, but
its implemented full native comparison is not admitted under the 3,600 CPU-second
inclusive cap. No native posterior outcome from this handler was consumed.

The initial full-support synthetic model estimated 1,580 CPU seconds per execution
and 8,204.71 inclusive of three executions, independent review and administration.
One bounded exact sparse-transition optimization estimated 2,140 per execution and
11,024.21 inclusive. Both estimates include all 256 rows, two source conditions,
eight laws, every available checkpoint, every report and future coordinate. They
are scaled engineering estimates, not scientific outcomes or actual charges.

Both immutable test snapshots, timing evidence and charges are retained. The
implementation passes expanded scalar Bayes and future-coordinate controls,
impossible-report and copied-source controls, a complete synthetic handler roster,
and source corruption checks. Its resource failure is scoped to the current
implementation. No sampled replacement, new observation, fit or cap extension
is admitted. Any further repair needs a newly identified computational cause.

The independent joint retrospective sufficient-state comparison proceeds with
its own source, controls and timing. Source uncertainty and two-report dependence
remain prepared alternatives; the forest and research week remain open.
