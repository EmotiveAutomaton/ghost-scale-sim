# Matched-count context-balanced retention: prospective admission contract

Does coverage of query contexts reduce retrospective forgetting at the same
retained-source count? The retained forgetting comparison locates a descriptive
loss from recent-source windows. This successor changes only which sources retain
their joint past-state/future-group tables, using no outcomes to choose them.

Use all eight development laws, both evidence conditions, two draws, all 28,672
complete posteriors and 505,856 source identities in the accepted parent. Keep the
seven available checkpoint/horizon cells and the 48 unavailable strata. No new
sample, fit or protected lineage is admitted. The five copy probabilities and
eight endpoint reports are unchanged. Full retention is the reference.

For half-time and quarter-time recency, retain source times strictly above
floor(checkpoint/2) and floor(3*checkpoint/4). Match each resulting source count
with context round-robin selection: visit labels 0,1,2,3, taking each context's
most recent unselected source and skipping exhausted contexts. Stop at the fixed
count. As a prospective global label-permutation sensitivity check, repeat with
the fixed reverse order 3,2,1,0. These are two declared orders, not a search or
exhaustive permutation average. Equal source counts need not give equal storage.

The seven compared states are full retention; half and quarter recency; half and
quarter balanced retention in each of the two fixed orders. Retain all original
source mass. Forgotten sources use the original uniform sixteen-maker prior with
their actual context counts. All observed copy counts remain, including forgotten
sources. No hidden state, report informativeness or measured score affects selection.

Architecture is exact supplied-law Bayes with a finite, mass-preserving remainder;
there are zero fitted parameters and no outcome labels used for training. Complete
posterior, law and source identity are evaluator-only inputs. Every future time,
context and endpoint is scored. Report exact unsupported mass separately from
supported expected squared regret, maximum probability error and group total
variation. Retain NaN masks, selection identities, remainder histograms and all
float64/int32 storage counts, including shared prior entries.

Average exact report probabilities within each posterior, 128 Cartesian identities
within each draw, two draws within each law, and eight laws equally. Keep evidence,
checkpoint, horizon, copy probability and retention state separate. Compare balanced
minus recency within paired cells. No practical margin or confirmatory interval is
frozen; a descriptive advantage is not established practical importance.

Before native execution require scalar Bayes and explicit one-hot loss fixtures,
dense/sparse/constant/disjoint laws, older informative context, identical roster
and single-context identities, source-order invariance with times preserved,
outcome-blind selection, exact remainder mass, the fixed global reversal and native
input/roster corruption controls. Verify both native and portable dispatch.
Complete-support synthetic timing covers every structural shape and source density,
including evaluation and serialized raw outputs. The inclusive 3,600 CPU-second cap
covers tests, failures, original execution, both complete replays, independent
reconstruction and regroup. Require independent numerical reconstruction before
acceptance. No change to the 24-hour B-family, 80-hour exploratory or 96-hour total
ceilings; sixteen hours stay reserved.

Two-report dependence and time-balanced source retention are prepared independent
alternatives. Failure or null on this card does not close the research week.
