# Independent learned sequence-factorization review

Reconstruct every saved goal-sequence and operation-sequence marginal by scalar
integer-label membership and accurate sums. Independently multiply those marginals
and compare all 5,832 probabilities. No producer factorization, distribution,
score or regroup routine is imported. Frozen fitted forecasts and accepted native
targets remain inherited inputs; this review does not refit or resample episodes.

After verifying each saved marginal within 1e-12, score the product of the saved
binary64 marginals. This preserves the forecast that was actually executed when
roundoff breaks a mathematical tie. Rank using independent lexicographic sorting:
descending probability, then original alphabet priority followed by unseen labels.
Native reference arms use ascending full-label priority. The 90% set ends at the
first cumulative mass reaching 0.9. Never adjust probabilities to match a score.

Reconstruct all logarithmic and squared losses, excess losses, native compatible
mass, nominal candidate coverage and size, unsupported modal assertions, confidence,
abstention, unknown probability, truth outside the alphabet and all per-step local
goal/operation accuracies. Include the separate native joint and product rulers.
Reproduce every original joint-readout metric, not only its logarithmic loss.
All evidence tiers, budgets, draws, feature seeds and development laws are mandatory.

Regroup original and reconstructed rows separately. Use scalar within-lineage
averages and multiplicity-weighted 10,000 paired sixteen-lineage resamples with
seed 191005. Preserve all 64 budget contrasts, 16 normalized log-budget areas,
128 metric means, both training-draw means and all five feature-seed means.
Conditional lineage intervals do not quantify universal training uncertainty.

Controls include correlated and independent joints, equal marginals with different
joints, full unknown probability, zeros, integer-axis order, candidate ties,
normalization, complete synthetic reconstruction and deliberate corruptions of
scores, marginals, native targets, summaries, row coverage and reader input roles.
Freeze tests, source and full input hashes before execution. A measured synthetic
screen must fit remaining inclusive card allowance. The existing 1,800 CPU-second
cap includes all producer/replay/checker work, failures, tests and review; it is
not enlarged. One worker, one numerical thread, CPU only and below normal.

Numerical acceptance requires the completed independent checker and event review.
Retrospective quotient updating and temporal factorization remain two prepared,
unimplemented alternatives. No new fit, protected lineage or confirmation is used.
This is a constructed-method diagnostic, not historical or human intent evidence.
