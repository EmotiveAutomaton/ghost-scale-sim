# Repeated mechanical replies: prepared finite alternative

Does repeated noisy disclosure buy information only when the replies contain
independent evidence? Use the existing 640-query roster, eight supplied laws and
both tool mechanics. At reliability 0.75, enumerate all reply strings of lengths
one, two and four for each fixed field, under two actual channels: independent
noise per reply, or one noisy reply copied unchanged. No new samples or fits.

Compare no request, one request and the two repeated-request budgets. Contrast
correct channel conditioning with treating copies as independent. Keep the field
fixed before the first reply; report skill and belief separately. Enumerate full
legal completions and native versus uniform conditional laws, and score native
and equal-query populations. Charge every returned reply at prices 0, 0.02 and
0.1 nats; include an explicitly separate one-acquisition copied-display cost
diagnostic. Never count an averaged forecast as an expected realized score.

Retain request counts, expected logarithmic and squared forecast losses, true
endpoint probability, residual ambiguity and infinite-loss mass separately.
Controls require copied-reply identity under the correct channel, the reliability
one identity, a reliability-half no-information fixture, known scalar Bayes
updates, support/fallback checks and a deliberately corrupted likelihood table.
Bind all complete reply strings, probabilities, posterior forecasts and inputs.

The finite enumerator now includes a zero-request baseline and both known-channel
and independence-assumed readers for every actual channel. All binary strings
are retained, including mixed copied strings with zero actual probability.
At zero modeled reply mass use uniform likelihood-supported legal completions,
or the whole legal group if the supported set is empty, as in the single-reply
parent. Reader packets expose the declared source relation, reply sequence and
reliability; truths, likelihood tables and conditional laws stay evaluator-only.
No result has been consumed. Before dispatch freeze the enumerator, source and
complete query roster, then pass isolated controls. Its
half-hour cap includes controls, both complete replays and independent numerical
review within the existing F/global allocation. No tiny setting, protected
lineage or resource extension is admitted. It is independent of retrospective
evidence after future-schedule grouping; it needs no noisy-disclosure win.
The result would concern constructed information and provenance, not human
report reliability, learned access or historical intention.
