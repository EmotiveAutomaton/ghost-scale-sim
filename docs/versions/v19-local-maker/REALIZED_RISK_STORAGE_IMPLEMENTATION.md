# Realized-risk storage: prospective implementation contract

The prepared protocol asks whether expected source coverage gains survive a
guarantee on every realized storage mask. Freeze the same 28 roster/cost/budget
problems, three supplied priors and verified finite mask library. For each problem
compare floors of zero, half and all of the deterministic maximum-minimum mass.
Reject any mask below the floor under any prior. Every surviving mask separately
meets the unchanged byte budget. The deterministic baseline remains feasible.

Optimize minimum expected mass using exact rational basic-lottery enumeration.
Retain all optimal basic vertices; select the lexicographically largest rational
weight vector in ascending mask order. The representation enumerates basic optima,
not the full continuum of optimal mixtures. Maximum support is three masks.

No fitting occurs. Architecture is exact finite-library optimization; capacity is
the complete frozen library at all three floors. Teacher inputs are evaluator
source rosters, structural costs and three prior vertices, with no forecast labels.
Schedule: eight development laws, two paired source draws, two evidence roles,
seven checkpoints and two byte budgets. All 28,672 source rows yield 172,032 output
rows across 84 distinct risk problems. These repeat structural answers across
laws and are not independent replications. No new observations, protected lineages,
tiny settings or reader inputs are consumed.

Preserve exact survivor/exclusion sets, all weights, expected masses, realized
mass floors, worst realized mass, expected gain over the deterministic baseline,
cost relative to unrestricted randomization, tie sets, actual byte charges and
source counts. Average paired draws within law before equally weighting laws.
Expected objectives and realized guarantees remain separate.

Admission requires live gain and known risk-cost controls, zero-floor identity,
empty/full controls, monotone feasible sets and optima, degenerate ties, input and
prior permutations, corrupted cost/mask/baseline/floor rejection and independent
full-constraint rational vertex reconstruction. Full-support synthetic timing
includes all three floors, serialization and hashes. The inclusive 1,800 CPU-second
cap covers admission, original, two full replays, independent review and write-through.
Frozen source and admission precede scientific outcomes. Existing serial ownership
executes all jobs. No forecast-accuracy, learned-provenance, historical-process or
human-intent claim follows from a storage objective.

Precision-choice stability and prior-informed lottery value remain independent
prepared alternatives. Their protocols are declarative until implementation and
admission; no exhaustion or occupancy claim.
