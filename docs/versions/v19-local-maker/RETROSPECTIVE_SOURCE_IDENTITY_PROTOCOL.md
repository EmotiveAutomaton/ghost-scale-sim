# Unknown identity of a retrospective report source: prepared alternative

Does averaging source-conditioned posteriors lose the evidence needed to infer
which old source a report concerns? This unimplemented independent B-family leaf
uses the same retained posteriors and every true distinct old source. A hidden
source is selected uniformly among those sources, then copied with one of the
five existing copying probabilities or independently emitted from its past state.
Every report endpoint is enumerated. The latent source roster is evaluator-supplied;
no claim of learned provenance follows.

Compare exact likelihood marginalization over sources before Bayes normalization
with uniform averaging of normalized source-specific posteriors and a most-recent
source decision. Exact posterior source weights depend on report likelihood.
Keep impossible report/source pairs and fixed-source support failures separate.
Report full future-coordinate discrepancies, source uncertainty and conditioning
cost; average report discrepancies with the declared marginalized report law.
Preserve all identities and multiplicities, pairing both observation draws within
each of eight laws. Include equal-likelihood and single-source identity controls,
unequal-likelihood positive controls, duplicates and impossible reports.

No execution is admitted by this document. Full-support synthetic tractability,
frozen implementation/inputs, isolated controls, both complete replays and independent
scalar reconstruction are required under a prospective inclusive 3,600 CPU-second
cap within the current B-family/global allowance. No fits, new observations,
protected lineages, sampled source identities or timing repair without an identified
cause are authorized. Two-report dependence remains an independent alternative.
