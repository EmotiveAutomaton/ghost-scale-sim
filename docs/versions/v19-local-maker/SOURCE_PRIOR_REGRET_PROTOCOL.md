# Source-prior regret: prepared finite alternative

Does protecting minimum retained mass differ from minimizing opportunity loss
against a prior-informed storage choice? Use the same frozen source rosters,
structural byte costs and finite candidate library as the robust source-prior
comparison. Freeze before outcome consumption: the same three uniform,
time-proportional and reciprocal-time priors, both existing budgets and exact
recent-time bit-vector tie rule. No new histories, outcomes, fits or lineages.

For every candidate, compute exact rational regret at each prior vertex against
that prior's unrestricted feasible retained-mass optimum. Minimize the maximum
vertex regret, retain every tie, and report minimum-mass tradeoffs against the
maximin choice. Maximum regret over the convex uncertainty set occurs at a vertex:
the unrestricted optimum is a maximum of linear set scores, so regret is convex.
Do not claim unrestricted minimax regret optimality for this candidate library.

Controls: exhaustive small subset libraries and rational prior mixtures, a known
maximin-versus-minimax-regret disagreement, identical priors, all-fit and empty
budgets, label permutations, exact ties and corrupted parent optima. Freeze source,
architecture, capacity and evaluator labels before execution. Complete-support
timing and original plus two full replays, independent rational reconstruction,
original-row paired regroup and full documentary write-through must fit a separate
inclusive 1,800 CPU-second cap within the existing family/global limits.

Prepared, unimplemented and unadmitted. No reader inputs. This is a storage
decision with specified uncertainty, not a forecast-optimality, learned-access,
historical-correspondence or human-intent result. Precision-choice stability
remains an independent alternative.
