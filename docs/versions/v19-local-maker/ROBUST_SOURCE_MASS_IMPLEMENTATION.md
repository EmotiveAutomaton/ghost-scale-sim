# Uncertain source prior: frozen implementation contract

Implement the prepared source-prior robust mass protocol using the accepted
source-mass frontier's complete 28,672 source rosters, seven checkpoints, eight
development laws, two draws and paired evidence conditions. Copy the verified
parent selection library and original structural/roster provenance as inputs.
No endpoint, likelihood, forecast, new fit, observation or protected lineage.

For each of 28 distinct roster/cost/budget problems, retain the recency,
probability-greedy, probability-per-byte and exact-optimum masks under each of
three priors. Deduplicate masks while retaining all twelve policy identities.
Recompute normalized masses with exact fractions under uniform, time-proportional
and reciprocal-time priors. The candidate's minimum mass over every convex
mixture equals its minimum over these three vertices. Select the greatest such
minimum; retain all tied masks and use the largest recent-time bit vector for
the reported choice. All three prior-specific optimum tie sets, rational masses,
regrets against the verified unrestricted prior optima, costs and identities stay
in the selection record. This is a candidate-library optimum, not unrestricted
minimax knapsack optimality.

Architecture/capacity: at most twelve identities per finite library, exact
fraction comparisons, two original byte budgets and 57,344 original output rows.
Teacher labels are the supplied three prior definitions and structural costs.
Schedule is a single complete pass over the existing paired population, followed
by adjacent and extracted-source complete replay. There is no training schedule.

Controls precede outcomes: exhaustive feasible subsets on small fixtures,
all eighth-grid two-prior convex mixtures, prior/item permutations, exact ties,
all-fit/empty controls, a prior-specific winner rejected by robust choice,
corrupted prior normalization and over-budget candidates. Complete-support timing
must include all rows, candidate mass evaluation, serialization and hashing.
The separate inclusive 1,800 CPU-second cap includes failures, controls, all
executions, independent reconstruction, paired/equal-law regroup and write-through.

Independent review must reconstruct parent optima, source rosters and structural
costs, every rational mass and regret, candidate identity and tie, and all 57,344
rows. Confirm original and both full replay bindings. Export scientific evidence
separately; no new reader inputs. Forecast optimality, learned provenance,
historical process correspondence and human intent remain outside this method.

Precision-choice stability and source-prior minimax regret are two separately
prepared alternatives; neither is an implemented/admitted job merely by being
listed. Original per-card ceilings and failed attempts remain unchanged.
