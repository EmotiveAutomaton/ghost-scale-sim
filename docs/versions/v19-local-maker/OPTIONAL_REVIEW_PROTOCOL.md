# Independent optional-request reconstruction

Reconstruct every group law, endpoint, before/after forecast, strict price
decision, matched-rate control and expected proper score from the frozen input
files. Use independent scalar conditional sums and branch-score averaging;
never call the optional-request producer's selection or scoring functions.
Native population weights inherit the independently verified disclosure parent.
Bind all source, plan, environment, input and completion hashes before dispatch.

Preserve the exact frozen binary64 entropy values for literal price decisions.
Independently recompute entropy and record every decision changed by scalar
summation, including request-rate and proper-score sensitivity. Field choices
at numerical ties inherit the parent's qualification. Do not silently change
the selection rule to fit outcomes. Validate all 640 queries, eight laws, both
tools, both law assumptions, both populations, four policies and three prices.
Rebuild the reader packet allowlist independently; individual hidden fields and
endpoint targets are never selection inputs.

Report each cell and paired law bootstrap means and contrasts against no
request, forced requests and matched-rate requests. Use 10,000 resamples with
seed 190977; the eight development laws are the independent resampling units.
Keep zero-support fallback and infinite-loss mass explicit. No confirmation
lineage, new episode, tiny fit or learned-access claim is admitted. Verification
shares the original optional-request half-hour F-family cap, including controls
and failed attempts; it does not create another resource allocation.

Known-answer controls include informative and constant-endpoint requests,
strict price ties, random-policy scores versus averaged forecasts, impossible
targets, and a complete native fixture. Deliberate corruption must reject
forecasts, prices, decision vectors, matched rates, conditional laws, targets,
reader truth leaks and denominators. Numerical acceptance follows the complete
checker and separate regroup; replay identity alone is insufficient.

Retrospective future-schedule updating and noisy mechanical disclosure remain
two independent prepared alternatives, with their existing finite protocols
and caps. Neither depends on optional requests winning.
