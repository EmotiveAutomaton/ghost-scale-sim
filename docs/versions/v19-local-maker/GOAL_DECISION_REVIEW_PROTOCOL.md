# Independent goal-decision review

Does choosing a whole modal goal path sacrifice stepwise localization with the
forecast held fixed? Original execution and two complete replays are bound before
outcome interpretation. Independent numerical acceptance is a separate step.

Reconstruct every inherited full forecast using integer labels and scalar sums.
Check the accepted operation normalizer and conditional goal marginals, then both
27-path forecasts. Check each saved probability, marginal, risk, choice, exact tie
count and margin. Binary64 probabilities and marginals must agree within 1e-12
before adjudicating their executed modes. No tolerance creates a tie; choose the
smallest numeric label among exact maxima. Native positive support never constrains
a learned choice. Score native expected step accuracy, whole-path accuracy,
zero-support choices, forecast Hamming and path risk and unchanged logarithmic
loss. Native choices supply separate objective controls. A product forecast may
have tiny numerical differences between nominally tied paths; inspect saved
values rather than attributing every changed decision to additional information.

Reconstruct all 10,240 learned rows, 32 native rows, 225,280 learned frame forecasts
and 5,120 inherited prediction-loss strata over all 704 complete-witness packets.
Regroup original and reconstructed rows independently: 192 budget contrasts,
48 normalized log-budget areas and 64 metric means. Scalar within-lineage means
and bootstrap multiplicity counts use the frozen seed 191011 and 10,000 resamples.
Both training draws and five fit seeds stay within each of sixteen lineages;
their variation remains separate. No new fit, sample, target or protected lineage.

Before dispatch require synthetic opposing optima, equal-marginal/different-joint
forecasts, exact and near ties, point/uniform controls, scalar risks and scores,
complete miniature execution, and corruptions of every saved decision field,
references, inherited forecasts, reader fields, strata, summary and bindings.
Verify native and portable routing in an isolated source checkout. Freeze the
checker source and all inputs. The existing serial queue alone executes it.

The original inclusive 1,800 CPU-second D-family card pays for this review,
controls, failures and operating work as well as producer execution and replays.
Measure synthetic cost before dispatch; protect the existing 16-hour campaign
reserve. Retain complete scientific outputs separately from unchanged anonymous
reader packets. Numerical acceptance must await the checker's completion event.
This constructed-method decision comparison cannot establish historical goals,
human intent, selective causal access or confirmation of the primary bank claim.

Retrospective quotient updating and fixed-cost goal-report abstention remain
prepared independent alternatives with their own tractability requirements.
