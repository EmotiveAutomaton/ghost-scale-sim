# Independent temporal factorization review

The independent checker reconstructs the frozen temporal diagnostic before any
numerical acceptance. Its inputs are the complete original output and the
inherited joint-readout forecasts, native reference targets and reader packets.
The original and both complete replay snapshots remain unchanged.

For every full label, integer division and remainder separately recover three
goals and three operations. Each corresponding pair belongs to one of eighteen
symbols. Scalar accurate sums over explicit membership lists reconstruct all
three marginals, independently of the producer's tensor reshaping and reduction
axes. Every saved marginal and resulting product must agree within 1e-12.
Verified saved binary64 marginals define the product used for scoring, preserving
the actual executed tie decisions. No outcome-dependent tolerance is fitted.

The shared independent sequence checker supplies forecast expansion, native
reference validation, sparse native scoring and regrouping; it imports no
producer distribution, score or regroup routine. Descending probability and
alphabet-first priority define learned modes and candidate ties; native rulers
use full-label order. The nominal 90% set ends at its first cumulative crossing.
Every original metric, product metric, native ruler and excess over native
uncertainty is reconstructed. Unknown tail mass spans all 5,832 labels. No public
support mask, latent-policy input, fit or new sample is added.

Expected complete coverage is 20,480 learned strata, 128 native score rows,
145,920 learned frame forecasts, 14,592 native frame/law combinations and 912
reader packets. Original and reconstructed raw rows are independently regrouped
using 10,000 paired sixteen-lineage draws, seed 191006. Both training draws and
five feature seeds stay within lineage. All 64 budget contrasts, sixteen
normalized log-budget areas, 128 metric means and separate draw/seed summaries
must reproduce. Intervals remain conditional on retained fits.

Known-answer controls include correlated paths, independent-step identity,
within-step dependence, equal step marginals with different paths, integer axis
reversal, point/uniform/unknown forecasts, candidate ties, zeros and malformed
inputs. Complete synthetic execution and deliberate marginal, score, reference,
reader-role, hash, summary, missing-row and duplicate-row corruption precede
admission. A synthetic timing screen must leave room inside the existing inclusive
1,800 CPU-second A-family card. Tests and failure costs are charged; the native
single worker owns campaign numerical execution.

This checks constructed forecast structure. It inherits the previously accepted
native target generation and frozen fits; it does not retrain, establish general
training-population uncertainty, historical process correspondence, causal access
or human intent. Retrospective updating and within-step factorization remain
prepared alternatives independent of this diagnostic's sign.
