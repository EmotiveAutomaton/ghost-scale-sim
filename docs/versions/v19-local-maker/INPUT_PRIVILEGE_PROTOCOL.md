# Hidden mechanic metadata: prepared independent alternative

How much endpoint precision depends on giving the reader the maker's skill and
belief metadata? The current forward queries supply both. This finite F-family
alternative removes one or both fields from the same retained 640-query roster;
it adds no fit, episode, operator, confirmation lineage or reader architecture.

Freeze public equivalence groups from query fields alone: full metadata,
skill omitted, belief omitted, and both omitted. Retain initial artifact and
the complete three-operation sequence. Use both existing tools and all eight
development laws. A group containing distinct exact endpoints establishes
ambiguity under the supplied evidence, with full membership retained. Report
each group's native mass, endpoint distribution and conditional entropy, plus
equal-query weighting separately. Count zero-mass groups explicitly. No best
or favorable witness selection is permitted.

Compare a uniform mixture over legal hidden-metadata completions to the exact
native-law mixture, with proper endpoint scores and complete group coverage.
The latter is a supplied-law oracle, not a learned reader. Matched public
inputs must receive identical predictions. Exact full-metadata fixtures provide
the known-answer control; a deliberately merged distinct-endpoint pair and a
constant-endpoint group test ambiguity and null behavior. Check probabilities,
mass conservation and independent enumeration before admitting native results.

This diagnoses input privilege and evidence ambiguity rather than forecasting
architecture or historical process recovery. It is independent of primitive
pooling and retrospective quotient updating. The handler is unimplemented and
unadmitted. Freeze source and all memberships before outcome consumption; cap
two CPU hours including tests, replay and review within unchanged F/global
ceilings. If those ceilings cannot accommodate it, preserve it as prepared.
