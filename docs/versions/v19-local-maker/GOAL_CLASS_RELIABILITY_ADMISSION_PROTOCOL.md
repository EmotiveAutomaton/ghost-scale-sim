# Complete class-wise goal reliability: executable contract

The prepared question is whether accurate confidence in a selected goal conceals
errors in unselected goal probabilities. Use the accepted goal-decision forecasts
and native targets, independently of the modal-confidence diagnostic's sign.
Retain all sixteen development laws, two training draws, five feature seeds,
four budgets, four readouts, two forecasts, three positions and three goal classes.
No new architecture, teacher, fit schedule, samples or protected lineages are used.

Fixed integer-tenth binary64 bins retain all numerator/denominator sums and empty
means. Native weights and equal-frame weights are separate populations. Every
class receives signed forecast error, absolute bin discrepancy, squared forecast
error, expected one-versus-rest Brier loss and native uncertainty. Equal-class
means average the three class scores; multiclass Brier loss sums them. Do not
pool class bins to conceal opposite class errors. Pool laws only within class,
separately reporting pooled-bin discrepancy and average lineage-bin discrepancy.

Expected one-versus-rest Brier loss is c(1-p)^2+(1-c)p^2. Its decomposition is
c(1-c)+(p-c)^2. Sum across all three classes and independently check the expectation
of squared distance from each possible one-hot outcome. Use saved binary64
marginals after validating them against their frozen sequence forecasts. Values
within 4e-12 above one are preserved for calibration and clipped to one only for
proper Brier scoring and its decomposition. Save clipping count/mass and the
remaining normalization discrepancy; never renormalize silently. Identity
checks tolerate 1e-10 to account for admitted roundoff. Invalid probabilities,
missing classes, non-normalized weights and changed parent scores fail admission.

The complete output has 92,160 learned class rows, 288 native class rows, 30,720
multiclass decompositions, 225,280 frame forecasts and 5,120 fine-parent identities.
The existing 704 reader packets remain distinct from scientific forecasts and
evaluator targets. Fixed bootstrap seed 191015 and 10,000 paired law resamples
yield 2,160 budget contrasts and 540 areas. Two training draws and five seeds stay
inside each law. There are 576 class means and 192 equal-class means. Equal-class
means are descriptive, with no new pooled confidence interval.

Controls include correct selected-class confidence with wrong remaining classes,
known one-hot expectations, native uncertainty, uniform probabilities, permutation
of class labels, all exact/adjacent bin edges, empty bins, cancellation, clipping,
conservation failures, complete synthetic execution and frozen-input corruption.
Freeze admitted source/input/plan/environment hashes before dispatch. Original,
adjacent and extracted-source replay execute through the existing serial queue.
Numerical acceptance additionally requires a separately implemented scalar checker
and an independent paired regroup. Replays alone establish reproducibility.

Conservative timing for all three executions, independent review, tests, failures
and operating charges must fit the inclusive 1,800 CPU-second D card. Preserve
the 96-hour total and 16-hour reserve. No allocation extension or new owner.
This is constructed-method reliability, not historical goals or human intent.
