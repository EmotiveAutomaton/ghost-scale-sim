# Joint forecast partitions: implemented successor

Does retaining the full 27-outcome forecast over three local goals distinguish
native dependencies that its nine marginal probabilities discard? This D2 alternative is implemented and source admitted, independent of retrospective updating. It reuses
the admitted goal-decision roster, without a fit, new sample or protected lineage.

Group all law/frame pairs separately by exact binary64 equality of the full
27-outcome forecast and by its three three-goal marginals. Keep each readout,
forecast, budget, draw and seed separate. Retain every membership and native joint
target, native and equal-frame populations, and empty/zero-mass handling. Establish
the partition refinement relation from exact stored forecasts; a rounding-driven
exception is a numerical finding, not a mathematical information certificate.

Measure native joint-target variation left inside each partition, separating
within-law from across-law variation. Full joint targets and group means are
evaluator-only references. A finer partition is no evidence of a trained decoder
or historically correct explanation. Preserve complete paired law resampling,
with each partition rebuilt from law multiplicities; no bootstrap of pooled means.

Require equal-marginal/different-dependence fixtures, identical joint controls,
one-bit distinctions, zero weights, complete membership coverage, permutations,
direct scalar squared-error identities and deliberately faulty merging. Freeze
an executable full-scope synthetic cost model, source and exact uncertainty summary
before outcomes. The separate inclusive cap is 1,800 CPU seconds within the
existing family/global ceiling. If it cannot fit, record the scoped limitation;
do not replace exhaustive grouping with sampled collisions or enlarge a cap.

The [executable admission](JOINT_FORECAST_PARTITION_ADMISSION.md) freezes its complete calculation and uncertainty. Native execution receipts are present; numerical and replay acceptance await independent completion-event review. The prepared description above is preserved in the frozen source.
