# Aggregate report state: implementation and prospective admission

The prepared single-report question now has an exact finite implementation.
No native result is accepted. The report law, uniform source mixture, copying
probabilities and complete saved roster are fixed before execution. All eight
development laws, both evidence conditions, two paired draws, 28,672 posteriors,
505,856 source candidates and 1,146,880 posterior/report queries remain. The 48
unavailable checkpoint strata stay absent. There are no fitted parameters,
training labels, new observations or protected-lineage consumption.

For each future-equivalence group, retain its probability and its joint mass
with each of the eight independent-report endpoints, averaged over all eligible
sources. Retain eight integer counts for copied endpoints. For each copying
probability, mix the copied group masses with the independent joint table and
normalize. A separate full-hypothesis contraction supplies the exact reference.
The implementation measures every future coordinate through lossless sparse
schedule increments; total variation is reported separately from forecast error.
Unsupported reports have explicit masks and undefined errors.

Aggregate state uses nine float64 values per group and eight int32 counts. The
per-source comparison reuses deterministic group masses and stores all mixed
group/past-state masses, structural group/state indices, source metadata and eight
copy counts. Neither method needs a forgotten-source remainder. Both share the
512-value endpoint law and complete group/future schedule, reported separately.
Full hypothesis weights and their full past/future schedule are also counted.
No minimum-state or optimized-latency claim is made. Construction, complete
forecast checking and raw serialization are included in execution timing; native
accounting also includes setup and input loading.

Forty-three isolated controls passed with no failures, skips or warnings. They
include independent scalar joint Bayes for every report and future coordinate,
explicit one-hot squared loss, dense/sparse/constant/disjoint laws, source/group/
endpoint permutations, corrupted joint tables and copied histograms, support and
undefined-mask corruption, malformed inputs, full native integration and portable
dispatch. A source snapshot preserves the admitted implementation.

Complete-support synthetic timing uses all seven structures and both source
densities, with three serialized 32-row batches per shape. The original execution
estimate is 447 CPU seconds, including a 50% margin and 120 seconds for input
and setup. Three executions total 1,341 seconds; independent reconstruction has
870.5 seconds, original-row regroup 150 seconds and operations 200 seconds.
Prior checks and failed attempts are added before admission. The inclusive card
ceiling remains 3,600 CPU seconds within the existing family and global ceilings.

Completion requires original, adjacent and extracted-source replay bindings,
independent numerical reconstruction and a separate original-row regroup with
two draws inside each of eight equally weighted laws. The aggregate is specific
to one fixed source/report law. Arbitrary source-prior changes, source identity
disclosure and two dependent reports remain outside its scope. Highest-probability
retention and aggregate context disclosure remain prepared alternatives.

[Original protocol](AGGREGATE_REPORT_STATE_PROTOCOL.md).
[Context-disclosure alternative](AGGREGATE_CONTEXT_STATE_PROTOCOL.md).
