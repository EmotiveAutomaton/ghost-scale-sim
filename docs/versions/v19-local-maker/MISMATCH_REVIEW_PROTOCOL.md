# Independent actual-versus-stated reliability review

Can a mechanical reply still help prediction when its stated reliability is
wrong? Independently reconstruct the fixed original comparison with scalar
Bayes sums and legal mechanics. Rebuild conditional weights from retained native
population masses or uniform legal completions. Native masses inherit the
independently verified parent. Keep actual reply probabilities distinct from
assumed likelihoods; no actual accuracy enters the reader's forecast.

Reconstruct all 5,184 cells, 47,616 modeled reply-law rows, 96 forecast files,
both reply bits and all actual-accuracy probability arrays. Check the no-request,
half-assumed and truthful-parent identities. Explicitly preserve both zero-mass
fallback conventions and infinite-loss mass. Score realized replies separately
before averaging; finite loss contributions alone cannot rank infinite losses.
Verify both populations, all three fixed prices, request counts, exact query
roster, law and file coverage, and every permitted reader projection.

Freeze 10,000 paired eight-law resamples with seed 190981. Keep tool rule,
conditional law, actual accuracy, assumed accuracy, request, population and
price separate. Report means, differences from no request, from the calibrated
diagonal and from ignoring the reply. All 13,608 estimates require a second
regroup from original cells before numerical acceptance. Identities repeated
across accuracy settings are not additional independent evidence.

Known-answer controls precede dispatch: scalar odds, half-channel identity,
truthful conditioning, both fallback rules, explicit infinity, constant endpoint
collapse, a complete fixture and deliberate forecast, actual-probability, target,
reply-string, conditional-weight, modeled-mass, fallback, reader-field, cost,
denominator, duplicate-row and missing-file corruption. Native runtime and
standalone replay dispatch are checked. Source, all original outputs and inputs
are frozen for the existing serial queue; no producer source is changed.

The original half-hour card ceiling includes controls, execution, both replays,
review and failures. There is no fitting, new episode or protected lineage.
This is a supplied-law constructed-method comparison, not learned access,
historical process correspondence or human intent. Retrospective evidence
updating and finite uncertain-reliability sets remain independent prepared
alternatives; numerical acceptance of this comparison is not their prerequisite.
