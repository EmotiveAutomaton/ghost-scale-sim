# Realized-risk-constrained channel regret: prepared alternative

Does bounding every realized coverage loss remove a storage lottery's expected
regret advantage? Preserve the 28 libraries, 15 correctly supplied mixtures and
cue reliability interval [1/3, 1]. No new fitting or sampled observations.

For each deterministic policy, compute its maximum coverage loss over all
positive-prior latent sources and all labels possible anywhere in the interval,
relative to the best library mask for that latent source. The optimal fixed
mask supplies a feasible realized-loss threshold. Use thresholds at that value,
halfway from that value to one, and one. Admit only policies satisfying the
threshold before optimizing a lottery over their endpoint regret vectors.
The informed benchmark remains the original unrestricted library. Every mask
must meet the original byte budget. Retain excluded policies, all extreme
lotteries and ties, expected regret, maximum realized coverage loss and bytes.

Controls: exact singleton identity, known risk/expectation tradeoff, identical
prior null, nested feasible sets under relaxed thresholds, unchanged unrestricted
reference, corrupted charges, zero-weight support and independent convex hull.
Preserve all population joins, paired/equal-law summaries and both full replays.
Require full-support timing and frozen source before dispatch within a prospective
inclusive 1,800 CPU-second cap. Prepared, unimplemented and unadmitted. This asks
about finite supplied-law risk constraints, not forecast or process correctness.
Precision-choice stability remains an independent alternative.
