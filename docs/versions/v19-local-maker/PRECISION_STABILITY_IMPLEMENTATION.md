# Precision choice stability: executable contract

Use the same eight supplied laws, 81 precision assignments, three byte budgets,
four count vertices and lengths 16/64/128. Retain the previous producer ranges
and compute independent scalar ranges from the same supplied laws. Each uncertain
coordinate is the closed interval between the two floating values. These intervals
do not certify containment of the exact real-valued law ranges.

Convert finite binary endpoints to integers with an exact common power-of-two
denominator. For every feasible ordered assignment pair and every ordered vertex
pair, bound their linear contrast using interval endpoints, cancelling common
coordinates before choosing extremes. If L(v,w) and U(v,w) are those bounds,
the difference between robust objectives lies between min_w max_v L(v,w) and
max_v min_w U(v,w). This minimax relaxation is conservative; it does not assume
interval corners contain all extrema of the piecewise-linear objective difference.
Scale exact integer numerators for all three lengths. Retain every pair's bounds
and all sixteen vertex contrasts.

A candidate is not ruled out if its lower difference bound is nonpositive against
every recorded optimum. This is a conservative set relative to those references,
not an exact set of all possible optimizers. An assignment is guaranteed optimal
within the arithmetic box only if its upper difference is nonpositive against
every feasible competitor. For each recorded choice, retain the maximum positive
upper difference as a regret bound. Recorded computed-float ties remain unchanged;
the new exact linear calculation can also expose summation-order sensitivity.

Validate zero-width identity, exact ties, a strict margin, context permutation,
widening, all corners and interior points on reduced fixtures, common-coordinate
cancellation and corrupted charges. Full-support synthetic timing covers every
feasible ordered pair and all vertex contrasts. Original and two complete replays
must fit an inclusive 1,800 CPU-second cap. Preserve raw pair bounds, exact integers,
source/input/environment pins and independent reconstruction before acceptance.
No fitting, new observations, protected lineages or reader inputs.

Randomized channel regret retains its measured timing blocker. Realized-risk-
constrained channel regret and a supplied-law exact-comparison frontier remain
prepared alternatives; their admission is independent of this result.
