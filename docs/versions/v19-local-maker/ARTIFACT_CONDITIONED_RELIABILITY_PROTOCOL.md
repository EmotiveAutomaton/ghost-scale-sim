# Endpoint artifact reliability: prepared independent alternative

Can calibration pooled across visible endpoint artifacts conceal opposing errors
even when the reader has complete witnessed operations? This D-family diagnostic
uses the accepted goal-decision forecasts and is independent of any operation-
grouping result. It is prepared, unimplemented and unadmitted.

Group frames by the entire visible three-unit final artifact, using every legal
rendering tuple including empty groups. Derive groups only from public packets;
never use evaluator goals, losses or a favorable group subset. Retain sixteen
development laws, two training draws, five fit seeds, four budgets, four readouts,
both frozen forecasts, three goal classes, three positions, and both native and
equal-frame populations. No new training, samples or protected lineages.

Freeze ten integer-tenth bins. Retain group/bin joint weight, forecast sum and
native-correctness sum, conditional signed error and explicit empty means.
Marginalization must reproduce the ungrouped class sums. Compare absolute
discrepancy before and after artifact pooling; this measures cancellation under
a different public partition, not acquired information. Native self, opposite
group error, equal error, empty groups, permutation and total-mass fixtures are
mandatory. Seed 191017 fixes 10,000 paired law resamples with draws/seeds inside
laws; no frame-wise resampling. Include all per-class means and conditional-fit
intervals without a new architecture ranking or fitted practical threshold.

Before admission, freeze a complete executable enumeration and demonstrate that
all tests, failures, original execution, both full replays and independent
reconstruction fit an inclusive 1,800 CPU-second D card. If that fails, record the
specific timing or implementation limit. Retrospective updating remains an
independent alternative. Prediction and native targets remain scientific/evaluator
exports, separate from reader packets. This is a constructed calibration method,
not historical goal correspondence or human intent.
