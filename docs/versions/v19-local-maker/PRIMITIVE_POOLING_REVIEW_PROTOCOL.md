# Independent primitive input-pooling reconstruction

Reconstruct the complete fixed pooling comparison before accepting a numerical
advantage. Use all original frozen inputs, both saved training draws and support
modes, both tools, both input orders, truthful/wrong reports, budgets 0/16 and all
three maps. No fitting, additional sampling or confirmation lineage is used.

The checker imports no pooling or feedback producer. It reconstructs each
destination/outcome count by scalar summation across the supplied equivalence
class, subtracting each source row's unit prior and restoring one prior per
group. Distinct complete inputs each count once; duplicate reports do not.
The independently validated feedback reviewer supplies report generation,
score-stratum enumeration and numerical comparisons. Its separately validated
executor and explicit path sum reconstruct every endpoint and forecast.
Parent training and native path probabilities inherit their bound reviews.

Check all saved counts, normalized tables, forecasts, proper scores, population
masses, original support labels, anonymous reader projections, pool maps and
group denominators. Require complete output and stratum coverage. Keep the
unpooled zero-feedback identity separate from pooled no-feedback comparisons.
Controls include a complete native fixture and deliberate corruption of counts,
tables, forecasts, scores, maps, group counts, masses, reader fields and strata.

Average both saved fits within each of eight paired lineages. Use 10,000 paired
lineage resamples with seed 190972. Report both fit means separately. Contrasts
are pooled minus matching unpooled, feedback minus the same arm at zero feedback,
wrong minus truthful reports, and reverse minus forward order. Retain changed/
stay/all, all support subsets, and native/equal-query populations separately.
The practical margin is 0.02 nats. Equal-query predictions repeat across laws;
degenerate conditional intervals do not measure training or feedback uncertainty.

This is a constructed-method comparison with supplied input semantics, skill and
belief metadata. It does not establish discovery of the equivalence, historical
process correspondence or human intent. Both complete original replays remain
separate requirements. The checker consumes the existing two-hour pooling cap,
within unchanged family/global limits and the existing serial owner. Retrospective
evidence updating and the hidden-metadata boundary remain prepared alternatives.
