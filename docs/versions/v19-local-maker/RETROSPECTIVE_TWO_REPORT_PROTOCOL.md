# Two past reports and retained dependence: prepared alternative

Does state sufficient for one retrospective report also preserve the dependence
needed for two reports? This unimplemented B-family alternative uses the same
saved unknown-time/type posteriors, eight laws and both source conditions and
draws. It is independent of any prior-conditional rival advantage.

Compare full hypothesis conditioning with a representation retaining separate
future-group/past-state tables and a representation retaining joint past-state
pairs within each future group. Enumerate every ordered pair of available earlier
report times and the four-context/eight-endpoint report alphabets. Distinct reports
have explicitly independent sources conditional on the complete hypothesis. Same
source copies are identity controls. Retain zero-probability pairs separately.

No claim of general minimality follows from a two-report sufficiency result.
Report additional float64 mass and shared integer structure separately, as well
as sequential update discrepancy and the all-future forecast total-variation
bound. Preserve every identity and multiplicity in a lossless factorization.
Require direct small-roster enumeration, constant and stationary laws, conditional
independence and dependence controls, order agreement for two independent reports,
and deliberately deleted joint cells before dispatch.

Freeze full-support synthetic timing before outcomes. The prospective inclusive
cap is 3,600 CPU seconds within existing B-family/global limits, including tests,
failed attempts, both complete replays and independent reconstruction. If complete
support is intractable, record the blocker; do not substitute sampled time pairs,
fit models, generate new observations or consume protected lineages.
