# Independent joint-process readout verification

The original joint readout execution is complete. Its numerical finding remains
unaccepted until full replays and independent reconstruction pass. This checker
uses a separate source snapshot in the existing serial scientific queue. It adds
no training population, target fit, tiny-model setting or scientific contrast.

Before opening completed outcomes, execute independent known-process, constant-head,
unseen-label, finite-difference gradient, hidden-field rejection, corruption and
undo controls. The four dedicated tests also compare proper scores against an
explicit 5,832-entry vector and check all joint-label codes. Numerical controls
are part of the queued job, not claimed passed by structural admission.

Reconstruct every native path probability and transient execution state across
32 training and 16 development lineages. Independently rebuild public projections,
full goal-operation posteriors, native weights and the four auxiliary outcome
distributions. Verify both training selections and interleaving; reproduce each
prefix-only label alphabet without development labels. Check public feature
serialization and every fixed random basis, centered old-head ridge solution,
singular-vector projection, probability-repaired bank and target-head parameter
count. Reconstruct each saved softmax forecast, objective and gradient without
refitting the target heads. Optimizer trajectories remain the responsibility of
the adjacent and extracted-source full replays; solver success and achieved
gradient tolerance remain separate.

Independently reconstruct full-universe log loss and squared error, compatible
probability mass, the declared stable 90% candidate policy, truth coverage,
abstention, top incompatibility and each position's expected goal/operation
accuracy. Reaggregate by natural packet mass within each lineage. Average draws
and feature seeds inside each lineage before the fixed 10,000-resample bootstrap;
reproduce normalized log-budget areas and retain draw/seed contrasts separately.

The checker shares the target card's six-hour ceiling and original setup debit;
all controls, failed attempts and verification are charged. The global 96-hour
ceiling, 16-hour reserve and untouched test/confirmation populations do not change.
Failure preserves its receipt and blocks acceptance of the affected claim only.
The existing crossed-rule and support/holdout alternatives remain independently
specified behind their original regression barrier.
