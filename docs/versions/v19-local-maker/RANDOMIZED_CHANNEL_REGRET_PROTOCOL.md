# Randomized channel-regret protection: prepared alternative

Does a lottery over byte-feasible contingent policies improve worst expected
regret across an unknown cue channel? Use the same 28 mask libraries and 15
source mixtures. Reliability lies in [1/3, 1]. Compare deterministic minimax
regret, the optimal fixed mask and nominal two-thirds choices.

For every deterministic policy retain its two endpoint regrets. A lottery's
expected regret is the convex combination of these values. Enumerate single
policies and every two-policy mixture that equalizes endpoint regrets, retaining
feasible weights, all optima and a prespecified lexicographic tie rule. This
solves the finite two-endpoint minimax problem; independently check the convex
hull and all score-line intersections. Every realized policy and mask must fit
the budget. Report expected coverage and regret separately from the worst
realized coverage loss. Randomization never supplies new evidence.

Controls: known strict lottery improvement, no-benefit null, point interval,
permutation, duplicate policy invariance, dominated-policy invariance, feasible
weights and individual byte feasibility. Preserve the original population joins,
paired/equal-law summaries, source/input/environment pins and both full replays.

Prepared, unimplemented and unadmitted. Require complete-support timing and source
freeze within a prospective inclusive 1,800 CPU-second cap before dispatch. No
new fit, observation, protected lineage, acquisition fee or reader input.
Precision-choice stability remains the independent alternative.
