# Independent public-operation calibration review

Reconstruct every operation/bin weight, forecast and native-correctness sum
with scalar index membership and accurate summation. The checker imports no
producer scoring, binning, decision or aggregation function. Reuse the separate
independent scalar base-three marginal and binary64 bin checker. Reconstruct
native marginals independently; the unsaved native matrix projection is recreated
only to preserve exact bin assignments after scalar verification. No rounding or
renormalization replaces admitted probabilities.

Check all six public operations, three positions and classes, ten bins, both
populations, 320 forecast bundles, 92,160 metric rows, 704 reader packets and
5,120 parent loss/accuracy identities. Native self-forecast calibration is zero.
Empty means are NaN exactly at zero denominators; finite or infinite replacements
must fail. Preserve complete rebuilt scientific arrays separately from reader
inputs. Group mass, conditional signed errors, pooled/grouped absolute discrepancy
and their triangle-inequality gap are independently recomputed.

Regroup original and reconstructed rows separately: 576 means, 576 within-readout
cancellation estimates, 1,296 paired budget contrasts and 324 log-budget areas.
Seed 191016 fixes 10,000 paired law resamples. Two draws and five seeds stay inside
each of sixteen laws. Use scalar law means and bootstrap multiplicities, distinct
from the producer's direct-index path. A third original-row direct-index check is
required at final numerical acceptance. All intervals condition on the fits.

Known cancellation, no cancellation, native self, empty groups, exact/adjacent
edges, permutations, mass conservation, full synthetic execution, roster and
corruption fixtures precede source freeze. Synthetic full-size timing must fit
remaining inclusive 1,800 CPU-second card resources, including failures, tests,
replays, review and operating work. Dispatch through the existing single-worker
queue, CPU only, below normal, one numerical thread. Original snapshots remain
unchanged; no fits, samples, model setting or protected lineage are admitted.

Execution/replay acceptance and numerical acceptance remain separate. This is an
exploratory constructed-method diagnostic of aggregation, not historical goal
correspondence or human intent. Retrospective updating and endpoint-artifact
reliability remain two prepared independent alternatives.
