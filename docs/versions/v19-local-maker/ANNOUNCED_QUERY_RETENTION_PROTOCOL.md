# Query announced before source retention: prepared alternative

Can knowing the future question before compression change which past sources
should be retained? This is a different evidence condition from a reusable state
that must serve every future question. Freeze all four legal query contexts and
announce each before selecting a compact state. At the same half/quarter source
counts, select sources matching that context first, ordered by recent time, then
fill remaining slots with the most recent other contexts. Compare with recency
and evenly spaced retention. Never select from endpoints, likelihoods or gains.

Use the complete saved population, all laws, both evidence conditions and paired
draws, all copying probabilities and report outcomes. Keep every source's original
probability in the uniform-prior remainder and retain the complete copy histogram.
Score only the announced context for each conditional state, while reporting all
four equally. Contrast with a single reusable state. Four separately stored states
must pay four-state storage; they are not a free universal-memory improvement.

Prepared, unimplemented and unadmitted. Require direct scalar Bayes, independent
selection, tie/permutation/context-label covariance, no-matching-source fallback,
full/copy/constant identities, all input/support masks, complete-support timing
and source freeze. Prospective inclusive cap: 3,600 CPU seconds including failed
checks, three executions, independent reconstruction and original-row regroup.
No fitted model, protected lineage, new observation or occupancy sampling. No
practical margin or confirmation interval is frozen. Scope is a supplied-law,
query-announced constructed method, not learned provenance or historical intent.
