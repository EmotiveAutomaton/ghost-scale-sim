# Fixed nested bins: executable admission

Does probability-bin resolution conceal opposing errors without pooling public
request or artifact groups? The prepared fixed-bin protocol remains controlling.
Use the accepted goal-decision forecasts, all sixteen development laws, two draws,
five fit seeds, four budgets, four readouts and both frozen forecasts. Preserve
three positions, three goals, native and equal-frame populations. No fits,
samples, model settings or protected lineages are consumed.

Five, ten and twenty equal-width bins use exact integer-ratio binary64 edges;
probability one belongs to the last bin. Keep unmodified probabilities and every
bin membership. Pack sufficient sums into consecutive arrays of lengths 5/10/20,
retaining bin weight, weighted forecast, weighted native correctness and NaN
conditional means exactly at zero weight. Record absolute discrepancy at all
three resolutions, both refinement increments and unbinned squared probability
error for every goal. Summing the three goal errors gives the unchanged complete
probability-vector error. Ten-bin sums and unbinned errors reproduce the existing
class-reliability measure within 1e-10. No clipping or renormalization changes
the stored values. Native self-forecasts must calibrate exactly.

Nested membership and refinement monotonicity are independent assertions; a
decrease below -1e-12 fails the instrument. Keep roundoff-scale negative values
as recorded. Preserve parent fine loss and modal goal accuracy, public packet
hashes, native support and population rosters. Reader packets contain public
evidence only; all probabilities, targets and score arrays are scientific and
evaluator exports.

Seed 191019 fixes 10,000 paired sixteen-law resamples, retaining two draws and
five seeds inside each law. Report 576 means, 1,152 within-readout refinement
estimates, 2,592 paired budget contrasts and 648 normalized log-budget areas.
These condition on retained fits; no architecture winner or practical threshold
is specified. The separate checker must rebuild every array and parent identity,
and final acceptance requires a third direct-index regroup and both complete
replays. Frozen source, plan, inputs, environment and cost receipts bind execution.

Known opposing and identical errors, native self, exact and adjacent boundaries,
probability one, empty bins, permutations, mass conservation, coarsening sums,
faulty bin assignments, complete synthetic execution and deliberate input
corruption must pass before dispatch. Full-size synthetic timing must fit the
inclusive 1,800 CPU-second D card: tests, failures, original execution, both
replays, independent reconstruction and operating work. Existing serial queue,
below-normal CPU priority, one numerical thread, no environment synchronization.

Retrospective updating and squared-error decomposition remain independent
prepared alternatives. This is a constructed-method calibration diagnostic;
historical process correspondence and human intent remain unestablished.
