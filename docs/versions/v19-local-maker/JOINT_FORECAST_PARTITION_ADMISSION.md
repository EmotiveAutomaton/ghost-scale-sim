# Exact joint forecast partitions: executable admission contract

Does the full 27-outcome forecast over three local goals distinguish native
dependencies that its nine marginal probabilities merge? This implements the
prepared joint-partition alternative on the complete accepted goal-decision
roster: sixteen development laws, 704 public frames, two retained training draws,
five paired seeds, budgets 32/128/512/2048, four readouts and both restricted and
goal-product forecasts. No new fit, sample or protected lineage is admitted.

Keep each setting separate. Form exact binary64 equality groups from the saved
27-vector and independently from the saved three marginal simplexes. Signed zero
compares equal; a one-bit difference remains a difference. Check all memberships
and the full-to-marginal refinement relation. Record any floating-point refinement
exception without rounding it away. Retain all law/frame pairs, zero masses and
the 27-outcome native target. Reader inputs remain the same public packets.

The score is the expected sum of squared distances from the native joint target
to its partition group's pooled target, split into variation within each law and
variation of law-specific group means. It is an evaluator reference, never a
trained reader or a supplied reader input. Compute the within-law term directly
from centered native targets. Compute across-law variation by the exact identity
that sums pairwise squared law-mean distances, weighted by the product of group
masses and divided by pooled group mass. Zero pooled mass contributes zero;
its saved mean is undefined. Preserve full moments and pair coefficients.

Deduplicate computations and saved arrays only when the complete exact membership
vector and weighting population are identical. All settings retain a hash-bound
reference to their full group array; no membership, law, frame or resample is
removed. Targets and populations are constant throughout this card. Admission
timing assumes no reuse, so no favorable native group structure is needed.

Use native and equal-frame populations separately. Freeze 10,000 paired
sixteen-law resamples with seed 191023; recompute pooled masses for every
multiplicity. Retain draws/seeds paired, reporting their means separately.
For each readout, forecast, weighting and term, report all four budgets for
both partitions, joint-minus-marginal contrasts, and normalized trapezoidal areas
over log budget. This yields 384 estimates, 192 contrasts and 48 areas. No
practical-impact threshold is declared. Intervals condition on retained fits.

Before dispatch require complete synthetic handler execution, equal-marginal
different-dependence and identical-joint controls, one-bit distinctions, zero
and unequal weights, permutations, every small law multiplicity checked against
an independent scalar centered implementation, and deliberate corruption of
memberships, parent identities, populations and public inputs. Verify original,
adjacent and extracted-source complete replays. Numerical acceptance requires
independent scalar reconstruction of every group and resampling from a separate
27-coordinate cross-moment expansion of pooled target squares, with a separately
assembled paired summary; replay alone is not
acceptance. Freeze a full-roster synthetic timing estimate including three
executions, independent reconstruction, additional regroup and operating costs
before native outcomes. The inclusive cap stays 1,800 CPU seconds, within the
existing family and global limits. No sampled replacement or cap increase.

The first complete timing estimate exceeded the cap. Retain that rejection.
The bounded verification optimization expands pooled target squares into all
diagonal and off-diagonal law cross-products, independently of the producer's
centered law-pair distances. Every group denominator is rebuilt in every law
resample. Equal-mass populations admit an exact common-denominator reduction.
Small exhaustive scalar controls must verify both forms. This changes arithmetic
cost only; all 27 coordinates, 1,280 logical bundles and 10,000 resamples remain.
Already-compressed array files may use stored outer archives; no evidence bytes
are discarded. Third regrouping reuses retained scalar moments without writing
another duplicate copy of each group array.

Retrospective full-simplex updating and reachable-posterior retrospective updating
remain independent prepared alternatives. The latter tests actual retained
posterior states without depending on the former's witness result. A null or cost blocker here
does not close the week or establish exhaustion. This is a constructed-method
diagnostic, not historical process correspondence or human intent.
