# Retained filter cost and storage — 22 September 2026

What does allowing an unknown maker-change time and type cost? The retained mixture uses 35 times as many hypothesis weights as a static account at 32 observations and 227 times at 128. All 337,920 cost rows and both complete replays verify. These are constructed-method component costs; coarse whole-filter timings and a symbolic caching calculation do not establish measured query break-even, learned memory efficiency, historical process correspondence or human intent.

The comparison reuses every checkpoint and all five supplied-law filters from the
unknown-time/type, actual-change-timing and source-identity-omission studies.
There are no new fits, histories, predictions or confirmation lineages. The
337,920 rows average the sixteen makers within each original draw/lineage cell,
yielding 21,120 cells. All five earlier score measures accompany every cost cell;
the three parent studies retain their different scientific populations. Stationary
copies and replay executions are not new independent evidence.

The table reports the number of float64 hypothesis weights and bytes for one
filter state. Rows name the supplied candidate family; the two observation lengths
are columns. These are components only, excluding priors, indices, observations,
temporary arrays and all other resident memory.

| Candidate family | 32 observations: weights / bytes | 128 observations: weights / bytes |
|---|---:|---:|
| Static maker or last-sixteen-source reset | 16 / 128 | 16 / 128 |
| Supplied change time and type | 32 / 256 | 32 / 256 |
| Unknown time, supplied type | 288 / 2,304 | 1,824 / 14,592 |
| Unknown time and type | 560 / 4,480 | 3,632 / 29,056 |

Every method can project to sixteen current-maker weights (128 bytes) for the
declared fresh endpoint questions. That projection is not a proof of recursive
sufficiency for later changes or a record of historical process. The common
supplied endpoint law occupies 4,096 bytes; four eight-outcome forecasts occupy
256 bytes. One context query requires 128 stipulated multiplications and 120
additions. Prefix reconstruction counts each retained source against each
hypothesis; reset retains at most sixteen supplied sources. Omitted source IDs
make copies count separately. Source-time likelihoods and query-time projection
remain distinct. Serialized source bytes are measured separately from RAM.

The 184,320 timing calls cover all original, adjacent and extracted-source
executions of the three parents. Their summed filter-call CPU time is
1971.703125 seconds; this excludes fitting, scoring, checks, exports and review,
so it is not campaign consumption. 139,430 calls (75.65%) report zero
duration and the smallest positive duration is 0.015625 seconds. The 720 complete
timing strata retain counts, sums, means, medians, minima and maxima. These
coarse native timings do not support fine operation costs or universal speed ratios.

The 80 symbolic structural cells cross 1/4/16/64 queries with three fixed relative
query-cost assumptions. Caching the same current posterior once and rebuilding it
for every query tie for one query; the stipulated arithmetic saves work from the
second query. This is an algebraic identity. It omits parsing, normalization,
additions, allocation and hardware effects, and cannot establish an empirical
break-even. Different filters also return different answers; their work is not an
accuracy-matched comparison.

Independent reconstruction uses no producer counting or filtering arithmetic. It
checks all raw counts, maker-averaged scores, complete hypothesis rosters, source
equivalences, checkpoint maps, source serialization, all native timing rows and
every symbolic surface. Nine isolated controls pass before this reconstruction,
including a complete retained fixture and corruption rejection. The original and
both complete replays match 95 deterministic outputs; all 105 inputs, source
archives, imported sources, plans, environments and timing receipts bind.

**Warrant:** verified descriptive constructed-method accounting, miniature —
architecture untested. No learned-memory or empirical caching advantage is
established. **Pursuit:** fixed likelihood tempering can test a different remedy
for undisclosed copies. Primitive feedback and bounded mixture compression remain
independent prepared alternatives. No winning filter or exponent is selected.

All exports here are scientific/evaluator evidence. Parent reader packets remain
unchanged and separate; no source truth, maker labels or scores enter reader input.
[Protocol](FILTER_COST_PROTOCOL.md),
[evidence and complete reconstruction](../../../results/v19/G19-B-filter-cost-1/README.md).
