# Independent time-balanced retention reconstruction

Select source-time ranks from exact rational interval midpoints, independently
of the producer's vectorized integer ranks. Sort by timestamps alone. Preserve
timestamps under source permutation; endpoints and contexts cannot affect
selection. Check all retained counts, irregular times, empty and full selections.

Reconstruct source-wise Bayes factors and bit schedules independently. Selected
sources contribute joint past-state/future-group numerators; omitted sources
contribute the original uniform sixteen-maker prior likelihood times group mass.
All original source probabilities and copy counts remain. Compute group/report
numerators directly and propagate to every future time, context and endpoint.
No producer evaluator, summarizer, transition matrix or direction kernel is used.

Explicit expected one-hot squared losses check regret against full retention.
Verify all report probabilities, exact support and undefined masks, future maximum
errors, group total variation, selected identities, remainder mass and histograms.
Recount structural cells with sets, separating mixed cells from deterministic
cells that reuse group weights. Float64, int32 and shared-prior costs stay separate.
Reference timing does not establish optimized compact-state latency.

Bind plans, source, environment, inputs, every deterministic output and separate
timings. Keep all 28,672 posteriors, 505,856 sources and 143,360 summary rows across
eight laws, both evidence conditions, two draws, seven structural cells and five
copy probabilities. Five retention states share eight endpoint queries. The 48
unavailable checkpoint strata remain absent. Reconstruct all fifty metrics and
1,120 paired draw strata, each containing 128 Cartesian identities. Separate event
review regroups ORIGINAL rows into 560 law strata and seventy population cells,
with two draws inside each equally weighted law. Both spaced-minus-recent
contrasts are paired differences, not independent replications.

Controls cover dense/sparse/constant/disjoint laws, source permutation, an older
informative source, full/certain-copy identities, every raw-field and undefined-mask
corruption, input/summary/roster/timing faults, native integration and portable
dispatch. Full-support synthetic timing covers all seven structural cells and
both source densities, including archive decompression and complete 32-row batches.
The original 3,600 CPU-second inclusive cap covers previous attempts, three
executions, controls, checker and separate regroup. No new fit, observation or
protected lineage. Numerical acceptance awaits completion and regroup; no practical
margin or confirmatory precision was frozen. Scope is a supplied-law constructed
method, with no learned provenance or historical-process claim.
