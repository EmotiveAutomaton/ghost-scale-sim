# Frozen support-table transfer to a changed tool

Does a learned composition advantage survive a changed primitive operation?
This implements the alternative specified in ROUTINE_REVISION_PROTOCOL before
unknown-change outcomes. Isolated controls and source admission are required.

Reuse both completed support-study training draws, each with original and
composition-holdout counts. Freeze every count table, support label, forecast,
nested uniform and all 640 supplied queries. Reconstruct all seven saved methods'
predictions and every original-law aggregate before interpreting transfer. The
original-law result is an identity control, never a new replicate. No fit occurs.

Enumerate original and presentation-only tool execution on lineages 190008–190015.
These are the same eight support-study evaluation lineages, not untouched
confirmation. Keep each rule's native probability weights. Compare seven fixed
methods: direct counts, nearest-query retrieval, exact learned propagation,
one/four/sixteen sampled paths, and the original-law oracle. Add a separate
true-law oracle reference. Every distribution, including both oracles, uses the
same final 1/32 uniform mixture. Wrong deterministic predictions receive probability
1/256. This is the prepared common-smoothing comparison; the older unsmoothed
transfer handler is a different design.

Cross all/changed/stay endpoint queries with all five original support strata:
all, seen, unseen, withheld action composition, and withheld composition with all
original-law primitives visited. Support is measured against training and the
original-law path; it does not imply that the changed transition was observed.
Preserve empty cells with zero mass and undefined conditional loss. Retain native
mass, query count, logarithmic loss, squared probability error and true-endpoint
probability. Normalize weights only within the named stratum. Cross-rule differences
include changed native population weights; matched query differences remain available.

Use paired coefficient lineages as uncertainty units; average two saved draws
within lineage and retain both fit modes. Report fixed method contrasts and
transfer differences with 10,000 paired lineage resamples, seed 190501, and the
existing 0.02-nat practical margin. No method or support subset is selected after
outcomes. Exact replay and independent native path, forecast, score and regroup
reconstruction are distinct requirements before scientific acceptance.

The reader archive contains supplied execution queries: skill, belief, initial
artifact and requested operation sequence are explicitly privileged task inputs.
It contains no actual changed-rule label, support category, teacher endpoint,
lineage, population weight or result. This is forward prediction under supplied
operations, not inverse reconstruction of historical local goals or human intent.
Native paths, models, uniforms, forecasts, assignments and scores remain separate
scientific/evaluator evidence with exact hashes.

The card has a four-hour cap including failed attempts, tests, original, both
replays and independent review, within existing family/global ceilings. It uses
no tiny setting, new episode seed or confirmation lineage.

Two independent alternatives remain prepared: the five fixed-budget count
reweightings in [SUPPORT_MIX_PROTOCOL](SUPPORT_MIX_PROTOCOL.md), and the
following off-midpoint change test. Neither requires transfer to win.

The off-midpoint B-family alternative freezes the five existing filtering methods,
priors, endpoint laws, two observation draws and eight original admission lineages.
Replace the actual midpoint switch with a switch at one quarter or three quarters
of length 32/128, for purpose and skill separately. Keep independent and duplicated
sources and the complete time/type hypothesis roster. The known-time/type reference
receives the actual possible time; other arms stay unchanged. Reuse original
uniforms and unchanged stationary controls without counting them again. Score the
same checkpoints, prediction and current-state coverage; report both locations
separately and paired with saved midpoint results. Freeze source-time semantics
and validate future-state remapping. A four-hour card, handler and source admission
are still required. This challenges the actual-time distribution without more seeds.
