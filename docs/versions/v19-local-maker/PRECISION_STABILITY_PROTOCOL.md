# Precision choice stability: prepared finite alternative

Do nearly tied storage choices imply unstable useful bounds, or only unstable
optimizer identities? The preceding scalar reviews found changed tie sets at
roundoff scale. Quantify this distinction without fitting or sampling new laws.

Freeze the same eight laws, 81 precision assignments, three byte budgets,
four count vertices and lengths 16/64/128. For each context/precision log-range,
use the interval between the producer and independently reconstructed scalar
value. These intervals describe two arithmetic evaluations, not certified
containment of an exact real-valued answer. Keep that limitation explicit.

For each recorded robust optimum and each feasible competitor, bound the
objective difference by enumerating all four maximizer-vertex choices and
using interval endpoints for their linear contrasts. Report a conservative
upper bound on regret and the assignments that cannot be ruled out. Do not
replace an unresolved set with a unique winner or call a conservative set exact.
Compare this against exhaustive interval-corner enumeration on reduced fixtures.

Required controls: zero-width identity, exact ties, a known strict margin,
context permutation, widening intervals cannot shrink the conservative set,
independent scalar interval extrema, and corrupted byte charges. Full-support
timing must include every feasible assignment pair and all vertex contrasts.
If that complete comparison exceeds its prospective inclusive 1,800 CPU-second
cap, retain the timing blocker without reducing the declared comparison after
seeing outcomes. Original and two complete replays, all raw pair bounds,
source/input/environment hashes, independent regroup and write-through required.

Prepared, unimplemented and unadmitted. Teacher is the supplied evaluator law.
No training, protected lineages or reader inputs. This is an arithmetic decision
sensitivity question, not attained inference accuracy, learned access, historical
process correspondence or human intent. Robust source-prior retained mass is
the independent alternative.
