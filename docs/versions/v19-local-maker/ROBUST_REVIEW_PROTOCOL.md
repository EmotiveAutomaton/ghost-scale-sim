# Independent check of robust reply forecasts

Rebuild all supplied group laws from the accepted parent's native masses and
independent endpoint mechanics. Scalar Bayes sums reconstruct all compatible
channel distributions, their equal mixture, coordinate maximum, normalizer,
zero-support masks and fallback records. Check the coding inequality for every
compatible candidate and endpoint, not just a favorable example. Reconstruct
every realized reply score, infinite-loss indicator, request cost and reader
packet, and all 3,456 summary cells. Actual reliability is evaluator-only.

Recompute 5,760 paired development-law means and differences from no request
with 10,000 resamples, seed 190983. Numerical acceptance requires a separate
regroup of the original cells and both complete source-bound replays. Keep
finite loss contribution and infinite-loss mass separate when ranking arms;
worst-channel loss is descriptive, not a minimax guarantee. Fixtures include
known coding bounds, duplicate-candidate sensitivity and deliberate corruption.
The existing 1,800-second robust-decision cap includes this verification.
