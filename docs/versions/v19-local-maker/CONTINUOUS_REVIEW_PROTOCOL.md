# Independent review of the continuous reliability certificate

Reconstruct every retained certificate using scalar hypothesis likelihoods and
the separately implemented symbolic tool executor. Validate all 32 law/tool/model
cells, every public group, both fields and replies, and the 17 fixed rational
grid points. Rebuild native and uniform conditional weights from the verified
parent population. Sum saved binary64 weights as exact fractions to check the
nonnegative hit/miss partitions. Check masses, endpoint compatibility, left
limits, coordinate extrema, total-variation diameter and interpolation weights.
An impossible truthful endpoint never becomes a forecast; its zero grid vector
is a labelled storage placeholder. Exact stored equality remains separate from
the 1e-12 reconstruction tolerance.

Compare every compatible half-, three-quarter- and full-reliability forecast
with the already accepted finite-set experiment, and compare all its extrema
and diameters. Bind that experiment's source plan and accepted output files.
The finite grid and exact rational fixtures check implementation; the algebraic
argument in the admission contract proves the interval claim. Neither numerical
agreement nor the grid alone proves a continuum theorem.

Verify the complete file roster, summaries, evaluator index, controls and reader
allowlists. Retain reconstructed cells and maximum numerical discrepancies.
No fitting, resampling, protected lineage or new scientific population is used.
All work, tests, failures and review remain inside the original 1,800 CPU-second
card ceiling. Original execution and both complete replays are prerequisites;
numerical acceptance and a final scientific disposition remain separate until
this checker completes and its own output bindings are reviewed.

The exhaustive retrospective-evidence and public-evidence joint-support decoder
designs remain independent prepared alternatives. The latter addresses the main
joint-process comparison; this supplied-channel certificate does not replace it.
