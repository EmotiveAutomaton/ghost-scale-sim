# Independent squared residual decomposition review

Reconstruct every ten-bin residual moment from the frozen goal-decision inputs
using scalar membership comparisons and accurate scalar sums. Compute within-bin
variance from centered residuals, independently of the producer's subtraction of
second moment and squared mean. Check the independent pairwise variance identity
on known-answer fixtures. Empty conditional moments remain undefined exactly
where population mass is zero; no clipping or forecast correction is permitted.

Verify all 320 forecast and moment bundles, native projections and self scores,
92,160 metric rows, 5,120 parent identities and every original/rebuilt paired-law
estimate. Binary64 forecast values define the frozen bin membership; scalar
marginal reconstruction checks those values before assigning bins. Retain the
sixteen development laws, both training draws and five seeds in paired resampling
with seed 191020 and 10,000 resamples. Preserve draw and seed variation separately.
Check all component estimates, baseline contrasts and normalized log-budget areas.
A third direct-index regroup remains required before numerical acceptance.

Controls cover signed and opposing errors, unequal weights, point mass and native
self, exact and adjacent boundaries, permutations, mass conservation, missing or
duplicate strata and corruption of moments, forecasts, schema, evidence and parent
scores. Require complete original, adjacent and extracted-source bindings. Retain
raw reconstruction separately from reader inputs and keep original sources frozen.

The inclusive 1,800 CPU-second D card covers producer, failures, tests, original,
replays, this review and exports. Measure synthetic tractability before admitting
the checker to the existing serial queue. No new fit, sample or protected lineage
is admitted. This verifies a constructed-method diagnostic, not historical goal
correspondence or human intent. No practical-effect threshold was specified.
