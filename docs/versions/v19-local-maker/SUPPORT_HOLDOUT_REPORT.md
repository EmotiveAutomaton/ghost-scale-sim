# Support and composition holdout: numerical adjudication pending

Does learned transition composition help on queries absent from training when every prediction has the same final smoothing? The support and composition-holdout experiment and both complete replays match all 41 deterministic outputs. Its 640 public queries declare mechanics and proposed operations; hidden endpoints and support labels remain separate. Independent numerical reconstruction is prepared before accepting an accuracy conclusion. This is a constructed-method reproducibility result, not historical process correspondence or human intent.

The frozen experiment reuses two original 2,048-path training selections. Its
paired holdout removes accept-tool followed by repair-evidence at the first two
steps, then rebuilds the same count estimators on fewer paths. Eight development
lineages supply complete native query populations. Direct lookup, nearest-query
retrieval, exact learned propagation, 1/4/16 sampled rollouts and exact mechanics
share a final uniform mixture of 1/32. Native transition and lookup pseudocounts
remain explicit. This does not capacity-match the factorizations.

The five frozen strata are all queries, seen and unseen full queries, the withheld
composition, and withheld compositions whose true primitive transitions were
observed in training. The final diagnostic uses evaluator truth, never a reader
input or mask. The independent checker preserves empty strata and checks native
probability weights, denominators, training counts, every forecast and every score.
No numerical accuracy estimate is accepted from replay identity alone.

All 406 sources, frozen inputs, plans, environments, 41 deterministic outputs and
separate timing records verify across original, adjacent and extracted-source
executions. The 640 query packets contain only the declared skill/belief mechanics
bits, initial artifact and proposed operations. They are not artifact-only reading.
Full raw truth, selected training paths, fitted arrays and diagnostic support
labels remain in the separate scientific/evaluator export.

Ten isolated checker/runtime/dispatch controls pass, including a complete native
fixture, explicit path propagation, undo-buffer support, tied retrieval, common
smoothing and deliberate forecast corruption. A failed serialization attempt is
retained and charged; the fix converts a control boolean for JSON without changing
scientific scoring. The checker is implemented for the existing serial queue;
numerical acceptance belongs to its own completion event.

**Warrant:** execution and reproducibility verified, numerical conclusion pending.
**Pursuit:** full independent reconstruction, then classify the paired support
strata. No additional tiny-model setting or reserved lineage is consumed.

[Frozen comparison](QUEUE_COMPLETENESS_PROTOCOL.md), [independent check](SUPPORT_REVIEW_PROTOCOL.md).
