# Continuous reliability: prepared independent alternative

Implementation update, 23 September: the handler and its isolated controls now
exist. The [executable admission contract](CONTINUOUS_RELIABILITY_ADMISSION_PROTOCOL.md)
freezes the proof, grid and output roles. The original prepared scope below is
retained; execution and independent scientific acceptance remain separate.

Does the finite reply set miss any endpoint forecast when reliability can take
every value between one half and one? This is an unimplemented D-family
certificate study, independent of the robust-decision ranking and retrospective
evidence updating. No continuum result is currently claimed.

Keep the verified 640-query, eight-law, two-tool, native/uniform population.
For each visible group, requested bit and reply, write the unnormalized endpoint
weights and total reply mass as affine functions of reliability. Derive the
fractional-linear interpolation between the endpoint conditionals. With positive
denominators, determine whether every interior forecast is a convex combination
of the endpoint forecasts. Treat zero reply mass at an endpoint separately:
an arbitrary fallback at an impossible observation is not a limiting forecast.

Enumerate all groups and fields. Preserve coefficients, compatible endpoints,
singular cases, limiting distributions and any counterexamples. Verify small
rational fixtures exactly, known constant/disjoint supports, deliberate incorrect
endpoint inclusion and a separately implemented scalar evaluation on a fixed
rational grid. The grid is a numerical control, not a proof over the continuum.
No conclusion about unknown mechanical laws or learned access follows.

Freeze the symbolic claim and complete finite roster before execution. Cap the
study at 1,800 CPU seconds including controls, both full replays and independent
review within the existing D budget. It requires no new fit, random episode,
protected lineage or budget extension. Retrospective updating remains a separate
ready alternative if this compilation fails its bounded tractability check.
