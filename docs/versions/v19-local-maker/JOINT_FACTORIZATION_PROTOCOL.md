# Learned joint dependence: prepared independent alternative

Do frozen learned joint forecasts encode useful goal/operation dependence, or
would the product of their own marginals perform as well? This A-family diagnostic
uses the original joint-readout forecasts, all four evidence tiers and training
budgets, two training draws, five feature seeds and sixteen development laws.
It is prepared and unimplemented, independent of uncertainty decomposition.
Unlike the completed exact D2 jointness fixtures, it examines learned predictions
at matched outcome supervision. Initial inclusive cap: 1,800 CPU seconds within A.

Expand the full unknown tail over the fixed 5,832-label universe. Obtain the 27-goal
sequence and 216-operation sequence marginals, then form their normalized outer
product without fitting, support restriction or hidden-policy input. Compare each
original learned arm and the same-budget frequency baseline with its own product.
Retain supplied exact posterior products as a separately labeled evaluator ruler.
Report native weighted proper losses, excess loss, native compatibility, nominal
candidate coverage, unsupported modal assertions and localized goal/operation
accuracy. Do not confuse a probability product with an average of scored forecasts.

Controls precede outcomes: independent joint identity, correlated binary fixture,
identical marginals/different joint distributions, zeros and full unknown mass,
normalization, axis reversal, and deliberately incompatible goal/operation pairs.
Reproduce original scores and scalar outer products independently. Average both
draws and all five seeds within each lineage before 10,000 paired resamples,
seed 191005; preserve per-budget results and normalized log-budget areas.

Freeze inputs, code and complete roster before execution. Preserve all original
forecasts, reader/evaluator separation, adjacent and complete extracted-source
replays, and independent reconstruction. No new tiny setting, protected lineage,
training data, family extension or confirmation is admitted. This asks about a
constructed learned distribution, not causal access or historical human intent.
