# Frozen goal decisions under complete witnesses

Does the modal whole path sacrifice stepwise localization for the same forecast?
Implement the prepared GOAL_DECISION_PROTOCOL without fitting or selecting new
outcomes. All four readouts, two forecasts (restricted and goal product), four
budgets, sixteen development lineages, two draws and five seeds remain fixed.
Use all 704 accepted anonymous complete-witness packets and their original targets.

Recover the exact accepted conditional forecasts from inherited full forecasts;
require byte equality for saved normalizers, marginals and witnessed operations.
Reproduce all 5,120 parent logarithmic-loss strata within 1e-10. Prediction loss
must be identical across both decisions. Parent alphabet-first choice priority
is deliberately replaced for this separately named comparison by smallest full
numeric label for exact ties, consistent across both decision objectives. No
tolerance rounds competing labels into a tie. Retain the complete 27 probabilities,
joint and coordinate choices, marginal probabilities, tie counts and margins.

Measure native expected step accuracy, whole-path accuracy, zero-native-support
choice frequency, forecast Hamming loss (fraction of incorrect step labels),
forecast whole-path loss and unchanged logarithmic loss. Record decision
disagreement and ties separately. Native posterior choices supply distinct rulers:
coordinate modes minimize forecast Hamming loss; joint modes minimize whole-path
loss. These optimality checks are separate from learned accuracy on native truth.
Public operation compatibility never excludes any of the 27 candidate goal paths.

All 10,240 learned score strata and 32 native rows are retained. For the six named
metrics, compare coordinate minus joint across 192 budget contrasts and 48
normalized log-budget areas. Bootstrap seed 191011, 10,000 paired sixteen-lineage
resamples; both draws and five seeds stay inside each lineage. Retain separate
draw and seed variation and all 64 metric means. No confirmation lineage is used.

Known-answer controls include opposing decision optima, a coordinate tuple outside
positive joint support, equal marginals with different joint modes, point/uniform
laws, scalar probability and accuracy calculations, exact ties, invalid inputs,
whole synthetic execution, unchanged prediction and deliberate reference, parent,
roster, reader and witness corruptions. Verify native and portable dispatch in an
isolated source checkout. Original, adjacent and extracted-source executions use
one immutable source and complete inputs. Independent reconstruction is required
before scientific acceptance; reproducibility alone does not establish correctness.

The inclusive D-family card ceiling is 1,800 CPU seconds for preparation, controls,
execution, both replays, independent review, failures and operating overhead.
Synthetic timing must protect 120 operating seconds and fit family/global caps.
No GPU, synchronization, new fit or owner is admitted. The existing native serial
queue owns dispatch. Reader exports contain only anonymous public packets;
forecasts, choices, scores and native targets are separate scientific/evaluator
exports. This constructed-method decision comparison cannot establish historical
goals, human intent, selective causal access or a primary-bank advantage.

Retrospective quotient updating and goal-report abstention remain separately
prepared alternatives. Neither requires a favorable outcome from this comparison.
