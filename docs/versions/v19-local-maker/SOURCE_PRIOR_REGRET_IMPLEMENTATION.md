# Source-prior regret: frozen implementation contract

Implement the prepared minimax-regret alternative on the same verified 28,672
source rosters, seven checkpoints, eight development laws, two draws and paired
evidence roles. Retain the twelve prior/policy identities and deduplicated masks
for every one of 28 distinct roster/cost/budget problems. No new observations,
fits, outcomes, tiny settings or protected lineages.

Architecture and capacity: exact rational evaluation of the fixed finite library
under uniform, time-proportional and reciprocal-time source priors, two existing
budgets, 57,344 original output rows. Teacher labels are these supplied prior
definitions and structural byte costs. Schedule is one complete retained pass,
then adjacent and extracted-source complete replay; no training schedule.

At each vertex, regret is the prior-informed unrestricted feasible optimum minus
the candidate's retained mass. That prior optimum is already represented in the
library and bound to the independently verified source-mass frontier. Maximum
regret over every convex mixture occurs at a vertex. Minimize that maximum;
retain every exact tie and select the largest recent-time bit vector. Record all
prior regrets, the minimax value, the maximin choice's regret and the chosen
minimum-mass cost. Optimality is within the candidate library only.

Before outcomes: exhaustive small subset libraries and rational eighth-grid
three-prior mixtures, a maximin/minimax-regret disagreement, identical priors,
empty/full budgets, prior and identity permutations, exact ties and corrupted
optima/regrets/masses. Complete-support synthetic timing must include all rows,
candidate comparisons, serialization and hashes. Source is frozen after these
controls and before native admission. Separate inclusive cap: 1,800 CPU seconds,
including failures, controls, all executions, independent reconstruction, paired
and equal-law original-row regroup and documentary write-through.

Independent review reconstructs structural costs and source rosters from retained
provenance, prior optima independently, every mask and rational regret, all ties
and minimum-mass tradeoffs, every original row and both full replays. Scientific
exports are evaluator evidence; no new reader inputs. Forecast accuracy, learned
provenance, historical process correspondence and human intent remain outside
this constructed storage decision.

Precision-choice stability and randomized source storage remain separately
prepared alternatives, requiring their own implementation and admission.
