# Independent joint partition review

Does the complete joint forecast preserve distinctions lost in its marginal
forecasts? This checker validates the frozen comparison without new fits or
lineages. It independently sorts exact binary64 equality classes, reconstructs
all 27 native target coordinates and scalar law/group masses, weighted sums,
second moments and centered within-law variation. Expanded squares check the
producer's pair-distance coefficients. Undefined zero-mass means remain explicit.

Every one of the 1,280 setting/population/partition bindings must match that
setting's original forecasts. Cache reuse requires identical complete membership
vectors and population; no logical setting is removed. Check all refinement
relations and retain binary64 exceptions without rounding. Reconstruct all 5,120
parent losses and preserve the 704 public packets separately from evaluator data.

Use the frozen seed 191023 and all 10,000 paired sixteen-law multiplicities.
The independent cross-moment expansion rebuilds pooled denominators in every
resample, for original and reconstructed moments separately. Two training draws
and five seeds stay paired. A separately assembled summary reproduces all 384
estimates, 192 joint-minus-marginal contrasts and 48 normalized log-budget areas.
The later event review must also perform a third regroup from retained arrays.

Before native dispatch require known-answer dependence and identity controls,
all small law multiplicities against direct centered scalar sums, permutations,
zero/unequal weights, exact one-bit distinctions, full synthetic execution and
deliberate corruptions of memberships, bindings, moments, summaries and parents.
Freeze checker source and the complete input inventory. Preserve original and
both replay snapshots. All tests, failures, checking and later regrouping charge
the same inclusive 1,800 CPU-second card; no cap extension is admitted.

Complete replay is evidence of reproduction, not independent numerical
acceptance. All native targets, group means and moments remain evaluator-only.
This is a constructed-method diagnostic; no historical process correspondence,
human intent or confirmation claim follows. Retrospective full-simplex and
reachable-posterior updating remain independent prepared alternatives.
