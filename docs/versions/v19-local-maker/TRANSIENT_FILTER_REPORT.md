# Purpose-switch filtering: complete replay, numerical review pending

Can an account of a possible purpose change preserve current-state information? The three-filter comparison and both complete replays match all 35 deterministic outputs, covering 2,048 observation streams and 33,792 scored rows. Independent reconstruction is now source-frozen before accepting prediction or state-recovery differences. This is a constructed-method reproducibility result; the true law and possible switch time are supplied, and historical process correspondence remains unestablished.

The question is whether a coherent account of one possible purpose change retains
the information needed about the current maker. Static exact filtering, a fixed
last-sixteen-distinct-source reset, and a joint no-switch/switch mixture see the
same observed endpoints and context. Every method receives the true endpoint law;
the mixture also knows the one possible change time. The actual switch assignment
and maker remain hidden. This is an exact reference for future recursive readers,
not learned switch detection or a relabeling of old global goals as local goals.

All 110,592 original three-operation paths are retained across eight admission
lineages, including complete local-goal and transient execution state. Their laws
generate repeated independent episodes for every one of sixteen initial makers,
two fixed observation draws, lengths 32/128, switch/no-switch, and independent/
duplicated-source conditions. Governing purpose changes only between episodes.
Skill, belief and routine are unchanged. No fit or reserved lineage is used.

There are 2,048 streams containing 163,840 presented observations, 33,792 scored
rows and 2,112 summary strata. A duplicated stream contains 24/96 distinct sources
instead of 32/128; every fourth slot copies the preceding source, including its
original time and context. Context rotation balances which contexts are omitted.
Differences between source conditions include reduced evidence quantity and cannot
isolate a pure duplication effect. All filters count each source once.

Endpoint logarithmic loss measures error predicting a fresh episode under the
current maker, averaged over four contexts. Current-state logarithmic loss, true
state probability, 90% credible-set inclusion and credible-set size remain distinct
outcomes. Boundary ties within 1e-14 are retained. Log scores use the predeclared
1e-300 reporting floor; raw distributions remain saved. No numerical advantage,
reversal or practically negligible difference is accepted in this replay review.

All 35 deterministic files match original, adjacent and extracted-source runs.
Every one of 424 frozen source files, eight raw input blocks, source archive,
plan and environment binding verifies. Each execution's timing has its own checked
hash and need not repeat. The original controls pass. Replay identity excludes
neither a shared implementation error nor a mismatch between the model and people.

An independent checker is implemented and frozen with 427 source files and 39
completed inputs. Nine isolated tests pass in 14.02 seconds, including a full
13,824-path native fixture, all three filters, both observation draws, current-state
remapping, paired population means, source/distribution corruption and evaluator
identity rejection from reader packets. Native and portable dispatch tests pass.
The two failed test attempts remain charged and retained: one NumPy Boolean needed
JSON conversion; deliberate corruption of a scratch fixture required an explicit
mutable write. Neither changed the original science or its scoring rules.

The checker uses the separate native execution ruler and stable endpoint summation.
Scaled sequential hypothesis products independently check the producer's accumulated
log likelihoods. It reconstructs every stream and source identity, posterior, joint
hypothesis, current-state remapping, forecast, score, tie and denominator. Each
length/switch/source/checkpoint cell stays separate, with all sixteen makers equally
weighted and the two draws averaged within coefficient lineage. All three pairwise
filter contrasts retain five metrics, eight lineage differences and 10,000 paired
bootstrap resamples. Campaign numerical acceptance awaits its completed execution.

READER.zip contains exactly 2,047 distinct anonymous observed-input packets; two
streams share the same visible content. Only contexts, observed endpoints and source
identity/time enter these packets. Maker identity, switch assignment, coefficient
lineage, draw pairing, law, posteriors and scores belong to SCIENTIFIC_EVALUATOR.zip.
The evaluator archive contains the full native inputs, observations, predictions,
source, plan, controls, summaries and timing. Its contents are never reader inputs.

**Warrant:** complete constructed-method reproducibility, numerical adjudication
pending. No learned competence, historical local-process identification, human
intent, architecture-general conclusion or confirmation follows. **Pursuit:** finish
the independent checker, then continue either changed-tool learned support transfer
or the unknown-time purpose/skill-change discriminator. Both are precisely prepared
with separate four-hour caps and require their own implementation and admission.
Neither depends on a filtering advantage. The week and protected reserve stay open.

[Frozen comparison](TRANSIENT_FILTER_PROTOCOL.md),
[independent checker](TRANSIENT_REVIEW_PROTOCOL.md),
[evidence roles](../../../results/v19/G19-B-transient-filter-1/README.md).
