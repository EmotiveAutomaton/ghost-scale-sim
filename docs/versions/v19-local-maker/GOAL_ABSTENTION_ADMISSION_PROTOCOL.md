# Fixed-cost goal-report admission

The prepared abstention comparison now has a candidate handler. It is not yet
scientifically admitted or executed. Freeze accepted goal-decision forecasts,
the complete 704 witnessed packets and sixteen development lineages, four arms,
four budgets, two training draws and five seeds. There is no fit or new teacher.

For costs 0.1, 0.25 and 0.5 per abstained step, compare always reporting coordinate
modes, reporting the joint mode only when its mean forecast step error is strictly
below cost, and independently reporting coordinate modes with step error strictly
below cost. Equality abstains, with no tolerance. Smallest numeric goal breaks
exact modal ties. The existing binary64 marginal computation fixes arithmetic;
each saved parent decision array must match it exactly before reporting.

Report coverage, correct and incorrect claims per step opportunity, native and
forecast expected costs, incompatible partial tuples and incompatible full tuples,
and unchanged logarithmic loss. Partial compatibility means at least one native
path extends all reported coordinates. The empty report is compatible. Conditional
error is incorrect claims divided by coverage; zero coverage yields null, including
in pooled summaries. Native policies are supplied-law references. Truth never
selects a learned report.

Keep every cost and policy separate. Each abstaining policy is contrasted with
always reporting at the same cost, forecast and budget for eight metrics. Retain
1,536 budget contrasts, 384 normalized log-budget areas and 288 metric means.
Seed 191012 fixes 10,000 paired sixteen-lineage bootstrap resamples; draws and
seeds stay within lineages, with their separate variation retained. Conditional
error in means is the ratio of pooled incorrect claims to pooled coverage, not
the average of conditionally defined ratios. No accuracy or cost practical margin
is invented from the unrelated 0.02-nat primary prediction margin.

Validate scalar exhaustive decisions across all 64 partial reports; all/none/
partial outputs; threshold equality and near equality; exact modal ties; the
joint-path versus step-risk distinction; partial support; complete fixture
execution; parent prediction and accuracy identity; and deliberate input, reader,
reference and roster corruption. Time a full synthetic workload before admission.
The inclusive D-family cap is 1,800 CPU seconds for controls, all failures, original,
two complete replays, independent reconstruction and operating work. Freeze source,
plan, environment and every input before dispatch through the existing queue.

Reader exports contain unchanged public witnesses only. Forecasts, all reports,
targets and raw costs remain separate scientific/evaluator records. Independent
reconstruction and complete replays are required for numerical acceptance. This
is an exploratory constructed-method tradeoff under stipulated costs, not a claim
about user values, historical goal correspondence or human intent.

Retrospective evidence updating remains independently prepared. A second prepared
alternative tests whether less specific goal reporting preserves useful report
coverage; it is specified separately and requires its own admission.
