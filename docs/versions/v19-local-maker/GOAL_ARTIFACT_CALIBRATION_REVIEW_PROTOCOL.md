# Independent endpoint-artifact calibration review

Reconstruct all eight visible endpoint groups directly from the three public
binary artifact units. Use independent binary encoding, never saved group IDs,
operations, requested or adopted purposes, targets or scores to choose a group.
Witnessed operations still constrain native goal support and parent identities.

Reconstruct every group/bin weight, forecast and native-correctness sum using
scalar memberships and accurate summation, independently of producer indexed
accumulation. Preserve raw binary64 probabilities, ten integer-tenth bins, all
empty groups, NaN means exactly at zero weight, conditional signed errors,
pooled and grouped absolute discrepancy and their nonnegative cancellation gap.
Check artifact marginalization against ungrouped sufficient sums in fixtures.
Independently verify base-three marginals before using saved probabilities;
the unsaved native matrix projection is recreated only after scalar verification
to retain the executed bin assignments. This is not a numerical-library audit.

Check 320 full array bundles, 92,160 metric rows, 225,280 frame forecasts,
704 unchanged reader packets and 5,120 parent loss/accuracy identities. Preserve
complete reconstructed arrays separately from reader inputs. Native self-forecast
calibration must be zero while its uncertainty remains separate.

Regroup original and reconstructed rows separately using scalar law means and
bootstrap multiplicities: 576 means, 576 within-readout cancellation estimates,
1,296 paired budget contrasts and 324 normalized log-budget areas. Seed 191017
fixes 10,000 paired resamples of sixteen laws, keeping two draws and five seeds
inside each law. Intervals condition on these fits. Final numerical acceptance
requires a third direct-index regroup of the original rows.

Known cancellation, no cancellation, native self, empty groups, exact/adjacent
bin edges, permutations, mass conservation, all eight endpoint assignments,
target leakage rejection, full synthetic execution and corruption checks precede
source freeze. Synthetic full-roster timing must fit the remaining inclusive
1,800 CPU-second D card, including failures, tests, replays and operating work.
Use the existing single-worker queue, CPU only, below normal and one numerical
thread. Original snapshots stay unchanged; no fits, samples or protected lineages.

Execution/replay acceptance remains separate from numerical acceptance. This is
an exploratory constructed-method diagnostic, miniature — architecture untested
beyond the declared roster. Historical goal correspondence and human intent
remain unestablished. Retrospective updating and declared-purpose calibration
remain two prepared, unimplemented independent alternatives.
