# Prior-informed lottery value: prepared finite alternative

Does learning the source prior before choosing storage have value beyond a fixed
randomized policy? Freeze the same finite mask library, three prior vertices and
28 roster/cost/budget problems. Enumerate prior mixtures with denominator four.
For each mixture compare expected retained mass when the prior identity is
revealed before choosing a feasible mask with the best single mask chosen before
revelation. Compare the existing robust coverage lottery separately. Teacher input
is the supplied prior distribution; no forecasts, outcome labels or new data.

Retain rational masses, all ties, selected masks, individually realized bytes and
the nonnegative value of prior information. A fixed lottery cannot beat the best
fixed mask on this linear average objective; use that identity as a ruler and
measure the value of revelation, not a supposed violation of it. Prior identity
here is an evaluator variable, not learned provenance or access to human intent.

Known strict value, identical-prior zero value, point-mixture zero value, prior
permutation, exhaustive independent optimization and corrupted-byte controls
precede admission. Original and two full replays, complete-support timing,
independent reconstruction, paired/equal-law regroup and write-through must fit
an inclusive 1,800 CPU-second cap within the remaining family allowance. Preserve
source, plans, input/environment hashes and raw evidence. No fitting, new laws,
protected lineages, sampling or reader inputs. Prepared, unimplemented and
unadmitted; precision-choice stability is an independent alternative.
