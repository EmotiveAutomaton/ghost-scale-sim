# Scalar checker cost repair

The absolute-envelope producer and both complete replays remain immutable. Its
first numerical checker was tested but not admitted because the inclusive
1,800 CPU-second card could not cover its conservative remaining cost.

The repair removes array allocations from scalar finite-value comparisons and
converts coordinate slices to Python floats once before scalar summation. It
retains the independent scalar algorithm, `math.fsum`, every original bin,
zero-weight and empty-bin behavior, all conservation and nesting checks, raw
residual reconstruction, original tolerances, parent identities, and every
paired-law estimate. It does not import producer sums or change scientific scores.
The original tested source, failed fixture, timing receipts and charges remain.

Before admission, require the complete original 77-control suite plus exact
agreement with the earlier independent bin algorithm and explicit nonfinite and
changed-sum rejection. Execute from an isolated source copy. Retain complete
source, test log, benchmark, input and plan bindings. Numerical acceptance still
requires execution against every original output and independent regrouping.

The full-size synthetic benchmark measures two complete sixteen-law, 704-frame
bundles and the complete 92,160-row regroup. Preserve the prior slower-of-two
estimate, 50 percent margin, 336 bundle allowance, four regroup passes, four
archive passes and 90 additional seconds, plus 120 operating seconds. Charge
the benchmark and all repair tests before checking the unchanged inclusive cap.
No smaller margin, new sample, model fit, protected lineage or cap extension is
authorized. If the repair cannot fit after these charges, retain the blocker.
