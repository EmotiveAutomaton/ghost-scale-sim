# Conditional goal dependence: execution admission

This implements the prepared witnessed-goal protocol on its complete declared
evidence tier. The full within-step diagnostic remains implemented but undispatched:
its conservative complete-work estimate exceeded its unchanged 1,800-second cap.
No outcome or favorable lineage was used to reduce that comparison's roster.
This separate question conditions on the actual public operation witnesses and
asks whether dependence among the remaining goal alternatives helps prediction.

Retain all 704 complete-witness packets, sixteen development laws, two training
draws, five feature seeds, four label budgets and four readouts from the accepted
joint-readout study. Original fits, targets, source and population are immutable.
Bind the accepted support decoder's masks, frame order and raw score strata too.
Every public witness must select exactly its 27 goal alternatives in that mask.

Expand the original unknown tail first. Normalize on the supplied operation
sequence, retaining the accepted support decoder's binary64 normalization order.
Multiply the three three-way marginals of the resulting 27-way goal distribution.
Keep original, restricted joint and restricted factored forecasts as three arms.
Store all goal marginals, restriction normalizers and witnessed operation codes.
Native posteriors and their products are separate evaluator rulers. All original
and accepted restriction scores must reproduce within 1e-10 before claims.

Expected coverage is 7,680 learned strata, 48 native strata, 112,640 learned frame
forecasts and 704 unchanged public packets. Expected summaries are sixteen budget
contrasts, four normalized log-budget areas and 48 metric means. Resample sixteen
paired lineages 10,000 times with seed 191008, averaging two draws and five seeds
inside each lineage. Report draw and seed variation separately. The primary contrast
is restricted goal product minus restricted joint logarithmic loss; all other
proper-score, compatibility, candidate, localization and abstention metrics remain.

Known-answer controls cover correlated and independent goals with fixed operations,
identical marginals but different paths, integer-axis reversal, uniform unknown
mass, point forecasts, zero restriction mass, wrong witnesses, invalid inputs,
complete synthetic execution, corrupted scores/masks/reader fields and missing
strata. Native dispatch and portable handler coverage are part of the scoped suite.

Independent numerical acceptance requires direct scalar enumeration of all 27
goal labels, restriction and marginal reconstruction, verified saved binary64
forecasts before tie-sensitive scoring, every native/learned metric, original
number reproduction and complete paired regroup. It may reuse the existing
independent score checker, not the producer's scoring or marginal routine. Tolerances
are 1e-12 for probabilities and 1e-10 for scores. Full adjacent and extracted-source
replays remain separate requirements. The complete independent handler is not yet
implemented and is the next review step after execution; no numerical acceptance
is claimed from producer controls or replay alone.

The synthetic complete-work estimate is 854.46 CPU seconds, including a 25% compute
margin and 240 seconds for input/output, regrouping and operating work. The native
inclusive 1,800-second cap remains binding. The 96-hour total, 16-hour protected
reserve, single existing worker and campaign clocks remain unchanged. No fit,
protected lineage, new setting, or scientific owner is used.

Retrospective evidence updating and temporal alphabet-mass matching remain two
independent prepared alternatives. Full within-step factorization retains its
timing blocker and controls. This is a constructed-method diagnostic; it cannot
establish historical goals, causal access, human intent or confirmation.
