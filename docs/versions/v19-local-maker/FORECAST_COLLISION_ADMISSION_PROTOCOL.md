# Exact forecast collisions: executable scope

Does an identical frozen nine-number local-goal forecast represent different
native goal distributions? The prepared collision protocol controls this test.
Preserve sixteen laws, two training draws, five fit seeds, four budgets, four
readouts, two forecasts and all 704 complete-witness frames. There are 320
settings, each with separate native and equal-frame populations. No new fits,
samples, near-equality merges or protected lineages are used.

Within each setting group frames by exact binary64 equality of all nine forecast
coordinates, treating signed zeros as equal. Every law/frame membership remains
explicit through complete law and frame rosters and an integer membership array.
Report frame multiplicities, law/frame multiplicities, positive-mass membership,
singleton counts, group mass and weighted native target. Empty groups retain
undefined means. Verify the three position simplexes separately. Reconstruct
all 5,120 accepted parent loss and accuracy identities before interpretation.

Weighted squared probability error decomposes into distance from the pooled
group target, target variation among frames within each law, and variation of
law-specific group targets across laws. All four components are retained for
each position and goal. Native pooled targets are evaluator-only oracle references;
neither forecasts nor reader inputs are replaced.

Seed 191022 fixes 10,000 paired sixteen-law resamples. Each resample rebuilds
group mass and target sums from its law multiplicities. Average across the two
draws and five fit seeds only after regrouping each setting. Keep draw and seed
means separately. Preserve 2,304 within-readout estimates, 1,728 predictive-bank
minus baseline budget contrasts and 432 normalized log-budget areas. No practical
threshold or architecture winner is specified. Do not treat pooled group means
as independent laws.

Equal-frame group masses are identical across laws. Expanding the squared pooled
numerator into a law-pair Gram sum computes the same resampled target variation
without repeating a frame-scale calculation. Direct scalar law/frame enumeration
checks this identity, including unequal weights and zero masses. Native populations
with unequal group masses use explicit resampled sums and denominators. This is
an algebraic cost reduction, not an approximation or a change to grouping.

Before admission require controls for identical and opposing native targets,
within-law and across-law variation, one-bit-distinct forecasts, unequal and zero
weights, permutations, singleton groups, total error conservation, faulty merges,
all small law multiplicities, complete synthetic native execution, parent and
roster corruption, and native/portable dispatch. Original, adjacent and extracted
source replay must agree. Independent scalar reconstruction and a third regroup
remain required before numerical acceptance.

The inclusive D-card cap remains 1,800 CPU seconds. Retain the first failed
cost benchmark. Full synthetic timing includes all resamples, both populations,
array I/O, summary costs and conservative independent-review reserves. Retrospective
updating and comparison with a complete joint-goal forecast remain independent
prepared alternatives. This constructed-method diagnostic does not establish
usable learned access, historical correspondence or human intent.

The first native attempt stopped before scoring because exact summation of
native target components produced 1.0000000000000002 in 174 coordinates. The
replacement admission permits the existing 1e-12 simplex normalization tolerance
at the upper component bound. Raw values, exact equality keys and all scoring
remain unchanged: there is no clipping or renormalization. A dedicated fixture
retains the one-bit distinction above one and rejects a 2e-12 excess. The original
attempt, source, partial outputs and charge remain separate; its unstarted
replays are disposed before admitting a replacement.
