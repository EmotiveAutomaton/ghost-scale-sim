# Independent fixed-bin goal-confidence reconstruction

Freeze this checker before consuming calibration outcomes. It imports no producer
decisions, scoring, binning or aggregation. Integer base-three paths and scalar
sums independently reconstruct each goal marginal from the frozen fine forecast
and each native correctness probability from sparse evaluator targets. Validate
the saved binary64 marginals before exact modal choices. Validate saved confidence
and correctness before exact bin, clipping and support decisions; near-boundary
arithmetic agreement does not authorize changing the recorded decision primitive.
For native self-forecasts, recreate only the unsaved matrix projection primitive
and check every marginal against scalar sums; this is not a numerical-library audit.

Use scalar right-edge comparisons to fixed integer-tenth binary64 edges. Rebuild
all ten weight, confidence and correctness sums with accurate scalar summation;
empty bin means remain undefined. Independently recompute signed error, absolute
bin discrepancy, squared discrepancy, expected binary Brier and logarithmic loss,
including explicit infinite mass and probability-clipping count/mass. Test
zero-weight impossible outcomes and within-bin cancellation. Proper losses use
the stipulated clipped probabilities; raw calibration uses unaltered values.

Retain all sixteen laws, two draws, five seeds, four budgets, four readouts, two
forecasts, two weighting populations and three positions. Reproduce all 5,120
fine-loss/modal-accuracy parent identities. Check all saved arrays, identities,
complete row/packet rosters and scientific/reader separation. Independently regroup
original and reconstructed rows using scalar means, bootstrap multiplicities and
explicit logarithmic quadrature: 720 contrasts, 180 areas and 192 pooled means.
Seed 191014, 10,000 resamples; draws/seeds remain within laws. Nonfinite pairs have
undefined estimates and explicit counts. Pooled bin numerators and denominators
are averaged before dividing, separately from average lineage-bin discrepancy.

Complete synthetic execution and corruption controls, isolated source admission,
and conservative full-roster timing precede dispatch. Original producer/replay
snapshots remain unchanged. Charge checker/tests/failed attempts to the existing
inclusive 1,800 CPU-second D card and preserve the global reserve. The existing
single-worker queue owns execution. A later completion event must verify hashes,
native accounting, every reconstructed result and a third direct-index regroup
before numerical acceptance. This verifies constructed-method calibration, not
historical goal correspondence or human intent.
