# Joint versus marginal forecast partitions: admission and execution status

Does a full forecast over three local goals distinguish native dependencies that
its separate marginal forecasts merge? The joint-partition handler freezes the complete 27-outcome target, exact joint and marginal forecast groups, both populations and 10,000 paired law resamples. Every setting retains all memberships, using hash-bound reuse only for identical complete partitions. An independent cross-moment calculation passes exhaustive scalar controls. No numerical result is accepted.

The source-frozen comparison retains sixteen development laws, 704 public frames,
two existing training draws, five paired seeds, four budgets, four readouts and
both restricted and goal-product forecasts. Native and equal-frame populations
produce 1,280 logical group bundles. The fixed summaries are 384 estimates, 192
joint-minus-marginal budget contrasts and 48 normalized log-budget areas. Every
law multiplicity rebuilds group masses; repeated frames and seeds are not laws.

All 53 isolated producer, parent-input, runtime, portable-dispatch and independent
regroup controls passed without skips or warnings. They include complete synthetic
execution, equal-marginal different-dependence cases, zero masses, one-bit
differences, all small law multiplicities, permutation and deliberate corruption.
All 762 source files and 327 accepted parent inputs are hash-bound. No new fit,
tiny setting, sample or protected lineage was used.

The first synthetic timing estimate exceeded the inclusive 1,800 CPU-second cap
and is retained. Expanding the independent pooled-target square into complete
law cross-products, storing already-compressed arrays without recompression, and
avoiding duplicate array writes during regroup reduced the estimate to 1,525.72
CPU seconds including tests and prior work. This includes a 50% timing margin,
three complete executions, scalar reconstruction, two regroup calculations,
archives and 120 operating seconds, assuming no beneficial partition reuse.
Neither the population, resample count nor cap changed.

The existing serial supervisor dispatched the original and both full replays.
All three now have native completion receipts. Their output integrity, replay
equality, group reconstruction and scientific conclusions have not been reviewed
in this admission turn. A native running heartbeat was observed; the job exited
before an external worker-priority inspection. The frozen executor sets below-normal
priority and one numerical thread. No replacement service or second owner was
created, and the priority observation limitation remains explicit.

Reader inputs contain only the unchanged public packets. Native joint targets,
group means, pair moments and diagnostic scores remain evaluator/scientific
evidence. A finer partition would not establish a trained decoder, historically
correct goals or human intent. The independent next alternatives are
[full-simplex retrospective updating](RETROSPECTIVE_QUOTIENT_PROTOCOL.md) and
[retained reachable-posterior updating](REACHABLE_RETROSPECTIVE_PROTOCOL.md).
Both remain unimplemented, require bounded admission and do not depend on this
comparison producing an advantage. The week remains open with 73 accepted batches.

[Frozen executable contract](JOINT_FORECAST_PARTITION_ADMISSION.md).
[Admission evidence](../../../results/v19/G19-D-joint-forecast-partition-1/ADMISSION.json).
