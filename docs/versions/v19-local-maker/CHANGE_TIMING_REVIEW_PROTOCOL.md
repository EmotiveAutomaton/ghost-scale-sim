# Independent review of earlier and later actual changes

The frozen [timing comparison](CHANGE_TIMING_PROTOCOL.md) requires reconstruction
before numerical acceptance. This checker uses independent scaled likelihood
products, native path probabilities, endpoint-law summation, stream generation and
scoring. It never imports the producer's filtering or scoring functions. The
previous independent checker supplies the separately retained parent identities.

Rebuild both actual boundaries and every copy from the original random namespace.
Check all five hypothesis rosters, priors, source-time likelihoods and query-time
state projections, every float64 joint weight, row mapping, forecast, type mass,
score, population denominator and reader projection. Independently reconstruct
the original midpoint/stationary stream identities and scores. Retain them once;
they are paired references, not additional sampling. All frozen inputs, sources,
environment, plans, raw outputs and separate execution timings must verify.

Average sixteen initial makers equally, then average the two observation draws
within each of eight coefficient lineages. Retain separate actual quarter, length,
factor, source condition and checkpoint. Keep draw means and lineage values.
Produce means, every distinct paired arm comparison and each same-arm contrast
against the original midpoint. Use 10,000 paired lineage resamples with seed
190501 and percentile 95% intervals, conditional on this finite population. The
0.02-nat practical margin applies only to logarithmic losses. Fixed checkpoints
do not match evidence age after changes; no location-pooled estimate is admitted.

Before the scientific check, test uniform nulls, direct scalar products at both
boundaries, future-state projection, duplicate conflict and impossible evidence,
paired draw/lineage aggregation, reader truth rejection, corrupted parent scores,
corrupted joint mappings and one complete native fixture. Source freeze and
admission precede campaign execution through the existing single-worker queue.
The review shares the timing card's four-hour cap, including failed controls,
replays and administration. No fit, new setting, protected lineage or new science
sample is admitted by this verification. Bounded primitive feedback and source
identity omission remain independent prepared alternatives.

Exact laws and the allowed change family are supplied. This is a constructed
method test, miniature — architecture untested. Current-state recovery and future
prediction remain distinct from historical process correspondence and human intent.
