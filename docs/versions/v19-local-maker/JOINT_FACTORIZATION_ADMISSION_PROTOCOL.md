# Frozen learned goal/operation dependence: admission contract

Does dependence in a learned joint process forecast improve its predictions
relative to the product of its own goal-sequence and operation-sequence marginals?
This implements the prepared JOINT_FACTORIZATION_PROTOCOL without new fits,
support restriction, outcome labels, samples or protected lineages.

Use every saved original joint-readout forecast: four evidence tiers, nested
budgets 32/128/512/2048, two training draws, five feature seeds, sixteen development
laws, raw history, frozen latent state, predictive bank and matched frequencies.
Expand the original unknown probability over all unseen labels. A label encodes
one of 27 goal sequences and 216 operation sequences. Sum each sequence marginal
and take its outer product. Dependencies within each sequence remain intact.

Reproduce every original metric before accepting an extension. Preserve native
weighted logarithmic loss, excess over native entropy, squared probability error,
native compatible mass, nominal 90% candidate coverage/size, unsupported modal
assertions, per-step goal/operation matches, modal confidence and abstention.
Original alphabet-first tie priority is retained identically in both learned
arms. Product forecasts can create native-incompatible combinations; do not mask
them using truth or public mechanics. This is the tested dependency removal.

Native joint posteriors and products are separate evaluator-only rulers, once
per tier and law. Their ranking uses ascending full-label order, with no training
alphabet. Preserve every native score and both marginal arrays; zero mass off
native support is permitted, zero probability on a positive target fails scoring.
Products do not average scores. Learned and native products never enter reader
inputs. Store learned marginal arrays, full source forecasts and exact references.

The primary diagnostic contrast is product minus original logarithmic loss for
each arm, separately by evidence tier and budget. Average draws and seeds within
each lineage before 10,000 paired lineage resamples, seed 191005. Preserve all
draw/seed summaries and normalized log-budget areas. Report all other metric
means; a beneficial marginal or component cannot replace the G-P1 joint outcome.
Conditional lineage intervals are distinct from training-population uncertainty.

Controls precede outcomes: independent-joint identity, correlated binary targets,
identical marginals with different joints, full unknown mass, zeros, normalized
outer products, axis reversal, incompatible cross pairs, complete synthetic
execution and deliberate score, input, hash, missing/duplicate-row and reference
corruptions. Independent review must use scalar label arithmetic, independently
reconstruct each saved marginal and product, every score and all original values,
then regroup original and reconstructed rows separately. Replays alone do not
establish numerical validity. Freeze code and inputs before scientific execution.

The inclusive A-family cap is 1,800 CPU seconds for preparation, tests, failures,
original execution, adjacent/extracted-source replays and independent review.
Synthetic timing must leave room for the complete checker. No family extension,
new tiny setting, confirmation or shared resource ownership change is admitted.
Retrospective quotient updating and temporal joint factorization remain prepared
independent alternatives. The latter removes across-step dependence instead of
goal/operation-sequence dependence. No win is needed to continue either branch.
