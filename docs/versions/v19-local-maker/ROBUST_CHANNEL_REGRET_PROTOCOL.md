# Robust cue-channel regret: prepared finite alternative

Can a deterministic storage policy protect against unknown cue reliability
without discarding useful cues? For each of the same 28 finite libraries and
15 quarter-grid mixtures, enumerate all three-label contingent policies.
Reliability is unknown within the closed interval from one-third to one.
Minimize maximum regret relative to channel-informed optimal policies over
that interval. Retain the optimal fixed-mask baseline and nominal two-thirds
policy, every exact tie, rational endpoint regret and each mask's byte charge.

For a frozen policy, its expected score is affine in reliability. The optimum
over finitely many policies is convex piecewise affine; their difference is
convex. Therefore maximum regret on this interval is attained at an endpoint.
Verify this reduction with a separate rational enumeration of all score-line
intersections and endpoints on small fixtures, and evaluate interior reliability
two-thirds as a diagnostic, without changing the robust choice. Expected
coverage, expected regret and maximum realized coverage loss remain distinct.

Controls: identical-prior null, matched point interval, known robust compromise,
joint label/prior permutation, inclusion of fixed policies, interval widening
cannot reduce optimal worst regret, all ties and corrupted byte charges.
Reconstruct original libraries and population joins; preserve paired/equal-law
summaries, original and two complete replays, raw evidence and all bindings.

Prepared, unimplemented and unadmitted. Freeze source and pass full-support
timing before admission under a prospective inclusive 1,800 CPU-second cap
within existing resources. No fitting, new observations, protected lineages,
acquisition fees or new reader input. This is finite supplied-law policy
robustness, not learned access, attained forecast accuracy or human intent.
Precision-choice stability remains the independent alternative.
