# Independent noisy mechanical-reply reconstruction

Reconstruct every legal completion, endpoint, conditional weight and binary
reply posterior using scalar Bayes sums independent of the producer. Rebuild
the actual reply probabilities from each query's true mechanical field; they
are evaluator weights and never reader inputs. Recompute expected realized
reply logarithmic and squared losses, true-endpoint probability, ambiguity,
request rates and stipulated costs. Retain infinite-loss mass without clipping.

Check all 4,608 cells, 23,808 channel-law rows, complete forecast arrays and
reader allowlists. Require reliability-half no-information identity and
reliability-one truthful-parent forecasts. Validate both zero modeled-mass
fallbacks, including an impossible literal skill reply. The trusting rival
must share the channel-aware field choice. Preserve the frozen binary64 choice
only when the independent entropy discrepancy is within 1e-14; retain scalar
and vector selection sensitivities with their score effects separately.

Regroup the eight paired development laws with 10,000 bootstrap resamples and
seed 190978. Keep tool rule, conditional model, reply accuracy, reader, policy,
population and price separate. Report means, differences from no request,
trusting minus channel-aware, and entropy choice minus each fixed field.
Native masses inherit the independently verified parent. Intervals condition
on that roster and supplied laws; finite contributions never stand for total
loss when infinite mass is positive. A second original-cell regroup is required
before numerical acceptance.

Controls cover known scalar Bayes answers, constant endpoints, no-information
and truthful channels, zero support, infinity and deliberate corruption of
forecasts, reply probabilities, targets, choices, costs, laws, entropy, fallback
records, sensitivity, reader fields and denominators. Freeze the checker and
all inputs after isolated controls, then dispatch through the existing serial
queue within the original half-hour card cap. No new fit, episode, protected
lineage or scientific owner is admitted.

Execution and complete replay can be accepted separately from numerical
adjudication. This is a constructed information method with supplied mechanics;
it cannot establish learned access, historical process correspondence or human
intent. Retrospective quotient updates and independent-versus-copied repeated
replies remain separate prepared alternatives.
