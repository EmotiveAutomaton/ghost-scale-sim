# Channel reliability misspecification: prepared finite alternative

Does mistaking cue reliability harm storage even when the source-prior mixture
is correct? Keep all 28 finite mask libraries and 15 quarter-grid mixtures.
Cross nominal and actual correctness probabilities 1/3, 2/3 and 1; errors split
equally between the two other labels. Freeze conditional choices under the
nominal channel, then score them under each actual channel. Compare actual-channel
optimal cue use and the optimal fixed mask. Keep all ties, rational regret,
signed gains, zero nominal probability with positive actual probability, and
individually feasible byte charges. This differs from wrong prior-mixture weights.

Controls require matched-channel identity, identical-prior null, known harmful
overconfidence, uninformative actual-channel limits, prior/label permutation,
complete policy enumeration and corrupted charges. Preserve all original
population joins, paired/equal-law summaries and two complete replays. No fit,
new sampling, protected lineage, acquisition fee or reader input is authorized.
This is supplied-channel sensitivity, not calibrated reliability in people.

Prepared, unimplemented and unadmitted; a protocol is not a queue job. Freeze
source and pass full-support timing before admission under a prospective inclusive
1,800 CPU-second cap within existing family/global resources. Precision-choice
stability remains a separate prepared alternative.
