# Independent joint-reply reconstruction

Does dependence between noisy skill and belief replies change what a reader
can predict when the marginal reliability of each field is held fixed?
Independently rebuild likelihoods from agreement patterns, using direct
products for independent errors and all-agree/all-disagree probabilities for
shared flips. The producer instead enumerates latent flips. Scalar Bayes sums
and independently implemented mechanics reconstruct every legal completion,
conditional weight, modeled reply mass, fallback and endpoint forecast.
Native population masses inherit the independently verified parent.

Check all 3,072 score cells, all possible reply strings and actual reply
probabilities, no-request and identical-marginal identities, request counts,
and complete reader projections. Scores average realized reply losses, not
loss at an averaged forecast. Preserve impossible replies with zero actual
mass and infinite-loss mass separately from finite loss contributions.
Fallbacks at zero modeled mass are conventions, not evidence.

Regroup eight paired development laws using 10,000 resamples and seed 190980.
Keep tool rule, conditional law, actual channel, requested fields, assumed
channel, population and price separate. Report means, differences from no
request, both-fields minus each single field, product-of-marginals minus
joint-aware conditioning, and shared flips minus independent errors. All
7,488 estimates require a second regroup from original cells before numerical
acceptance. These intervals condition on the fixed roster and supplied laws.

Eighteen isolated controls cover known scalar odds, identical marginals with
different joint information, parity at half reliability, truthful identity,
constant endpoints, both fallback rules, explicit infinity, a complete
fixture and deliberate forecast, reply-probability, target, string,
likelihood, cost, law-weight, fallback, reader-field and denominator corruption.
Native runtime and standalone replay dispatch controls are included.

Freeze checker source and original evidence before dispatch through the existing
serial queue. Its charge remains inside the same half-hour card cap, including
original execution, both full replays, controls, review and failures. No fit,
new sample or protected lineage is admitted. Execution and replay acceptance
remain separate from numerical adjudication. This supplied-law constructed
method does not establish learned access, historical process correspondence
or human intent. Retrospective updating and reliability mismatch remain two
independent prepared, unimplemented alternatives.
