# Independent checkpoint-compression review

Can a smaller set of complete maker/change hypotheses preserve useful prediction
and uncertainty at a fixed checkpoint? Numerical acceptance requires independent
reconstruction of the saved selection and scores, followed by paired regrouping.
This checker is implemented; its mandatory numerical fixtures run first in the
existing serial worker. Syntax inspection and frozen admission are not test passes.

Bind the original plan, source archive, complete receipt, all outputs and parent
inputs. Independently enumerate the complete unknown-time/type roster and state
projection at each checkpoint. Sort using Python's explicit probability/index
ordering, independently of the producer's array sort. Check every int32 selection,
float64 normalized weight and row assignment. Preserve zero weights, exact ties,
all five capacities and original full-roster ordering. Sum state buckets with
accurate scalar sums and contract the supplied endpoint laws with the independent
scalar score kernel. Reconstruct current-maker loss, endpoint loss, true-state
mass, tie-inclusive 90% inclusion and size, removed mass and storage counts.

The full mixture must exactly reproduce saved parent forecasts and scores, while
the independent arithmetic agrees within 1e-10. The sixteen-state current marginal
is an exact same-time endpoint predictor; it is not a recursive sufficiency claim.
The original 1e-300 logarithm floor stays fixed, including large losses when a
truncated account assigns zero mass to the actual maker. Component weight/index
bytes exclude shared hypothesis metadata, runtime objects and acquisition cost.

Reconstruct all 14,080 maker-averaged cells from 225,280 rows. Average sixteen
makers within draw, then both saved draws within eight paired coefficient
lineages. Preserve every capacity, evidence tier, history length, reference change
type, actual change/stationarity, source-copy condition and checkpoint. Compare
each capacity against all, and omitted against aware at the same capacity. Retain
draw means and lineage values, plus 10,000 paired lineage resamples with seed
190501. The 0.02-nat margin applies to losses only. Separate set inclusion from
set size and probability quality. No selected best capacity or new replication.

Mandatory controls cover a known ambiguity, zero-mass loss, zero-weight removal,
stable ties and index sensitivity, an actual change boundary, scalar forecasts,
complete paired draw regrouping, a complete two-horizon native fixture with both
source conditions, and deliberate retained-index corruption. Runtime accounting
and portable dispatch tests run in the same gate. A failure blocks campaign
reconstruction and is retained. No concurrent numerical test runner is launched.

Parent laws and complete posteriors inherit prior verified provenance; this is
not a new native-law or likelihood-filter audit. Reader inputs remain the unchanged
bound parent exports. Scientific selections, laws, true makers and hypotheses are
never reader inputs. Source and input hashes accompany every receipt.

All work is charged to the original two-CPU-hour compression cap, within the
existing B-family/global pool. No fits, new observation draws or protected
lineages. Independent [primitive feedback](PRIMITIVE_FEEDBACK_PROTOCOL.md) and
[current-marginal transitions](MARGINAL_TRANSITION_PROTOCOL.md) remain prepared;
neither needs compression to win.

The first independent checker admission was withdrawn before numerical execution after static review found a NumPy boolean in a JSON control verdict. Its source and plan remain retained. A separately frozen replacement converts the verdict to a native boolean and adds a serialization regression check; scientific selection, projection, scoring and population definitions are unchanged. All numerical fixtures remain mandatory before reconstruction.

That replacement stopped at its fixture gate: eight tests passed, including the
complete native reconstruction and deliberate corruption test; one assertion
rejected 0.37499999999999994 against a lower bound of 0.375. Exact rational
arithmetic gives (3/10)/(1/2 + 3/10) = 3/8; the computed value is one binary64
step below it. The next frozen checker replaces that lower bound with an
equality check against the exact expected mass, allowing one binary64 step.
This is a tighter known-answer check, not a changed scientific tolerance.
The failed gate, log, source, fixture outputs and CPU charge are retained.
All nine tests must pass in the serial worker before campaign reconstruction.
No selection, score, bootstrap, population or practical threshold is changed.

The separately frozen assertion repair has now passed all nine mandatory controls
without skips, including the complete native fixture and deliberate corruption.
Independent campaign reconstruction was observed running afterward. This gate
verifies the repaired checker apparatus; it does not accept the campaign scores.

Completed review: the repaired checker verifies all 225,280 rows and 14,080 maker-averaged cells within 2.85e-14. A separate event review recombines all 19,800 estimates and paired intervals. Both full replays match 111 deterministic outputs. The [report](MIXTURE_COMPRESSION_REPORT.md) separates endpoint similarity from lost maker support; preceding pending statements describe the admission history.
