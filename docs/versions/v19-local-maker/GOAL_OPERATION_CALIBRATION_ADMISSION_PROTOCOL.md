# Public-operation calibration: executable contract

Does pooling complete witnesses across public operations conceal opposing errors
in local-goal probabilities? Retain the prepared sixteen-law, two-draw, five-seed,
four-budget, four-readout, two-forecast roster. Use all three positions, all three
goal classes, all six legal public operation labels and ten fixed integer-tenth
bins. Native and equal-frame populations stay separate. Group membership uses
the witnessed operation at the scored position, never a target or outcome score.

Retain joint group/bin weight, forecast sum and native-correctness sum before any
division. Store every empty group and bin; derived means are NaN exactly when
their denominator is zero. Operation marginalization must reproduce ungrouped
class sums on the same raw binary64 probabilities. Raw admitted roundoff remains
unchanged. This diagnostic adds no proper-loss clipping or model fit.

Pooled discrepancy sums absolute bin error after summing operations. Grouped
discrepancy takes the absolute value before summing operations and bins. Their
difference is the cancellation gap, nonnegative by the triangle inequality apart
from recorded roundoff. Signed conditional group errors remain available. The
gap is a property of aggregation, not new evidence or improved forecasts.

Save 320 complete array bundles with axes law, population, position, goal,
operation and bin, 92,160 class-level metric rows, native self-forecast arrays,
704 unchanged reader packets and all 5,120 parent loss/accuracy identities.
Retain 576 metric means and 576 within-readout cancellation estimates; 1,296
bank-versus-baseline budget contrasts and 324 log-budget areas use seed 191016
and 10,000 paired law resamples. Both draws and five seeds stay inside each law.
Intervals condition on retained fits. No practical magnitude threshold is added.

Known cancellation, no-cancellation, identical-error, native-self, empty-group,
permutation, exact/adjacent-edge, conservation and corruption fixtures precede
dispatch. Synthetic timing must admit three complete executions and independent
reconstruction, all tests, failures and operating work inside the inclusive
1,800 CPU-second D card. Numerical acceptance requires a separate checker of all
group/bin arrays, parent identities and paired regroup; replay alone is insufficient.
Source, inputs, plan, environment and operation/bin definitions freeze before run.

This is a constructed-method calibration diagnostic. Supplied witnessed operations
do not establish historical goal correspondence or human intent. Reader packets
exclude predictions and evaluator truth. No new fit, tiny setting, sample or
protected lineage is used. Retrospective updating and artifact-conditioned
reliability remain prepared independent alternatives.
