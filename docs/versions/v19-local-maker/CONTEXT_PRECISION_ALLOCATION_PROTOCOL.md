# Context-specific law precision under a byte budget: prepared alternative

Does allocating precision across query contexts tighten the conservative
rounding bound at fixed shared-law storage? Use the same eight retained laws,
the same three storage precisions, and the fixed balanced context counts for
lengths 8, 32 and 128. There are exactly 81 assignments of three precisions to
four contexts. Enumerate them all before selecting the minimum summed
context log-ratio range under each of the fixed 1,280, 1,536 and 2,048-byte
budgets. Count actual stored law entries; compare uniform precision wherever
feasible and retain all ties. Shared schedules and posterior state are separate
costs. Teacher access is the supplied law, exclusively evaluator information.

Freeze this finite architecture, all assignments, budgets and lexical precision
tie order before any allocation outcome. Support loss yields an infinite range;
no probability floor or outcome-selected query schedule is allowed. Selection
is over this finite assignment set, not a general optimal-compression claim.
Report every assignment's bytes and bounds, the exact chosen set, equal-law
summaries and dominated choices. This does not test realized posterior error.

Required controls: exhaustive independent scalar enumeration, equal-context
and exact-cast identity, budget infeasibility, nonmonotonic greedy failure,
context permutation with matched counts, support loss and deliberately
undercharged bytes. Preserve original and two complete replays, all raw
assignments and independent reconstruction. No fits, new histories or protected
lineages. Full-support synthetic timing and source admission must precede
execution. Prepared, unimplemented and unadmitted, with a separate inclusive
1,800 CPU-second cap within existing family/global ceilings. The independent
robust source-prior mass alternative remains prepared. No learned-access,
historical-process or human-intent conclusion follows.
