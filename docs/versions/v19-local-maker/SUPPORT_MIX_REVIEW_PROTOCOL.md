# Independent practice-mixture reconstruction

The original and both complete replays agree. Numerical acceptance now requires
an independent native kernel, reconstruction of every parent count table from
its ordered observed transitions, and reconstruction of every fixed mixture.
The checker imports no producer planner, kernel, mixture or scoring routine.
It retains the original policies, verifies optimality and learned values using
independent matrix-vector Bellman reductions, and sums all 512 endpoint paths per
context. Different near-tie action choices are recorded, never substituted.

Verify all 576 parent tables, 960 mixture tables, their context successes, equal-
context means, restricted-context scores, count mass, visitation, changed seen
probabilities and policy changes. The two endpoint mixtures are parent identities.
No new trajectories, fit, neural setting or confirmation lineage is used.

Report every mean and adjacent-weight contrast, the full endpoint-span contrast,
and active-minus-demonstration at every fixed weight and budget. Retain four
draw/acquisition-seed means; average those pairs within each of eight lineages.
Use 10,000 paired lineage bootstrap resamples with seed 190501. The frozen two-
percentage-point practical margin applies to production success, not support counts.
All weights are reported; there is no selected optimum or assumed monotonic curve.

Controls precede campaign reconstruction: a fully controllable law, action-inert
law, known complete fixture, single-prior/count conservation, deliberate count and
score corruption, ordering rejection, and paired uncertainty with repeated fits.
The original source, raw tables, plan and environment stay immutable; freeze this
checker and its input hashes before admission to the existing serial queue.
Its full charge is included in the original four-hour E-family mixture cap.

All table pairings, native kernels, scores and aggregate analysis are scientific/
evaluator evidence. The original observed-transition reader export remains separate.
Replay establishes reproducibility; independent reconstruction establishes only
the stated constructed-method result, not historical correspondence or human intent.

Two independent prepared alternatives remain off-midpoint actual purpose/skill
changes in SUPPORT_TRANSFER_PROTOCOL.md and bounded primitive feedback in
PRIMITIVE_FEEDBACK_PROTOCOL.md. Neither depends on a mixture advantage. Neither
is implemented or admitted by this review protocol.
