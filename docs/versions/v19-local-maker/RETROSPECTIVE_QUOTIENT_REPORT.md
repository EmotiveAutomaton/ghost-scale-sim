# Retrospective quotient: execution and replay verified

We tested whether preserving future forecasts also preserves updates from a new report about the past. All 43 deterministic outputs match both complete replays. Independent numerical acceptance remains pending. This is a constructed-method certificate over possible mixtures; it does not establish reachable-posterior effects, historical process correspondence or human intent.

A future-schedule group contains maker hypotheses that have the same states
from the current checkpoint onward. This certificate asks whether exchanging
two group members can change the response to a separately sourced report about
an earlier state. It enumerates every within-group unordered pair with different
past schedules, every third hypothesis outside that group, every past time,
four report contexts and eight possible endpoints. The two compared mixtures
place half their mass on a pair member and half on the same third hypothesis.

All eight development laws, horizons 32 and 128 and structural checkpoints
8, 16, 17, 20 and 32 are included: ten structural cells and 80 law/checkpoint
rows. The 97,693,154,304 logical comparisons per law count a Cartesian product;
they are not independent samples or directly executed scalar evaluations.
Complete pair/third identities and multiplicities bind the lossless factors.
The factorization preserves the exact law and both updated quotient weights,
allowing every remaining-time endpoint forecast to be reconstructed. No absent
128-horizon posterior is regenerated and no mixture is claimed reachable.

Valid and impossible reports, binary64-nonzero and greater-than-1e-12 witnesses,
and maximum quotient and future-coordinate changes remain separate. Binary64
nonzero is not exact rational arithmetic. These are finite exhaustive comparisons,
with no sampling confidence interval and no weighting as a reachable population.
A copied report would supply no new information; this likelihood experiment
expressly uses a separately sourced report.

All 43 deterministic outputs agree across original, adjacent and extracted-source
complete executions. All 770 source files, 12 input files, source archives,
plans, environment fingerprints and output bindings verify. Raw summaries are
exported as unadjudicated scientific evidence. No numerical advantage or null
is accepted here; the independently accepted scientific batch count stays 74.

The independent checker passes 64 isolated controls without skips or warnings.
One initial suite failed because its portable handler was not yet registered;
the repaired entry passed the full isolated suite. The failure and charge are
retained. Tests include complete synthetic producer/checker execution, direct
exhaustive scalar Bayes and future forecasts, uniform and stationary identities,
same-time identity, zero/disjoint emission laws, permutations and deliberate
corruption of pair identities, schedules, every membership/multiplicity layer,
all likelihood-kernel fields and every summary count or maximum.

Independent reconstruction uses bit transitions, tuple classifications and
integer counters without importing producer transition or aggregation code.
It verifies all original identities before collapsing identical calculations.
Each law's 131,072 Bayes cells are computed scalarly and checked against a
separately rearranged formula. Scalar endpoint differences reconstruct every
future-mask maximum. All 80 original rows must match, including empty and
impossible cells. Reconstructed evaluator factors and summaries are retained.
A separate event-owned regroup is still required before numerical acceptance.

The full structural roster and an outcome-independent full synthetic-law
workload give a conservative inclusive estimate of
458.83 CPU seconds under the unchanged
3,600-second card cap. That includes 131.94
seconds for the checker, 60 for the
third regroup and 150 for remaining
operations. Tests, the failed test attempt and previous executions are charged.
The existing serial queue dispatched the source-frozen checker and native
BelowNormal priority was observed. Its completion/failure event owns numerical
review. No second scientific service, fit or protected lineage is admitted.

All laws, hypotheses, mixtures, kernels and outputs are scientific/evaluator
evidence. This batch adds no reader inputs. The original and both replay source
snapshots remain unchanged. Reachable-posterior updating and joint retrospective
likelihood state remain separately prepared, unimplemented alternatives. A3,
C2 and F2 remain subject to their own unmet triggers. This miniature is
architecture untested beyond the declared roster; neither historical process
correspondence nor human intent follows. The research week remains active.

[Execution contract](RETROSPECTIVE_QUOTIENT_ADMISSION.md).
[Independent review](RETROSPECTIVE_REVIEW_PROTOCOL.md).
[Execution evidence](../../../results/v19/G19-B-retrospective-quotient-1/EXECUTION_REVIEW.json).
[Evidence roles](../../../results/v19/G19-B-retrospective-quotient-1/EXECUTION_EVIDENCE_ROLES.json).
