# Independent pooled replacement review

Reconstruct all six arms from saved original transition counts and complete
fixed feedback rosters. A separate scalar calculation enumerates each supplied
equivalence group. Addition keeps its original empirical counts; replacement
discards them only for observed groups. Both retain one prior per endpoint and
count each distinct complete feedback input once. Conflicting repeated input
reports fail. Unobserved groups retain their pooled counts.

Use the separately validated mechanics and explicit path sum to reconstruct all
forecasts, proper scores, native population weights and support denominators.
Audit every reader projection, group membership, query and output inventory.
Do not import the producer's pooling, replacement or scoring routines.

Regroup both retained training draws within each of eight paired development
laws. Report means, replacement minus matched addition, pooling minus matched
unpooled update, feedback minus zero, wrong minus true and reverse minus forward.
Keep tool, support, changed/stay and native/equal-query strata distinct. Use
10,000 paired lineage resamples, seed 190975. Equal-query intervals can be
degenerate because predictions repeat across laws; these do not estimate
training or feedback-population uncertainty. The practical margin is 0.02 nats.

Require known scalar counts, one-prior and unobserved identity controls,
duplicate/conflict controls, a uniform null, paired regroup fixtures and a full
native fixture with deliberately corrupted counts, forecasts, losses, group
maps, populations, reader fields and denominators. Source admission and portable
dispatch precede native review. All work remains within the existing one-hour
replacement cap, including original execution, replays, tests and review.

This verifies a constructed method with supplied grouping semantics. It does
not establish discovered mechanics, historical process correspondence or human
intent. Original sources and scores remain unchanged; no new fit is admitted.
