# Coupled arithmetic interpolation: prepared alternative

Does preserving correlation between two arithmetic evaluations narrow uncertainty
about a precision choice? Retain the eight laws, 81 assignments, three budgets,
four count vertices and lengths 16/64/128. Interpolate every context/precision
range with one shared weight between the producer and independent scalar arrays.
This is a one-dimensional sensitivity model, not a certificate for true values.

Convert endpoint floats to exact rationals. Enumerate all vertex-line intersections
within weight [0,1] for every feasible assignment; then enumerate objective-line
intersections between assignments on each resulting segment. Retain all optimizer
changes and ties and the maximum regret of each recorded producer choice over the
whole interval. Compare against the conservative independent-coordinate box bounds.
No fitting, observations, protected lineages, new reader inputs or sampled weights.

Controls include zero-width identity, a known optimizer reversal, exact ties,
common weight versus independent coordinates, context permutation and byte charges.
Require complete-support timing including every pair/segment, frozen source and
both full replays within a prospective inclusive 1,800 CPU-second cap. Prepared,
unimplemented and unadmitted. Realized-risk-constrained channel regret remains
an independent alternative; the randomized-channel timing rejection stays retained.
