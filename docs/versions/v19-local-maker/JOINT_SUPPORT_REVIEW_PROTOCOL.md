# Independent review of public-mechanics support decoding

Reconstruct every legal operation path using a separate scalar executor, all
eight hidden starts and both skill/belief bits. Preserve all 27 goal sequences.
No producer mechanics, support, scoring or regrouping function is imported.
Validate exact reader field allowlists, packet hashes and unchanged packets;
compare every mask and retain native posterior support as a separate target.

Reuse the previously accepted parent forecasts and native posterior masses with
their hash bindings. This review does not refit models or regenerate the earlier
population. Reconstruct the full unknown tail, sum legal mass independently,
and compare every saved normalizer. After this comparison, use saved normalizers
to adjudicate discrete candidate decisions on the actual binary64 forecast.
Known alphabet labels retain priority at exact ties; unseen labels enter the
90% candidate set when needed. Empty/zero-mass support remains an explicit failure.

Compute proper scores, native-compatible mass, candidate coverage/size, incompatible
mode, abstention, tail mass and every localized goal/operation score by sparse
frame-local truth sums followed by native lineage weighting. Reproduce all 10,240
original strata and all 23,040 support-comparison strata, with no selected subset.
Independently regroup all 128 budget contrasts and 32 log-budget areas. Average
draws and seeds within each of sixteen lineages, then use the frozen 10,000 paired
resamples and seed 191003. Preserve draw and feature-seed summaries separately.

Known-answer controls precede campaign input access. They cover hidden starts,
goal retention, request invariance, tool/undo mechanics, reader leaks, impossible
witnesses, empty/zero-mass fallbacks, native-versus-legal compatibility, unknown-only
support, exact ties, scalar proper scores, deliberately excluded truth and a complete
synthetic producer/checker fixture with corruption detection. Original sources and
criteria remain frozen. All work shares the existing 3,600 CPU-second card cap,
including failures, controls, original, both full replays and this checker.

Complete replay verifies reproducibility. Independent numerical acceptance still
requires review of this checker's complete output and full documentary write-through.
This is a constructed-method supplied-mechanics diagnostic; legal support, predictive
quality and historical correspondence are separate. No new fit or protected lineage.
