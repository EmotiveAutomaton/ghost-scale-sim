# Independent complete-class goal reliability reconstruction

Freeze this checker before consuming numerical class-reliability outcomes. It
imports no producer scoring, binning, decisions or aggregation. Scalar sums over
base-three paths reconstruct every learned and native goal marginal. Validate
saved binary64 probabilities before exact bins and clipping; unsaved native
projection primitives are recreated only after scalar agreement. This checks
the application, not the numerical library.

Reconstruct all three classes at all three positions with native and equal-frame
weights. Retain every fixed integer-tenth bin's weight, forecast and correctness
sums, explicit empty means, signed error, absolute discrepancy, clipped count/mass,
squared forecast error, expected binary Brier loss and native uncertainty.
Enumerate each one-hot outcome explicitly for an independent multiclass Brier
expectation; check its agreement with the class sum and uncertainty decomposition.
Raw calibration stays distinct from clipped proper loss. Preserve normalization
error rather than silently renormalizing. Native self-forecasts must have zero
calibration error while retaining proper uncertainty.

Require complete rosters: 92,160 class rows, 288 native rows, 30,720 decompositions,
225,280 frame forecasts, 5,120 fine-parent identities and 704 unchanged reader
packets. Regroup original and reconstructed rows independently with scalar means,
bootstrap multiplicities and explicit logarithmic quadrature: 2,160 budget
contrasts, 540 areas, 576 class means and 192 equal-class means. Seed 191015 and
10,000 paired law resamples retain the two draws and five seeds inside each of
sixteen laws. Equal-class means are descriptive; class-specific bins never pool
opposite class errors. Pooled law-bin discrepancy and average law-bin discrepancy
remain separate. A third direct-index regroup is required at numerical review.

Before admission, validate known one-hot expectations, hidden unselected-class
error, uniform/native forecasts, label permutation, exact and adjacent bin edges,
cancellation, empty bins, roundoff, invalid conservation, complete synthetic
execution and corrupted inputs/outputs. Isolated tests, conservative full-roster
timing and source/input/plan/environment hashes precede serial dispatch. Tests,
failures and review remain inside the existing inclusive 1,800 CPU-second D card.
Original and replay snapshots stay unchanged. No fitting, sampling, protected
lineages or new model settings. Execution write-through can establish reproduction;
scientific numerical acceptance awaits the checker's completion-event review.

This is a constructed-method reliability diagnostic. It establishes neither
historical goal correspondence nor human intent. Reader packets stay separate
from forecasts and evaluator targets. Retrospective updating and operation-group
calibration remain prepared independent alternatives.
