# Practice support mixtures: replay verified, numerical review pending

Can reallocating a fixed practice-evidence budget restore production in an unvisited context? All 960 saved mixtures and both complete replays match across 1,743 deterministic outputs. The independent count, policy, score and paired-lineage checker is source-frozen; numerical benefits and tradeoffs remain unadjudicated. This is a constructed-method reproducibility result, not historical process correspondence or human intent.

The comparison reweights saved transition counts from restricted-start and matched-
start acquisition. It uses five fixed matched-start weights, 0, 1/4, 1/2, 3/4 and 1,
at budgets of 32, 128 and 512 episodes for active acquisition and demonstrations.
All eight existing coefficient lineages, two training draws and two acquisition
seeds remain. No episode, fit, neural setting or confirmation lineage is added.
The one-half count prior appears once; total empirical feedback mass is three
times the episode budget. These fractional sufficient statistics are not five
new on-policy learning histories. Both endpoint mixtures are exact parent identities.

The producer reconstructs all 576 parent tables from ordered observations before
mixing, reproduces their original context scores and evaluates each mixed policy
by two path calculations. Its 30 aggregate cells and 960 underlying rows retain
success in both contexts, their equal mean, the restricted context, visitation,
newly visited state/action rows, policy changes and changes in already-seen
conditional probabilities. Production success is the probability of meeting the
functional endpoint condition under the stipulated high-skill native actor.

All 1,743 deterministic outputs match across original, adjacent and extracted-
source executions. All 446 producer sources, 780 parent inputs, source archive,
plans, environment and execution-specific timing hashes verify. Replay agreement
does not exclude a shared implementation error. The original summary is retained
as unadjudicated evidence; no numerical advantage, reversal, equivalence,
monotonicity or best mixture is accepted in this reproducibility review.

The independent checker reconstructs the native transition kernel from retained
coefficients and every parent count table from ordered logs. It checks chronology,
undo state, allowed transitions, exact active/replay identity, every mixed count,
one-prior accounting and fixed mass. It imports no producer planner, kernel,
mixture or scoring routine. Separate matrix-vector Bellman reductions check saved
values and policy optimality. Near-tied action differences are counted without
replacing the original first-maximum policy; explicit sums over all 512 endpoint
paths per context evaluate those saved policies unchanged.

Its regroup retains all 30 means, 24 adjacent-weight contrasts, six endpoint-span
contrasts and 15 active-minus-demonstration contrasts. Each has 12 production or
support measures. Two draws and two acquisition seeds are averaged within each of
eight paired lineages; all four fit means remain separately visible. Conditional
95% intervals use 10,000 lineage bootstrap resamples with seed 190501. The frozen
two-percentage-point practical margin applies to production success, not count
or policy-change measures. All weights are reported without selecting a winner.

Eleven isolated controls pass, including a complete native mixture fixture,
controlled and inert laws, paired uncertainty, deliberate count/score/ordering
corruption and runtime/replay dispatch. The first test attempt had two fixture-
mutation errors because its archive writer refuses overwrites; those failed
attempts remain retained and charged. The corrected tests mutate only temporary
fixtures and now exercise the intended rejection checks. The earlier complete
regression remains 120 Ghost and five Torch controls; it was not repeated here.
Checker admission freezes 449 sources and 1,747 completed inputs. Admission is
not completed campaign verification.

OBSERVED_READER.zip is byte-identical to the previously reviewed practice-reader
export: deduplicated observed transitions with content-hash names and no lineage,
draw, acquisition arm, score or native law. Its chosen action is an intervention
the learner observed, not an inferred hidden local goal. No new reader task is
claimed. SCIENTIFIC_EVALUATOR.zip holds complete mixed and parent tables, raw
scores, evaluator laws, ordered inputs, source, plan and timing. REPLAY_RECEIPTS.zip
binds all three execution receipts/timings. EVALUATOR_MANIFEST and EXPORT_MANIFEST
bind the member and artifact bytes. Evaluator material never becomes reader input.

**Warrant:** constructed-method reproducibility, numerical adjudication pending;
miniature — architecture untested. No confirmation, historical process recovery
or human intent follows. **Pursuit:** finish independent numerical reconstruction.
Off-midpoint actual purpose/skill changes and bounded primitive feedback remain
two prepared independent alternatives, each with a four-hour cap. Their handlers
and exact input admissions remain outstanding; neither requires a mixture win.

[Frozen experiment](SUPPORT_MIX_PROTOCOL.md),
[independent review](SUPPORT_MIX_REVIEW_PROTOCOL.md),
[evidence roles](../../../results/v19/G19-E-support-mix-1/README.md).
