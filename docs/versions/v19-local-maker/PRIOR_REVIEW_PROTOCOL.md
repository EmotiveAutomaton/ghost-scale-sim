# Independent source-prior verification

Reconstruct every retained source prior, report normalizer, source posterior,
entropy, support mask and complete hypothesis update without the producer's
evaluation, sparse transition or grouping kernels. Independently enumerated bit
transitions and indexed state sums reconstruct every future time, context and
endpoint. Compute proper-loss regret by explicit one-hot outcome losses, separate
from squared forecast discrepancy. Preserve undefined-report NaNs and use the
unchanged absolute tolerance of 1e-12 on normalized quantities.

Before native reconstruction, require full-shape synthetic timing, direct scalar
controls, sparse/disjoint/constant laws, single-source and permutation identities,
each retained-field corruption, complete parent integration and binding corruption.
Admission must include all existing source-prior charges, both full replays,
checker tests/timing, complete independent reconstruction and operating reserve
within the original 3,600 CPU-second card and B/global caps. No sampling or fits.

Rebuild all 143,360 summary rows and 1,120 paired strata. Preserve every one of the
128 Cartesian identities per draw, both draws inside each of eight equal laws,
both evidence conditions and all seven available checkpoint cells. All 48 absent
strata stay absent. Weight report errors by the true marginalized report law
inside each posterior; source-prior uncertainty is not a new independent sample.
After checker completion, separately regroup the original rows before numerical
acceptance. Reader inputs remain unchanged. This checks a supplied-law method,
not learned provenance, historical process correspondence or human intent.
