# V19 test coverage and serial validation

The 21 September coverage pass separates completed evidence, running comparisons,
implemented successors awaiting controls, and branches whose scientific admission
condition is unmet. The frozen opening manifest remains unchanged. The current
coverage ledger is `results/v19/TEST_COVERAGE.json`; it does not claim the week is
complete or turn a conditional extension into an unconditional requirement.

The joint goal-operation comparison is already running on its immutable sources.
The full V19 test inventory now runs in one frozen serial validation job, including
the new complete native fixtures and portable-dispatch inventory check. The Ghost
interpreter runs ordinary tests; the existing installed CPU Torch interpreter runs
the five tiny-reader regression controls while the parent waits. This is fixture
validation of the two existing settings, not another architecture setting. Preserve
both environments, CPU accounting, failures, temporary records and all live sources.
The test job uses the existing queue and cannot launch another scientific worker.

After every required test passes, the same job mechanically freezes and appends
the two designs below, each followed by a full adjacent replay and a replay from
extracted sources. Their names, populations, models, inputs and comparisons are
fixed in the validation plan before tests execute. A failed test releases neither
design and delivers the existing failure event for a scoped repair. The controller
makes no scientific selection. Numerical result review and full write-through
still belong to the event reviewer before any scientific acceptance.

## D: two supplied rule changes together

Can a family containing each individual rule change still omit processes when
the changes occur together? Use the original eight development lineages
190000–190007, all sixteen makers, four contexts and three-step native paths.
Cross the previously studied presentation-only tool with an inverted evidence
repair: for skilled makers evidence becomes the complement of the perceived
claim; unskilled repair remains a toggle. Goals and the controller are unchanged.
This models unfamiliar execution semantics with the original goal-selection
mechanics; it does not assert optimal adaptation to the changed tool.

Enumerate four populations: original, tool-only, repair-only and both. Each has
13,824 trajectories per lineage, with native probability weights. Compare exact
families containing only the original law; original plus either single change;
original plus both single changes but no combination; all four laws; and the
true-rule oracle. Priors are equal across supplied laws, retaining native within-law
weights. Score E0, E1, sparse witnesses and full witnesses separately. All rules,
hidden makers, actual goals, evaluator weights and pairing remain outside the
anonymous evidence archive.

Store complete goal-operation joint distributions, compatible-set coverage,
abstention, missing truth mass, finite log-loss contribution and entropy. Positive
truth with zero predicted mass means infinite expected log loss, never a clipped
finite result. Supplying both rules is hypothesis provision, not cause invention.
There is no fitted model or claim of learned compositional transfer. Independent
verification must reconstruct each executor and probability, full posteriors and
native denominators, then compare both replays. Charge at most four CPU hours
including all three executions and review, within the D-family/global ceilings.

Pre-execution validity review identifies a visible-execution alias: complementing
the skilled maker's belief turns inverted repair into ordinary repair. The
tool-only family can therefore retain process support for the combined law even
while assigning different native weights. The controls check that alias directly;
they do not require the scientific comparison to find missing support. Retain the
full probability/entropy contrast and report this structural limitation. The first
unstarted validation snapshot is retained as superseded because its fixture had
mistakenly required a missing-support effect. No scientific result used that check.

## F: support, compositional holdout and common smoothing

Does learned propagation help because primitive transitions were seen while full
queries were not, and how much does sampling-dependent smoothing explain? Pin F1's
two training selections and eight saved count models; use its two draws 190301 and
190302 at 2,048 paths. Reuse original fitted counts. For one new paired comparison,
remove every training path whose first two operations are accept-tool then
repair-evidence. Refit the same count estimators on the remaining paths, without
replacement or tuning. Report original and retained training counts: this holdout
changes training support and reduces the available training sample.

Evaluate all native paths on the other eight original development lineages
190008–190015. These are development data, not reserved test or confirmation data.
Retain all-query estimates and prespecified strata: seen/unseen full query, withheld
composition, and withheld composition whose three true primitive transition rows
were seen in training. The final stratum uses evaluator truth for diagnostics;
it never masks or supplies a reader forecast. Record its own population mass and
denominator, including absent strata rather than silently claiming support.

Compare direct counts, nearest-query retrieval, exact learned propagation,
1/4/16 learned sampled rollouts, and exact mechanics. Retrieval averages tied
nearest training count vectors by Hamming distance over the six supplied query
fields. Every arm receives the same skill/belief mechanics bits, initial artifact
and proposed operations. No arm receives a true transient state at prediction.
Reuse F1's deterministic query uniforms and nested sampled prefixes. Smooth every
arm's final distribution with the same fixed uniform mixture of 1/32, including
the exact reference. Monte Carlo frequencies have no additional endpoint
pseudocount, separating sample count from smoothing. Original transition-table
and direct-count unit priors remain explicit. This does not capacity-match the
different factorizations or establish historical process recovery.

Store full forecasts, visitation, selected paths, model counts, native-weighted
log loss and squared error. Time each arm's query separately, along with new fits
and total native cost. Analytic operation counts remain separate from hardware
timing. Report conditional paired lineage uncertainty, both training draws and
all strata without selecting a winner. Four CPU hours, including both full replays
and review, compete inside the F-family/global ceilings. No new tiny fit is used.

## Built conditional branch and remaining decisions

The changed-tool F2 handler and complete fixture are also implemented. It retains
all eight F1 arms, adds nearest-query retrieval and a true-rule oracle, and applies
unchanged saved F1 models to original versus presentation-tool populations. It
records hard-zero wrong-law loss explicitly and separates changed/stay queries.
It remains unadmitted: F1 did not establish the selective learned advantage named
by the transfer trigger. Its tests run now; its scientific population does not.

A3 remains unpromoted after the learned portfolio comparison; C2 remains
unadmitted after both allowed access sites failed to establish joint selectivity.
No further site or architecture search is authorized by this coverage pass.
Primary confirmation and sensitivity decisions follow the current joint readout.
Broader optional distribution/routine/mixture questions remain explicit frontier
items; they are not evidence of completed tests. Preserve the immutable interim,
final and CPU ceilings, and continue meaningful branch reviews at event boundaries.

The standalone portable replay entry point lacked dispatch entries for several
newer handlers although extracted-source native replays had run. This pass adds
those entries and a parity regression; it does not relabel the earlier native
replay evidence as execution of the standalone entry point.

The first executed regression barrier passed all 116 Ghost controls but failed
during Torch test collection: the launcher and imported scientific module both
called the one-time interop-thread initializer. Its failed receipt, source and
child charge remain retained; no successor was admitted. A new source revision
imports the scientific module as the sole thread initializer and verifies both
thread counts are one. The recovery repeats the full frozen test inventory,
including the newly added independent joint-readout controls, before releasing
the same two predeclared successor designs. No scientific threshold, model
setting, population or old result changes.

## Executed recovery review

The replacement regression completed all 31 frozen test files: 120 Ghost
controls and five installed-Torch controls passed, with no skips. Its 626 retained
output identities, logs, source archive and 406 source files verify. Native charge
retains both the failed first execution and the successful recovery, including
the waited-for Torch children. The failed barrier admitted no successors.

The successful barrier released exactly the two designs above and their four
replays. Every successor source and input pin verifies; the designs match the
failed barrier byte for byte as parsed records. No protected lineage or new tiny
setting was consumed. Regression success admits execution; each scientific
result still requires independent numerical verification and documentary review.
[Engineering evidence](../../../results/v19/G19-validation-all-4/README.md).
