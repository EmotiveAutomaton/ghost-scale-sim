# Unbinned absolute residual: prepared independent alternative

How much frame-level probability error can remain hidden even by twenty bins?
This unimplemented, unadmitted D-family diagnostic compares absolute residual
before versus after bin averaging. It is distinct from squared-error decomposition
and requires no successful preceding result.

Reuse the accepted goal-decision forecasts and the complete sixteen-law, two-draw,
five-seed, four-budget, four-readout, two-forecast, three-position, three-goal
roster. Keep native and equal-frame weights separate. Compute the weighted mean
absolute forecast-minus-native probability for each stratum and subtract the
frozen five-, ten- and twenty-bin absolute discrepancies. The triangle inequality
makes each difference nonnegative up to the frozen -1e-12 roundoff tolerance.
This provides an aggregation ceiling: it neither identifies a useful adaptive
partition nor improves any reader forecast.

Retain per-frame signed residuals and absolute weighted contributions as evaluator
evidence, separate from unchanged public reader packets. Validate constant signed
error, opposing errors sharing a bin, native self, zero weights, permutations and
all bin boundaries. Independently reconstruct the scalar absolute sums, previously
accepted bin discrepancies, complete roster and parent identities. Require both
full replays and a third regroup before numerical acceptance.

Freeze seed 191021 and 10,000 paired sixteen-law resamples; both draws and five
seeds remain within each law. Report all budgets and normalized log-budget areas,
with no result-selected threshold, bin rule, new fit, sample or protected lineage.
An inclusive 1,800 CPU-second D card must cover tests, failures, benchmark,
execution, replays, verification and export before admission; no family extension
is implied. This constructed-method estimand says nothing about historical goal
correspondence or human intent. Retrospective updating is independently prepared.
