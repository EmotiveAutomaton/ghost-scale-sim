# Independent unknown-time/type filtering review

Does accounting for an unspecified change preserve useful current-state information?
Before accepting numerical differences, reconstruct the frozen five-arm comparison
independently of its filtering, stream generation, endpoint-law and score routines.
Original, adjacent and extracted-source identity remains a separate requirement.

The checker consumes all completed native paths and every saved stream, float64
joint array, hypothesis/row mapping, current-state posterior, forecast and score.
The separate execution ruler validates native operations and probabilities; stable
summation reconstructs endpoint laws. Original uniforms reconstruct the observation
roster, context rotation and source-time identity, including repeated observations.
Purpose and skill complements remain separate. No fitted setting, new seed or
reserved lineage is used.

Explicit hypotheses retain initial maker, factor and every admitted time. Scaled
likelihood products check the producer's logarithmic accumulation. Half the prior
mass belongs to no change; change mass divides uniformly over admitted factors,
times and initial makers. An unelapsed possible change never remaps current state.
Repeated sources count once at their original time; conflicting identities fail.
The static and last-sixteen-source arms retain their exact definitions. Every joint
row must be consumed exactly once, with no extra arrays or unbound hypothesis rows.

Reconstruct endpoint logarithmic loss, current-state logarithmic loss and probability,
90% credible-set inclusion and size, and all type posterior masses. Preserve the
1e-300 reporting floor, 1e-14 boundary ties and 1e-10 numerical agreement tolerance.
These numerical tolerances are separate from the 0.02-nat practical endpoint margin.
Reader packets must match exactly the observed contexts/endpoints/source identities.
Hidden assignments, hypotheses, scores and truth remain evaluator material.

All sixteen makers are equally weighted within each observation draw. Average the
two draws within each of eight coefficient lineages. Retain length, possible factor,
actual switch, source condition and checkpoint separately. Report all ten paired
method contrasts over five metrics using 10,000 paired lineage resamples at seed
190501. Identical no-change streams for the two factors are paired controls, not
extra independent observations. Type uncertainty reflects the supplied hypothesis
family; every arm still receives the exact law and allowed horizon/change family.
All actual switches occur at the midpoint. Duplicated streams contain fewer distinct
sources. Fresh-episode prediction does not establish historical local-process recovery.

Controls cover direct products, time/type prior allocation, future remapping, both
factor involutions, uniform-law nulls, repeated and conflicting sources, empty
support, binary mappings, full native fixtures and all population denominators.
Source, complete inputs and environment freeze after isolated validation. The existing
serial queue owns execution; review is charged to the original four-hour card.

The independent prepared alternatives remain [changed-tool support transfer](ROUTINE_REVISION_PROTOCOL.md)
and [fixed-budget support mixtures](SUPPORT_MIX_PROTOCOL.md). Neither needs this
comparison to win. The active week and protected confirmation allocation remain open.
