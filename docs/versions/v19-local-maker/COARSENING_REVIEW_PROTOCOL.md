# Independent fixed-report reconstruction

Reconstruct the frozen endpoint and three binary report comparisons without
importing the producer's content matrix, evaluation, summary or sparse transition
kernel. Enumerate each report's endpoint members. Sum source-wise Bayes factors,
normalizers and posterior numerators with the supplied equal source prior.
Independent bit schedules and indexed hypothesis accumulation reconstruct every
future time, context and endpoint. Explicit expected losses against all eight
one-hot outcomes independently check future loss reduction. Keep conditional
source entropy separate from future information.

Verify every retained raw coordinate, exact support mask and undefined-value mask
at the unchanged absolute 1e-12 tolerance. Reconstruct each summary and all paired
strata: 128 Cartesian identities per draw, two draws within each of eight laws,
with evidence and checkpoint cells separate. Preserve 48 unavailable strata.
Check all immutable inputs, timing coverage and complete raw-file coverage.
An independent regroup of original summary rows is still required after this
checker completes; execution and replay alone grant no numerical acceptance.

Before native dispatch require sparse/disjoint/constant laws, single-source and
permutation cases, corruption of every raw field, undefined-mask corruption,
complete integration and input/roster/timing/summary corruption controls. Positive
and placebo fixtures distinguish retained and discarded informative bits and
certain-copy state independence. Time complete synthetic archived batches across
all seven structural cells and both source densities. Include checks, failures,
all three producer executions, reconstruction, regroup and operations in the
existing 3,600 CPU-second card cap and B/global allocations. No fit, observation,
protected lineage or reader input is added. Two-report dependence and report
context disclosure remain separate prepared alternatives.
