# Full-simplex retrospective update certificate: executable contract

Does preserving every remaining maker-state schedule also preserve conditioning
on a new report about an earlier state? This implements the prepared alternative
on all eight development laws, horizons 32/128 and checkpoints 8/16/17/20/32.
These are full-simplex witnesses; missing retained 128-horizon posteriors are not
regenerated and no reachability claim is made.

Within every future-schedule group, enumerate every unordered hypothesis pair
that differs at any time from 1 through the checkpoint. For each pair enumerate
every third hypothesis outside its group, every past time, all four contexts and
eight possible endpoints. Compare half-mass pair-member/third-member mixtures
before and after the same separately sourced report. Include times at which the
pair has equal likelihoods. Preserve impossible reports for either/both mixtures.

Retain the complete pair identities, pair-to-class memberships and multiplicities,
third-hypothesis identities and finer past-state classes. Factor the Cartesian
product only by exact past states and future-state sets. The merged group is
stationary on this single-change roster; refuse any violation of that premise.
For report likelihoods a,b,z the two updated group weights are a/(a+z) and
b/(b+z). Their absolute difference measures quotient change. Multiplying it by
the largest endpoint-law difference over every remaining time/context/endpoint
gives the largest future forecast coordinate difference. Retain both weights,
report probabilities, complete schedules and laws, sufficient to reconstruct
every individual forecast; no sampled witness or selected example substitutes
for a denominator. Original memberships preserve finer time identities even when
the maximum can share a future-state set.

Report counts of valid and impossible comparisons, nonzero and greater-than-1e-12
update/forecast witnesses, and maxima separately for every law and checkpoint.
Exact means binary64 nonzero under this factored formula, not exact rational
arithmetic. There is no stochastic sampling interval for this exhaustive finite
certificate. Equal law summaries are descriptive and are not reachable-population
estimates. Source-copied observations carry no new information; this card's
likelihood report is explicitly separately sourced. Reachable retrospective
updating retains its separate protocol and prior-conditional rival.

Before dispatch require full synthetic handler execution; scalar exhaustive
conditioning and forecast comparisons; uniform and stationary identities;
same/future-time identity; impossible/unequal-weight controls; complete pair and
third multiplicities; and deliberately corrupt mappings and inputs. Freeze a
full-roster synthetic timing bound covering three executions, independent review,
third regroup and exports within the existing 3,600-second inclusive card cap.
No cap increase, fit, sampled substitute or protected lineage is admitted.

Original, adjacent and extracted-source complete replays are required. Later
numerical acceptance requires independent enumeration and scalar reconstruction
of the lossless factors and summaries; replay alone is insufficient. All laws,
mixtures and kernels remain evaluator evidence. This is a constructed-method
certificate and cannot establish historical process correspondence or human intent.
