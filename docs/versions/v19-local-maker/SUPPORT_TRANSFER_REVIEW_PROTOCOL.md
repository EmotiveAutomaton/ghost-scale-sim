# Independent changed-tool support-transfer review

Freeze a separate verifier before accepting numerical transfer differences. Use
the completed source-bound outputs and both saved training episode selections.
Reconstruct count tables from ordered episodes, including the composition holdout,
and use the previously validated independent native executor and explicit sum over
all intermediate artifact paths. Do not import producer forecast/scoring routines.

Check all original and changed-rule native paths and probability weights, all
supplied queries, uniforms, seven frozen forecasts and the separate true-law oracle.
Rebuild original-law support, changed/stay targets, every raw scored row and every
conditional stratum, including empty denominators. Reproduce the complete parent
original-law aggregates. Counts copied from training are checked against episodes;
original endpoints remain identity controls, not additional independent evidence.

Retain logarithmic loss, squared probability error and true-endpoint probability.
Report each method minus direct prediction and each changed-rule minus original-rule
contrast across all declared support/change strata and both training modes. Average
the two saved draws within coefficient lineage; retain draw means separately and
use 10,000 paired lineage resamples with seed 190501. Record the actual contributing
lineages and undefined empty-stratum estimates. Cross-rule contrasts include changed
native population weights. The fixed 0.02-nat practical margin is unchanged.

Controls include changed and undo/stay endpoints, common smoothing and its finite
wrong-law penalty, uniform propagation, support/holdout identity, empty denominators,
paired regrouping, a complete two-rule native fixture and deliberate forecast
corruption. Source, plans, inputs and environment are frozen before dispatch through
the existing single-worker queue. Verification consumes the transfer card's existing
four-hour cap. No new fit, seed, population or protected lineage is introduced.

Replay agreement establishes reproducibility, while this checker tests independent
numerical reconstruction. Final event review must recombine the paired estimates
and classify the supported scope. Supplied skill, belief and operation queries make
this a forward-prediction comparison. Neither predictive accuracy nor access to the
exact law establishes historical process correspondence or human intent.

Two independent alternatives remain prepared in SUPPORT_MIX_PROTOCOL.md and the
off-midpoint change design in SUPPORT_TRANSFER_PROTOCOL.md. Their handlers and
source-bound admissions are still required; neither depends on a transfer win.
