# Squared probability error: prepared independent alternative

Does a small binned mean error conceal variation of forecast errors inside bins?
This unimplemented, unadmitted D-family diagnostic is independent of the sign of
the fixed-bin comparison. Reuse accepted frozen goal-decision forecasts and all
sixteen development laws, two training draws, five seeds, four budgets, four
readouts, two forecasts, three positions and three goals. Keep native and
equal-frame populations separate. No new fits, samples or protected lineages.

Use the original ten integer-tenth bins. For residual e = forecast minus native
goal probability, retain population mass and the weighted first and second
residual moments in every bin. The exact identity decomposes mean squared error
into the weighted squared conditional mean residual plus the weighted residual
variance within bins. Empty bins contribute zero mass and retain undefined means.
Both terms are nonnegative up to recorded roundoff. This distinguishes stable
directional error from cancellation inside bins; it is an evaluator diagnostic,
not a calibrator available to a reader.

Freeze seed 191020 and 10,000 paired sixteen-law resamples with both draws and
five seeds retained inside each law. Report both terms, their total, all budget
contrasts and normalized log-budget areas; no effect-selected bins or practical
threshold. Controls: constant signed residual, opposing equal residual, native
self, empty and boundary bins, permutations, mass conservation, and an independent
pairwise within-bin variance identity. Reconstruct all arrays and the unchanged
parent unbinned squared error; require both complete replays and a third regroup.

An inclusive 1,800 CPU-second D card must cover executable controls, failures,
synthetic timing, original, replays and verification before admission. Preserve
source and role-separated exports. Retrospective updating remains the independent
B-family alternative. No new family allowance follows. This concerns a constructed
calibration method, not historical goal correspondence or human intent.
