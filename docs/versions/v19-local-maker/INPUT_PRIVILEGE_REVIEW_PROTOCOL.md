# Independent hidden-metadata review

Reconstruct every public equivalence group using visible-field tuples, all legal
hidden skill/belief completions, endpoints under the separately validated native
executor, and native query masses by scalar summation. The checker imports no
metadata producer functions. Preserve legal ambiguity separately from ambiguity
with positive native support, and count zero-mass groups and their explicit
uniform fallback. Rebuild every forecast, group distribution, entropy, proper
score and summary denominator. Check reader allowlists and complete membership.
The parent policy probabilities retain their separately verified provenance.

For each tool, projection, weighting and algorithm, report means and paired
omission-minus-full and native-minus-uniform contrasts. Use 10,000 paired
bootstrap resamples of the eight existing development laws, seed 190974.
Native and equal-query populations remain separate. Infinite-loss mass and finite
loss contributions are distinct; never present the latter as total loss when
the former is positive. There are no model fits or additional random worlds.
These intervals condition on the fixed roster and supplied laws.

Admission requires live ambiguity, constant-output null, full-metadata identity,
zero-native-support and zero-group-mass fixtures; a complete small producer run;
and deliberate corruption of distributions, targets, masses, membership, reader
roles and all denominator levels. Runtime and portable replay dispatch must pass
in an isolated source copy. Freeze sources and every original output/input before
the existing serial queue executes the checker. Its charge shares the original
two-hour F-family card cap; global ceilings and protected lineages do not change.

This is independent numerical review of a constructed-method input-privilege
comparison. It neither trains a reader nor establishes historical correspondence.
Retrospective evidence after future-schedule grouping and pooled feedback
replacement remain independent prepared alternatives.
