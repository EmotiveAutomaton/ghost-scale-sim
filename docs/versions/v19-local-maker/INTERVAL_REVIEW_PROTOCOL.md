# Independent retrospective interval review

Reconstruct the four frozen copying intervals from the independently accepted
source-mixture factors. The checker imports no producer kernels. It calculates
the posterior copy fraction by scalar Bayes normalization, obtains the complementary
independent fraction, and checks every endpoint probability and exact support mask.
Its extrema use both interval endpoints; the worst discrepancy from the midpoint
uses the more distant endpoint in posterior weight, not the midpoint in weight.

The parent acceptance binds independent reconstruction of all signed future
coordinate differences from complete hypotheses and supplied laws. Each coordinate
lies on the same one-dimensional posterior segment, so its extrema and every
homogeneous discrepancy scale with the reconstructed weight difference. Direct
synthetic hypothesis Bayes calculations must independently check all future
coordinates and interior copying probabilities before dispatch. Arbitrary
rectangular combinations of coordinate extrema are not jointly compatible.

Verify all original sources, inputs, archive members and both full replays.
Reconstruct every interval raw factor and row, including source counts and timing
coverage. Sources average within a posterior; each observation draw contains 128
equally weighted rows, the two draws remain paired inside each of eight laws.
A separate standard-library regroup of original rows must reproduce all 896
law/evidence/checkpoint/draw/interval strata before numerical acceptance. Equal
supported-endpoint means are descriptive counting, not a probability distribution
over reports under unknown provenance. Impossible fixed-point reports stay separate.

Require constant, sparse and disjoint laws, point intervals, impossible reports,
invalid intervals, corrupted factors, missing files, source-identity and denominator
changes, and complete producer/checker integration. Full-support synthetic timing
must fit the existing inclusive 3,600-second card with a separate regroup reserve.
All failures and verification are charged. No fits, new observations, protected
lineages, outcome-driven tolerances or source changes to completed jobs.

This supplies a constructed-method certificate conditional on an exact law and
declared provenance interval. It does not learn provenance, establish historical
process correspondence or establish human intent. Two-report dependence and
unknown source identity remain independent prepared alternatives.
