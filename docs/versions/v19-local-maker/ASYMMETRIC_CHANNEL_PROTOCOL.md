# Asymmetric cue errors: prepared alternative

Does protecting against uncertain cue accuracy still help when mistakes favor a
particular wrong label? Earlier cue comparisons divide wrong-label probability
equally. Freeze 28 libraries, 15 supplied mixtures and the original byte budgets.
For source j, assign probability r to label j, (1-r)*b to label (j+1) modulo 3,
and (1-r)*(1-b) to label (j+2) modulo 3. Treat r in [1/3,1] and b in [0,1] as
unknown. Evaluate the four endpoint channels; include symmetric b=1/2 and
r=2/3 as diagnostics. Duplicated perfect-channel vertices remain explicit.

Enumerate every deterministic three-label storage policy. Minimize maximum
expected coverage regret against the informed optimum of the same unrestricted
library at each channel. A fixed policy's regret is a maximum of multiaffine
functions, so iterated endpoint maximization supplies the rectangle maximum.
Compare with the earlier symmetric-channel robust choice, the nominal symmetric
two-thirds choice and optimal fixed storage. Preserve all ties, signed differences,
realized losses, all policy scores and costs. This is a deterministic comparison;
no lottery optimizer, fitting or new observations are commissioned by this card.

Controls: known direction-dependent reversal, symmetric restriction reproduces
the accepted reference, point-channel oracle identity, identical-prior null,
all feasible policies under label permutation, dense interior diagnostic checks
of the vertex proof, zero-prior support and corrupted charges. Retain the complete
population and paired/equal-law summaries, source/input bindings, independent
reconstruction and both full replays. Require full-support synthetic timing before
admission under a prospective inclusive 1,800 CPU-second cap. Prepared,
unimplemented and unadmitted; no forecast or process-correspondence claim.
