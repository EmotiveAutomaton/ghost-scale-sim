# Independent review of coarse goal reports

Reconstruct all 307,200 learned rows, 960 native rows, 30,720 fine-parent
identities and 225,280 learned frame forecasts. All five fixed partitions, both
report choices, both policies and all three abstention costs remain required.
Neither a sample nor a favorable partition can substitute for the roster.

Integer base-three goal labels define partitions independently. Accurate scalar
sums check every saved coarse probability and marginal before exact binary64
mode and strict threshold choices. Saved fine marginals determine the mapped
fine choice. No numerical tolerance creates a tie or changes a report. This
checks arithmetic agreement, not the implementation of the numerical library.

The checker accumulates correct and compatible mass directly over fine goal
labels, without projecting native targets through the producer's coarse table.
It enumerates every partial report, including empty and incompatible reports,
and reconstructs coverage, correct/incorrect mass, both costs, incompatibility,
fine and coarse loss, and fine alternatives per opportunity and reported claim.
Zero-coverage ratios remain undefined. Pooled ratios divide pooled masses.
Broad correctness remains distinct from fine-goal recovery.

Original and reconstructed rows separately reproduce every paired estimate with
scalar means, explicit log-budget quadrature and bootstrap multiplicities:
7,680 budget contrasts, 1,920 areas and 1,920 means. Seed 191013 fixes 10,000
paired lineage resamples. Two draws and five seeds remain inside each of sixteen
development lineages; their variation is separate from conditional-fit intervals.
A subsequent independent direct-index regroup of original rows is required for
final numerical acceptance.

Before dispatch, isolated controls must cover all partitions and partial reports,
coarse-correct/fine-wrong cases, modal noncommutation, one-class certainty,
threshold equality and adjacent floats, empty coverage, complete fixture execution,
roster corruption, changed reader fields, altered probabilities/reports/scores,
native support and parent identities. Native and portable routing must pass.
Synthetic full-roster timing plus controls, failures, execution, both replays,
reconstruction and operating work stays inside the inclusive 1,800 CPU-second
D card. The existing serial queue owns execution; no new fit or sample is used.

Original native posteriors and fitted forecasts are inherited verified inputs.
This is an exploratory constructed-method reporting tradeoff under stipulated
costs, not independent regeneration of earlier fits, historical goal recovery,
human intent or a statement of user preferences. Reader evidence remains separate
from all scientific forecasts, reports and evaluator truth.
