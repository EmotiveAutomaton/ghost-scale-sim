# Independent operation and conditional-goal uncertainty review

Reconstruct all original frozen joint-process forecasts without importing the
producer's distribution, component, score or regroup functions. Enumerate each
operation marginal by label modulo 216 across the complete 5,832-label universe.
Use scalar accurate sums for native target probabilities, conditional ratios,
all three losses and entropies, and their excesses. Check all three chain identities
and the previously accepted joint losses within 1e-10. Retain every unknown label.

Verify all 10,240 strata, 145,920 frame forecasts, 912 reader packets and complete
sixteen-lineage/two-draw/five-seed/four-budget/four-tier/four-arm roster. Reproduce
all 288 budget contrasts, 72 normalized log-budget areas and 64 means. Average
draws/seeds within each lineage before 10,000 paired lineage resamples with seed
191004. Use scalar means and bootstrap multiplicities as an independent grouping;
repeat grouping on the original saved rows to isolate score reconstruction roundoff.
Retain draw and feature-seed summaries separately from conditional lineage intervals.

The native posterior masses and fitted forecasts remain previously accepted inputs.
Localization, compatibility, candidate and abstention fields are inherited unchanged
and checked for identity, not represented as newly reconstructed here. Reader inputs
retain their artifact/context/witness allowlist; all evaluator arrays remain separate.

Known answers include uniform forecasts, perfect native forecasts, same operations
with different goals, same goals with different operations, unknown-only truth,
axis reversal, invalid mass, duplicate/missing strata, a complete synthetic fixture
and corruptions of scores, entropy, inherited fields, references and input hashes.
The checker uses the existing queue, one CPU thread and below-normal priority.
All preparation, controls, original execution, both complete replays and review
share the original inclusive 1,800 CPU-second A-family card ceiling. No fit, sample,
protected lineage or confirmation is admitted. Retrospective updating and learned
joint factorization remain independent prepared alternatives. This is a constructed
method diagnostic; numerical acceptance requires a separate completion review.
