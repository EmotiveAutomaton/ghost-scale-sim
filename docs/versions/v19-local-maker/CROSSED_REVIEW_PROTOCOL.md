# Independent crossed-rule verification

The completed crossed-rule comparison needs numerical adjudication after its
complete replays. This checker adds no fit, law, training setting or sampled
population. Freeze the original plan, complete outputs and exact source before
dispatch through the existing serial queue.

Before reading scientific inputs, run known-answer entropy, empty-support,
partial-coverage, corruption, undo, unchanged-family and belief-alias controls.
The independent implementation imports no producer executor, projection,
posterior or scoring routine. It reconstructs all four transient executors,
goal/operation probabilities, 216 paths in each maker/context stratum, anonymous
projections, full joint posteriors and naturally weighted scores. Check every
saved identity and complete denominator, not a prefix.

Reaggregate paired coefficient lineages. Bootstrap 10,000 draws with seed 190501,
reporting each truth rule, evidence tier and supplied family. Positive truth mass
at a predicted zero remains infinite expected loss; its finite contribution is
never presented as an ordinary finite loss. The belief-complement alias can
preserve support while changing native weights, so missing-support improvement
is not a required result.

All work shares the existing crossed-rule four-hour card and D/global ceilings.
Reserved lineages remain untouched. Numerical acceptance and a sensitivity or
continuation decision follow the checker completion event and full write-through.

Two independent scientific alternatives remain concretely prepared for later
admission. Neither is represented as an implemented job:

- **Routine and current request:** retain the three-unit executor and native
  sixteen makers; give a fixed inspection/revision opportunity after the third
  operation. Cross truthful changed versus unchanged requested purpose and
  intact versus hidden presentation evidence. Compare the frozen native policy
  with a supplied-purpose oracle on eight original development lineages. Freeze
  revision choices and costs before execution; this is a constructed-policy
  contrast, not learned human expertise. It requires its own controls and source.
- **Transient-state ambiguity:** keep the admitted recursive updater and saved
  fits, compare fixed periodic reset with an exact two-component mixture on a
  predeclared purpose-switch history. Report posterior coverage and future loss
  separately, with duplicate-source controls and explicit oracle-law privilege.
  A precise switch schedule, representation/update budget and source still need
  admission; no additional tiny-model setting is available.

The independently admitted support/holdout experiment has its own completion
events. It need not await a crossed-rule or primary-learning win.
