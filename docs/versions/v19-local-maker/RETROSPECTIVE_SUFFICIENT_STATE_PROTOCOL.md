# Joint retrospective likelihood state: prepared independent alternative

How much additional state is sufficient to update a future-schedule quotient
exactly after any one separately sourced report about the past? This B-family
alternative is prepared but unimplemented. It can run independently of whether
the full-simplex witness or prior-conditional reachable rival finds a discrepancy.

Use only the accepted unknown-time/type and source-omitted posterior rosters,
all eight laws and both observation draws, with the available checkpoints from
the accepted future quotient. Do not regenerate missing 128-horizon checkpoints.
For every retained posterior, store the joint mass of future group and maker
state at each earlier report time. These joint tables, together with the supplied
endpoint law, exactly determine each group's report likelihood numerator.
Normalize across groups to obtain the updated quotient, preserving impossible
reports explicitly. This representation retains more information than quotient
weights alone and must be labelled accordingly.

Compare complete hypothesis weights, future-group weights alone, and the joint
report-state tables. Retain sparse support maps and all zero masses. Charge
float64 values, shared integer indices and schedules, construction work, and each
report query separately. Reuse only identical structural support; do not assume
numeric equality between different posteriors. Report storage and query costs
alongside exact update and remaining-time endpoint forecast reconstruction.
Do not call the representation minimal without an independent lower bound.

Freeze the full structural roster, all report times, four contexts and eight
endpoints before outcomes. Validate on direct full-hypothesis Bayes conditioning,
uniform laws, stationary schedules, same-source copied-report identity, impossible
reports, and deliberately dropped group/state cells. Independently reconstruct
all law/checkpoint/source strata and keep both draws paired. This is a supplied-law
sufficiency and cost test, not learned access or historical process correspondence.

Require complete synthetic timing, source admission, original and both full
replays and independent numerical review within a 3,600-second inclusive cap,
competing within existing B-family and global limits. No fit, sampled replacement,
protected lineage, new service, or automatic expenditure extension is admitted.
