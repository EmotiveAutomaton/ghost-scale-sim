# Frozen temporal dependence: admission contract

Does dependence across steps improve a frozen learned process forecast beyond
three within-step goal/operation marginals? Keep the original joint-readout roster:
four evidence tiers, budgets 32/128/512/2048, sixteen development laws, two training
draws, five feature seeds and raw-history, frozen-latent, predictive-bank and
matched-frequency arms. No fitting, sampling or protected lineage is admitted.

Expand every unknown tail over the full 5,832-label universe. Decode three base-3
goals and three base-6 operations, pairing corresponding steps into eighteen-way
symbols. Sum the three step marginals and multiply them over time. Preserve
within-step dependence while removing all between-step dependence. Do not mask
impossible combinations with native truth or public mechanics. Native posterior
products remain separate evaluator-only rulers.

Reproduce every original metric. Preserve logarithmic/squared loss, native excess,
compatible mass, candidate coverage and size, unsupported modal claims, confidence,
abstention, unknown mass and all per-step goal/operation accuracies. Alphabet-first
tie priority remains for learned arms; native rulers use ascending full-label order.
The nominal 90% candidate set ends at its first cumulative crossing. Save every
three-by-eighteen marginal array, inherited forecast, native target and raw score.

The diagnostic contrast is product minus original logarithmic loss, separately
by tier, arm and budget. Freeze 10,000 paired sixteen-lineage bootstrap resamples,
seed 191006, averaging both draws and five seeds within each lineage. Preserve
per-budget estimates, normalized area over log budget, all metric means and
separate draw/seed variation. Conditional lineage intervals are not universal
training-population intervals. This comparison does not replace G-P1.

Controls precede outcomes: correlated versus independent temporal fixtures,
identical step marginals with different paths, scalar label-axis reconstruction,
within-step dependence preservation, incompatible cross paths, unknown-only and
point-mass forecasts, zeros, malformed distributions, complete synthetic execution,
and deliberate score, reference, reader-role, input-hash and row-roster corruption.
Independent numerical review must reconstruct every marginal and product by scalar
label enumeration, every score and paired regroup, using saved binary64 products
after marginal verification to preserve executed ties. Both complete replays and
independent reconstruction are required before scientific acceptance.

The inclusive A-family cap is 1,800 CPU seconds for preparation, tests, failures,
original execution, adjacent/extracted-source replays and independent review.
Synthetic timing must leave adequate review allowance. Retrospective updating
and within-step factorization remain prepared independent alternatives. No new
training setting, family extension, confirmation or service owner is authorized.
