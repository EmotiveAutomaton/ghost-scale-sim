# Bounded changed-primitive feedback: prepared independent alternative

Can direct evidence about a changed tool repair the frozen forward model's
changed-endpoint failure without damaging unchanged queries? The F-family
handler is implemented; isolated validation and source admission precede dispatch.

Retain both saved count-table draws and original/composition-holdout modes, all
640 queries, original evaluation lineages and final 1/32 smoothing. Freeze a
balanced enumeration of unique skill/belief/current/undo tool inputs before
execution. Supply deterministic next-artifact feedback on nested sets of
0/4/16/64 inputs, removing repeated inputs. This changes observed evidence.

Use three declared updates: unchanged counts; one added count per observed
transition; and replacement of only the observed primitive row by its empirical
conditional distribution. Preserve the saved unit pseudocount once in the additive arm;
replacement uses the same final forecast smoothing as every other arm. Freeze
lexicographic mechanics ordering, with reversed order as a separate sensitivity
condition, never a selected winner. These are deterministic feedback sets, not
new episode seeds or untouched confirmation. The roster and independence of
each distinct mechanic input must be checked before admission.

Report all methods/budgets, changed/stay groups and original support groups,
with denominators and exact scores. Pair with saved zero-feedback predictions;
retain native population weights and equal weights over the fixed query roster.
Separate new-semantics effects from weight shifts. The loss margin is 0.02 nats.
Lookup-table updates do not imply interpolation to unobserved primitive rows.

Controls: zero-feedback identity; no writes outside supplied rows; one prior;
source deduplication; a known tool; undo/stay queries; equally sized wrong feedback;
and independent path-sum reconstruction. Export feedback inputs and observed
next artifacts separately from rule identity, query targets, weights and scores.
Forward accuracy does not establish historical process or human intent.

Cap: four CPU hours including tests, runs, replays and review within F/global
limits. No tiny setting or protected lineage. Freeze source, exact feedback roster,
update capacity and comparisons before execution. This remains independent of
off-midpoint filtering and practice-support mixtures; no preceding branch must win.

## Pre-dispatch implementation reconciliation

The prepared wording specified a half pseudocount, but both retained count-table
fits use one count per outcome before training. Preserve that existing prior;
do not rebase it or add another prior. This correction precedes new feedback
outcomes and retains the originally required zero-feedback identity. The prior
draft remains in version history. The addition arm increments one observed
outcome by exactly one; row replacement discards only that observed row's old
counts and substitutes the deterministic empirical distribution.

The legal tool-input roster has 128 distinct rows: skill is necessarily one,
belief has two states, and current and undo artifacts each have eight states.
Enumerate current/undo pairs lexicographically, alternating the two beliefs
within each pair. Reverse the complete roster for the sensitivity condition.
Every nonzero budget balances beliefs. Repeated reports of one input count once;
conflicting reports are rejected. Distinct rows are not independent sampled
trials: undo can be irrelevant to a tool's deterministic output. No semantic
pooling to unobserved rows is performed.

Cross both original and changed tools with truthful feedback and a fixed wrong
feedback control that flips the last artifact bit. Score every combination of
two retained draws, two support modes, two tools, two orders, two feedback
conditions, four budgets and three updates. All 640 queries stay paired. Reuse
the original eight evaluation lineages and their saved trajectory probabilities;
native and equal-query weights remain separate. No new trajectories or fits.

Independently sum all 512 intermediate artifact paths for every saved forecast;
compare with state propagation at absolute tolerance 1e-12. Preserve updated
tables, predictions, all per-query proper scores, native masses, support labels
and complete denominators. Teacher access remains supplied skill, belief and
operation metadata. The primitive feedback has only input and observed outcome
fields; anonymous content hashes and exact budget slices prevent rule labels or
unused observations from entering reader inputs. Pairing and rule maps stay in
the evaluator export. Numerical acceptance still requires independent review
and both complete replays.

Two independent prepared alternatives remain: the
[retrospective quotient test](RETROSPECTIVE_QUOTIENT_PROTOCOL.md) and the
[primitive alias-pooling comparison](PRIMITIVE_ALIAS_POOLING_PROTOCOL.md).
