# Retained timing failure and identified measurement repair

The first synthetic admission estimate was 4,620.91 CPU seconds against the
prospective inclusive 3,600-second card, so no scientific execution was admitted.
Four individually timed rows per shape produced CPU readings quantized to
0.015625 seconds, including a zero reading for one shape. Multiplying the largest
single-row tick across the complete population overstated sub-tick work and did
not measure the producer's actual 32-row archive batches.

One identified timing repair measures three complete 32-row synthetic batches
per structural/source-count shape, including the same prefixed-array archive
serialization as the producer. Scale the maximum batch cost by the exact batch
count, retain the same 50% margin and input/independent-review/operations reserves.
Preserve the original source and failed estimate. No formula, population, source
roster, scoring tolerance or scientific outcome changes. A second failure remains
a substantive tractability blocker; it does not authorize sampling sources.

The first batch-timing rehearsal assumed that duplicate streams retained half of
their sources. The immutable bindings instead retain three quarters (rounded up).
That provisional estimate is superseded and cannot admit execution. The final
synthetic measurement uses the verified source counts for every structural cell;
all 112 binding inventories must match before dispatch. Both earlier timing
receipts and their charges remain. This corrects a population-size assumption,
not a numerical result or the scientific roster.
