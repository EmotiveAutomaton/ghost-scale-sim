# Checkpoint mixture compression: execution verified, numerical review pending

Can a smaller set of complete maker/change hypotheses preserve useful prediction and uncertainty at a fixed checkpoint? The five-capacity comparison completed 225,280 scored rows and exactly reproduced 45,056 full-mixture parent rows. Independent numerical reconstruction and complete replay acceptance remain pending; no compression benefit is accepted. This is a constructed-method execution result under supplied laws, not recursive sufficiency, historical process correspondence or human intent.

The frozen comparison retains 1, 16, 64, 256 or all complete maker/change
hypotheses from each saved checkpoint. The full roster contains 560 hypotheses
at 32 observations and 3,632 at 128. All eight development coefficient lineages,
two saved observation draws and sixteen equally weighted makers remain. Both
identity-aware and source-omitted presentations are used, with purpose/skill,
actual change/stationarity, independent/copied evidence and every original
checkpoint reported separately. No capacity was selected from outcomes. There
are no new observations, learned fits, tiny settings or protected lineages.

Original integrity verifies 488 frozen source files, 88 input files, 111
deterministic outputs, the source archive, plan and environment, and the separate
timing hash. The 225,280 rows form 14,080 maker-averaged cells. All 45,056 full
mixture rows reproduce their parent current-state distributions, forecasts and
scores exactly; the producer also checks 45,056 same-time marginal forecasts.
These are execution and apparatus identities. Their independent numerical audit
remains pending. Adjacent and extracted-source replays have separate later event
ownership; this delivered event does not adjudicate them.

The independent checker uses explicit Python probability/index ordering rather
than the producer's array sort, accurately summed state buckets and the separate
scalar forecast/score kernel. It reconstructs every retained int32 index, float64
weight, parent mapping, current distribution, forecast, score and summary cell.
The mandatory known-answer, zero-mass, tie/index, change-boundary, paired-regroup,
complete native-fixture and corruption tests run first in the existing serial
worker. Source and syntax inspection are complete; numerical tests have not yet
been observed to pass. Laws and parent posteriors inherit completed validation;
the new checker does not claim to revalidate their entire likelihood process.

Each retained weight is renormalized after deterministic selection; exact ties
use the original hypothesis index. The full condition preserves parent addition
order. Current-maker logarithmic loss retains the original 1e-300 floor, so zero
true-maker mass incurs a large finite penalty. Loss, true-state probability,
90% tie-inclusive set inclusion and size are separate measures. Weight/index byte
counts exclude shared roster metadata, runtime objects and initial acquisition.
The sixteen-state marginal requires 128 weight bytes and predicts same-time
endpoints exactly. That identity says nothing about future-change sufficiency.

Numerical review will average makers within draw, then both draws within each
coefficient lineage. Every capacity is paired against all, and omitted against
aware at matching capacity. All 880 mean strata and 11,880 metric contrasts remain,
with both draw means, paired lineage values and 10,000 eight-lineage resamples,
seed 190501. There are nine reported measures including storage/removal counts;
the 0.02-nat practical margin applies only to logarithmic losses. Reused streams,
stationary aliases and deterministic replays do not add independent replication.

Reader inputs are the two unchanged bound parent exports; no new reader packet
is claimed. Scientific core and eight lineage archives preserve plans, source,
parent receipts, observations, retained indices/weights, forecasts, scores and
maps. Sixteen large parent joint arrays remain in the retained evaluator capsule
with exact hashes and byte lengths in EVALUATOR_MANIFEST.json. They are not
embedded in the public archives. Every exported member is byte-checked. Evaluator
laws, true makers and hypotheses remain separate from reader inputs.

**Warrant:** execution bindings and internal identities verified; numerical
adjudication and complete replay acceptance pending. No benefit, harm, equivalence,
calibration or recursive-memory claim is accepted. Miniature — architecture
untested. **Pursuit:** finish independent reconstruction and retain all fixed
capacity contrasts. Primitive feedback and the finite current-marginal transition
diagnostic remain independent prepared alternatives, each requiring its own
implementation and admission. Neither needs this compression comparison to win.
The research week, its reporting times and protected confirmation reserve remain.

[Experiment](MIXTURE_COMPRESSION_PROTOCOL.md),
[independent review](MIXTURE_COMPRESSION_REVIEW_PROTOCOL.md),
[primitive feedback](PRIMITIVE_FEEDBACK_PROTOCOL.md),
[marginal transitions](MARGINAL_TRANSITION_PROTOCOL.md),
[evidence roles](../../../results/v19/G19-B-mixture-compression-1/README.md).

The first independent checker admission was withdrawn before numerical execution after static review found a NumPy boolean in a JSON control verdict. Its source and plan remain retained. A separately frozen replacement converts the verdict to a native boolean and adds a serialization regression check; scientific selection, projection, scoring and population definitions are unchanged. All numerical fixtures remain mandatory before reconstruction.
