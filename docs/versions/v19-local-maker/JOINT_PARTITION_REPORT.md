# Joint versus marginal forecasts: execution and replay verified

We tested whether full local-goal forecasts distinguish native dependencies that their marginals merge. All 366 deterministic outputs match the original and both complete replays. Independent numerical acceptance remains pending. This is a constructed-method diagnostic; historical process correspondence and human intent remain unestablished.

The complete 27-outcome distribution over three local goals defines one exact
binary64 partition of the 704 public frames. The nine marginal probabilities
define another. Signed zeros compare equal; a one-bit difference stays distinct.
Keep every setting's membership and report any numerical refinement exception.
The comparison includes sixteen development laws, two retained training draws,
five paired seeds, four budgets, four readouts, and two forecast constructions.
Native and equal-frame populations remain separate: 320 settings and 1,280
logical group bundles. No new fit, sample or protected lineage was used.

The diagnostic measures the expected summed squared distance between each
native joint target and its partition group's pooled target. It separates
variation among frames within a law from variation of law-specific group means.
Pooled target means are evaluator references, never reader inputs or a trained
decoder. Exact storage reuse retains a binding for every logical setting.

All 366 deterministic outputs match the original, adjacent and extracted-source
executions. All 762 frozen source files, 327 input files, source archives, plans,
environment fingerprints, outputs and separate timing bindings verify.
Execution and replay acceptance are complete; numerical acceptance remains open,
so the accepted scientific batch count stays 73. No advantage is accepted here.

The independent checker passes 57 isolated controls, with no skips or warnings.
They include complete synthetic execution, equal marginals with different joint
dependence, identical targets, zero and unequal weights, every small law
multiplicity, law/frame permutations, one-bit distinctions and deliberate
corruption of memberships, masses, sums, squares, centered variation, pair
coefficients, means, values, cache bindings, parents, reader fields and summaries.

The checker independently sorts equality classes and reconstructs scalar moments
for all 27 native target coordinates. Expanded squares check pair-distance
coefficients. Every setting's binding must agree with its original forecast;
sharing arrays never removes a setting. All 5,120 parent losses must reproduce.
Original and independently reconstructed moments are resampled separately using
10,000 paired sixteen-law multiplicities with the frozen seed 191023. The two
draws and five seeds remain paired. A separate summary implementation checks
384 estimates, 192 joint-minus-marginal budget contrasts and 48 normalized areas
over log training budget. A third event-owned regroup remains required.

Full-roster synthetic timing, including singleton, paired and all-frame groups,
both populations and no cache reuse, gives a conservative inclusive estimate of
1512.39 CPU seconds against the unchanged
1,800-second cap. It includes 990.00
seconds for checking, 225.00 for the third
regroup and 120 for remaining operations. Tests and prior execution are charged.
The existing serial supervisor dispatched the frozen checker, and native
BelowNormal priority was externally observed. Its completion/failure event owns
numerical review; no second scientific service was created.

Public reader packets, inherited scientific inputs and scientific outputs have
separate verified archives. Original and replay source snapshots stay unchanged.
The paired intervals condition on retained fits, with no practical-impact
threshold or independent confirmation. This constructed-method diagnostic is
miniature — architecture untested beyond its declared roster. It establishes
neither historical process correspondence nor human intent. Retrospective
full-simplex and reachable-posterior updating remain prepared but unimplemented
independent alternatives. No forest exhaustion or campaign completion is claimed.

[Execution contract](JOINT_FORECAST_PARTITION_ADMISSION.md).
[Independent review](JOINT_PARTITION_REVIEW_PROTOCOL.md).
[Execution evidence](../../../results/v19/G19-D-joint-forecast-partition-1/EXECUTION_REVIEW.json).
[Evidence roles](../../../results/v19/G19-D-joint-forecast-partition-1/EXECUTION_EVIDENCE_ROLES.json).
