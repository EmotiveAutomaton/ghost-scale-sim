# Misstated reply reliability: fixed implementation

Can a reader benefit from a noisy mechanical reply when its stated accuracy is
wrong? Keep the retained 640 queries, eight development laws, both tool rules,
native and uniform legal conditional laws, and native and equal-query scoring
populations. Request skill or belief once, separately. Cross actual accuracies
0.5, 0.75 and 1 with assumed accuracies 0.5, 0.75 and 1, enumerating both replies.
No request and correctly calibrated conditioning are fixed references. Do not
select the field or the assumed accuracy from truth or observed losses.

Retain actual reply probabilities separately from the assumed likelihoods,
all forecast vectors, zero-modeled-mass fallbacks, endpoint proper scores,
infinite-loss mass, ambiguity and request counts. Charge prices 0, 0.02 and
0.1 nats per return. The reader receives visible artifacts and operations,
requested field, reported bit and stated accuracy only. Actual accuracy,
hidden fields, law weights, endpoint truth and scores remain evaluator-only.

Before any execution freeze a handler, complete enumeration and source after
known scalar odds, half-channel identity, truthful-parent identity, explicit
zero-support/infinity and corruption controls. Require both complete replays
and independent scalar reconstruction before numerical acceptance. Paired law
intervals condition on the fixed roster; no fitting, new episode or protected
lineage is allowed. This is a supplied-law calibration method, not evidence
about human testimony, historical process correspondence or learned access.

The handler is implemented; source admission and dispatch are separate records.
Its half-hour CPU ceiling
includes tests, science, replays, review and failures within F and the global
allocation. It is independent of joint-field dependence and retrospective
quotient updating; neither must win to release it. A completed calibrated
diagonal is an identity reference, never a new replicated discovery.

The fixed implementation retains 5,184 score cells: eight laws, two tool rules,
two conditional laws, three actual accuracies, three assumed accuracies, three
request choices, two scoring populations and three prices. No-request rows are
identity references across actual and assumed accuracy, not new independent
evidence. Each of 96 forecast files stores both replies and all three actual
probability arrays. Only the assumed accuracy enters a forecast; the actual
accuracy enters evaluator reply probabilities and expected scores. The existing
validated binary channel and realized-reply scoring primitives are reused.
The schedule uses complete legal groups with no outcome-selected query or field.

Eleven isolated controls pass: scalar Bayes odds across all fixed accuracies,
half-channel identity, actual/assumed separation, truthful conditional identity,
both zero-support fallbacks, explicit infinite loss, a complete small fixture
checked against separate scalar posteriors, costs, reader allowlists, corrupted
inputs/design/probabilities, native runtime and standalone replay dispatch.
They do not replace independent reconstruction of the scientific population.
Architecture is a complete finite Bayes table with eight endpoint categories;
capacity is the existing legal completion roster. There are no fitted teacher
labels, optimizer choices or training draws. Endpoint truth is evaluator-only.

Independent review will retain actual accuracy, assumed accuracy, request, tool
rule, conditional law, population and price as separate axes. Freeze 10,000
paired eight-law resamples with seed 190981. Report means, differences from no
request, differences from the calibrated diagonal and from ignoring the reply
(assumed accuracy 0.5). Reconstruct all estimates again from original cells before
numerical acceptance. Retrospective evidence updating and the discrete uncertain-
reliability-set protocol remain prepared alternatives, independent of this sign.
