# Where retention errors occur in the future: prepared alternative

Does averaging the whole future hide different effects of retaining recent versus
evenly spaced sources on immediate and later forecasts? The verified time-retention
result reports whole-future mean loss and a maximum. It does not locate the error
in time. This branch changes the explanatory question without adding observations,
fits, laws or source rosters, and is independent of the nonuniform-source comparison.

Freeze three disjoint spans of the existing future schedule: the current checkpoint
alone, the first ceil((length-checkpoint)/2) strictly future steps, and the remaining
strictly future steps. Empty spans are explicitly unavailable, never zero-valued
accuracy observations. Keep the original whole-future result as a reproduction
check. All existing future times remain covered exactly once by the three spans.

Use all saved posteriors, eight laws, two evidence conditions, both draws and the
seven available horizon/checkpoint cells, all source identities, five copying
probabilities and every endpoint/context. Compare full, recent half/quarter and
spaced half/quarter retention under the existing uniform source law and original
uniform-maker-prior remainder. Preserve unsupported mass and undefined errors.
Retain each span's expected squared regret and maximum future probability error;
separate averaging over reports from averaging over time. Contrast spacing minus
recency within every cell, with both draws inside each equally weighted law.

Prepared only. Require scalar Bayes/one-hot controls, exact span coverage and
weighted recombination of full-horizon regret, empty/one-step horizons, source
permutation and constant/full/copy identities. Freeze source, input and environment
after complete-support synthetic timing including serialization. The prospective
inclusive cap is 3,600 CPU seconds for checks, failures, three executions,
independent reconstruction and original-row regroup within family/global ceilings.
No new fit, observation, protected lineage or sampling to fit the cap. No practical
margin or confirmation interval is frozen. This is a supplied-law constructed
method; no learned provenance, historical correspondence or human intent follows.
