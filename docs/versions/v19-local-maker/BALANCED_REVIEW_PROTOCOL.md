# Independent context-balanced retention reconstruction

Independently rank sources by within-context recency rank and then the declared
context order. Select the first matched number. This total ordering checks the
producer's round-robin queues without importing its selection function. Preserve
timestamps under source permutation; endpoints never affect selection. Check both
fixed context orders, exhausted contexts, single-context identity and empty rosters.

Reconstruct source-wise Bayes factors and bit schedules independently. Selected
sources contribute joint past-state/future-group numerators; omitted sources
contribute the original uniform sixteen-maker prior likelihood times group mass.
All original source probabilities and copy counts remain. Compute group/report
numerators directly and propagate to every future time, context and endpoint.
No producer evaluator, summarizer, transition matrix or direction kernel is used.

Explicit expected one-hot squared losses check regret against full retention.
Verify all report probabilities, exact support and undefined masks, future maximum
errors, group total variation, selected identities, remainder mass and histograms.
Recount joint structural cells with sets, distinguishing mixed from deterministic
cells that reuse group weights. Keep float64, int32 and shared-prior costs separate.
Reference timing does not establish optimized compact-state latency.

Bind plans, source, environment, inputs, every deterministic output and separate
timings. Keep all 28,672 posteriors, 505,856 sources and 143,360 summary rows across
eight laws, both evidence conditions, two draws, seven structural cells and five
copy probabilities. All seven retention states share the eight endpoint queries.
The 48 unavailable checkpoint strata remain absent. Reconstruct all seventy metrics
and 1,120 paired draw strata, each containing 128 Cartesian identities. Separate
event review regroups ORIGINAL rows into 560 law strata and seventy population
cells, with two draws inside each equally weighted law. Retention contrasts are
paired differences, not independent replications.

Controls include dense/sparse/constant/disjoint laws, source permutation, older
informative contexts, full-retention and certain-copy identities, every raw-field
and undefined-mask corruption, input/summary/roster/timing faults, native integration
and portable dispatch. Full-support synthetic timing covers all seven structural
cells, both source densities and balanced/random/single-context rosters, including
archive decompression. The original 3,600 CPU-second inclusive cap covers previous
attempts, three executions, controls, checker and separate regroup. No new fit,
observation or protected lineage. Numerical acceptance awaits completion and regroup;
no practical margin or confirmatory precision was frozen. Scope is a supplied-law
constructed method, with no learned provenance or historical-process claim.
