# Retrospective provenance intervals: independently verified bounds

We tested whether uncertainty about copying leaves bounded future forecasts after a report about the past. With copying probability anywhere from zero to one, the mean of each source’s largest future-probability width is 0.00492–0.01370 across retained settings. Independent reconstruction, all 896 paired strata and both complete replays verify this supplied-law constructed-method result. Learned provenance, historical process correspondence and human intent remain unestablished.

V19 remains active with 78 independently accepted scientific batches. Provenance-interval bounds and all 896 paired strata are independently verified. The unknown-source comparison passed 48 isolated controls and was observed executing through the existing serial queue, with both complete replays queued. Numerical acceptance awaits its completion-event review. Two-report dependence and source-identity disclosure value remain prepared, unimplemented alternatives. The prior-conditional reachable rival remains timing-blocked.

The fixed population contains 28,672 retained posterior rows from eight development
laws, two posterior evidence conditions, seven available horizon/checkpoint cells,
two observation draws and 128 equally weighted rows per draw. Every distinct old
source is retained: 505,856 source queries, four frozen copying intervals and
eight endpoints yield 16,187,392 source/interval/endpoint queries. They are not
independent laws. All 48 unavailable checkpoint strata remain missing. No new
observations, fits, seeds or protected lineages were used.

The four supplied intervals are zero to one, one quarter to three quarters,
zero to one half, and one half to one. At the original endpoint, Bayes updating
moves along a single segment between the old posterior and its independently
updated counterpart. The independent-update weight decreases as copying becomes
more likely. At every other supported endpoint, the update is independent;
copying with probability one makes those reports impossible. That impossibility
is retained rather than assigned a forecast.

All interval endpoints, midpoint weights, report-probability bounds, support masks,
and largest and average future discrepancies are retained as lossless factors.
The independently accepted parent binds the full hypotheses, native laws and
signed differences for every future coordinate. These reconstruct every interval
coordinate bound. Individual coordinate extrema cannot be freely combined into
a jointly compatible forecast; their complete joint compatibility is one segment.
The interval midpoint in copying probability is generally not the midpoint in
posterior weight. Its worst-case error uses the more distant segment endpoint.

All 4,170 deterministic outputs match the original and both complete replays,
including the extracted-source replay. All 798 source files, 2,184 input bindings,
plans, archives, output hashes and environment fingerprints verify. The three
timing logs are separately bound. New raw factors and summaries are preserved in
checked scientific/evaluator archives, and inherited inputs are bound to checked
published archive members or files. No new reader inputs were created.

The independent scalar checker passed 77 isolated controls without skips or
warnings. It uses the posterior copy fraction to reconstruct complementary
weights, endpoint likelihoods, exact support masks, widths and midpoint worst
discrepancies. Different summation orders use the frozen absolute tolerance of
1e-12. Every retained source and summary row is reconstructed. Its use of accepted
parent directions is explicit: it checks the interval transformation, conditional
on the already independently reconstructed complete future-coordinate factors.
Direct synthetic hypothesis Bayes calculations check all coordinates and interior
copying probabilities, with constant, sparse and disjoint laws. Point intervals,
impossible reports, corrupted fields, changed sources, missing/extra raw files,
denominator changes and full producer/checker integration are exercised.

One synthetic corruption test initially mutated both reference and saved arrays
because they shared memory. Copying the saved arrays fixes the test. The failed
log, source snapshot and CPU charge remain retained; neither scientific formula
nor frozen original source changed.

The full-support synthetic estimate is 884.15
CPU seconds inclusive under the unchanged 3,600-second card cap, including
273.00 seconds for independent reconstruction,
250 for a separate regroup and 200 for operations. The existing supervisor
dispatched the checker with 801 source files and 4,174 bound inputs. Its native
BelowNormal priority was verified. Its completed reconstruction and a separate standard-library regroup of the
original summary rows now pass. The regroup imports no producer or checker
summarizer: it verifies all 128 identities per draw, 896 paired draw strata,
448 law strata and 56 population rows. Its largest discrepancy from the checker
is 1.39e-17, below the frozen absolute tolerance of 1e-12. All checker source,
input, environment, archive and output bindings verify.

Sources are averaged within each posterior first; each draw then receives equal
posterior weight, both draws remain paired inside each of eight equally weighted
laws. All 896 law/evidence/checkpoint/draw/interval strata were checked. Old-
endpoint width and equal-supported-endpoint means remain separate. Equal endpoint
weights describe counting, not a calibrated report law under unknown provenance.
Unsupported fixed-point fractions are not folded into conditional errors.
The producer timing covers interval factors, raw writes and source accumulation;
parent loading is outside that region. Native CPU charges include those costs.
Division by query count reports amortization, not single-query latency.

Two-report dependence and source-identity disclosure value remain substantive
prepared alternatives. The unknown-source comparison is now implemented and admitted. The prior-conditional reachable rival remains timing-blocked after
one identified optimization. A3/C2/F2 keep their specific triggers. The week is
active; no exhaustion is claimed. This supplied-law constructed-method result is
miniature — architecture untested beyond this roster. Learned provenance,
historical process correspondence and human intent remain outside its warrant.

[Frozen design](SOURCE_INTERVAL_ADMISSION.md).
[Independent review](INTERVAL_REVIEW_PROTOCOL.md).
[Execution evidence](../../../results/v19/G19-B-source-interval-1/EXECUTION_REVIEW.json).
[Evidence roles](../../../results/v19/G19-B-source-interval-1/EXECUTION_EVIDENCE_ROLES.json).

The table gives the range across fourteen evidence/horizon/checkpoint settings
after equal weighting of eight laws and two paired observation draws per law.
Within each posterior, distinct sources receive equal weight. Width is the gap
between compatible future probabilities; each source first contributes its
largest coordinate width. Midpoint error is its worst discrepancy when using
the midpoint of the supplied copying interval. The final column additionally
averages equally over supported endpoints and is descriptive counting, not
a calibrated distribution over reports. All values are probability units.

| Supplied copying interval | Mean largest width at the old endpoint | Mean worst midpoint discrepancy at the old endpoint | Equal-supported-endpoint mean largest width |
| --- | ---: | ---: | ---: |
| 0 to 1 | 0.004920–0.013699 | 0.004266–0.011730 | 0.000615–0.001712 |
| 0.25 to 0.75 | 0.001253–0.003782 | 0.000849–0.002547 | 0.000157–0.000473 |
| 0 to 0.5 | 0.004266–0.011730 | 0.003395–0.009183 | 0.000533–0.001466 |
| 0.5 to 1 | 0.000643–0.001969 | 0.000404–0.001236 | 0.000080–0.000246 |

All eight report endpoints have support somewhere in each interval on this
roster. Fixing copying probability to one excludes seven of those eight
endpoints: the unsupported endpoint-count fraction is 0.875, not an error
probability or a report-probability mass. Other fixed bounds have full support.
Narrower supplied intervals reduce the compatible forecast segment. This is a
descriptive, supplied-law uncertainty result; no practical-effect threshold or
confidence interval was prescribed. It does not show that a learner can infer
these intervals or sources. The one-segment compatibility restriction and all
48 missing checkpoint strata remain.

[Numerical review](../../../results/v19/G19-B-source-interval-1/FINAL_REVIEW.json).
[Independent paired regroup](../../../results/v19/G19-B-source-interval-1/INDEPENDENT_REGROUP.json).

The successor asks whether a report with unknown source identity needs likelihood
marginalization before normalization. It compares that exact account with uniform
averaging of compatible source-conditioned posteriors and a most-recent-source
account. Impossible source/report pairs and support-failure probability remain
separate. All 28,672 posteriors and 505,856 distinct-source candidates remain,
with five copying probabilities, all eight endpoints and every future coordinate.
Forty-eight isolated controls pass; both native and portable dispatch are bound.
The source-frozen job was observed executing at below-normal priority through
the existing queue, with both complete replays queued. No numerical acceptance
is inferred from dispatch.

Individual-row CPU-clock quantization blocked the first timing estimate. Measuring
complete archive batches resolved that measurement issue; an intermediate
half-source-count assumption was then corrected against all 112 immutable source
inventories. Only the final full-roster measurement admits execution. Its inclusive
estimate at freeze is 3,103.69 CPU seconds under the 3,600-second card, including
both complete replays and 1,082 seconds reserved for independent reconstruction.
All earlier timing attempts remain retained and charged. A successful fixture
wrapper also encountered an immutable admission-alias collision; both successful
receipts were preserved and the final source tested under a new receipt identity.
No scientific run was restarted. [Successor contract](SOURCE_IDENTITY_ADMISSION.md).
