# Retrospective evidence after future-schedule grouping: prepared alternative

Does a representation preserving all remaining state schedules also preserve
updating on a newly supplied report about an earlier observation? This finite
B-family alternative has no handler or admitted outcomes. It is independent
of primitive feedback and needs no earlier numerical advantage.

Use the supplied unknown-time/type roster, horizons 32/128 and checkpoints
8/16/17/20/32. For each checkpoint, group complete hypotheses by the entire
remaining maker-state schedule. Enumerate all pairs within each group that
differ at a prior observation step. Preserve the complete memberships, with
no favorable witness selection. Use every earlier step from 1 through the
checkpoint, every context, endpoint, and all eight development laws.

Construct two full-simplex mixtures: equal mass on one member of the pair and
on a third hypothesis from a different future-schedule group. Exchange the
paired member in the second mixture. The mixtures have identical initial
quotient weights. Supply the same historical report to each, condition by
its state-dependent likelihood and compare updated quotient weights and every
declared future forecast. Enumerate third hypotheses through complete schedule
classes; retain multiplicities and any finer past-likelihood subdivisions.
Factor identical calculations losslessly. Zero evidence likelihood is an
explicit impossible-observation result, never a normalized zero vector.

Controls: identical full schedules including the past; uniform endpoint laws;
same-time and fresh-future reports; disjoint known likelihoods; complete pair
coverage; and deliberate faulty merging. Separate changing quotient weights
from changing forecasts, exact binary64 equality from 1e-12 display tolerance,
and full-simplex witnesses from posteriors reachable under actual observations.
This is a supplied-law representational guarantee question, not learned access,
historical correspondence, or human intent. No evidence is newly sampled.

Freeze a complete tractable lossless enumeration and source before dispatch.
If exhaustive factorization cannot fit the one-hour CPU cap including tests,
replays and review, record that scoped implementation limit and use the
independent primitive-feedback branch. No sampling substitute, tiny setting,
protected lineage, new owner, or automatic family-budget extension is admitted.
