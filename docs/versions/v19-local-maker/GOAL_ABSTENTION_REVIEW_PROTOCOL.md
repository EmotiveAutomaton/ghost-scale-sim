# Independent review of fixed-cost goal reports

Reconstruct every saved report from the accepted forecast and independently
checked marginals. Scalar goal enumeration validates probabilities, modes and
marginals; the recorded binary64 marginals define the executed strict thresholds.
Native forecasts have no stored marginals: repeat only their binary64 projection
primitive, check it against accurate scalar sums, then independently select
reports. This shared arithmetic is explicit and is not an independent test of
the numerical library. Equality abstains; no tolerance creates a modal tie or
changes the threshold. Verify all inherited decision fields before use.

Enumerate partial reports against sparse native goal labels. Score coverage,
correct and incorrect claims, stipulated native and forecast costs, partial and
full incompatibility, and unchanged logarithmic loss independently. Native truth
only scores learned reports. The empty report is compatible, and its conditional
error is undefined. Pooled conditional error is pooled incorrect mass divided by
pooled coverage; never average conditional ratios over unequal denominators.

Reconstruct all 46,080 learned score strata, 144 native rows, 225,280 learned
frame forecasts and 5,120 parent prediction/accuracy identities. Independently
regroup original and reconstructed rows with scalar means and bootstrap
multiplicities: 1,536 contrasts, 384 normalized log-budget areas and 288 means.
Keep two training draws and five seeds within each of sixteen paired development
lineages; use the frozen seed 191012 and 10,000 resamples. Training variation is
retained separately. No fitting, sampling or protected lineage is used.

Known-answer controls cover all 64 partial reports, scalar native cost, complete
and partial support, empty reports, exact/near thresholds, modal ties and the
distinction between joint-path probability and mean step error. Complete fixture
execution and deliberate corruption must fail invalid reports, probabilities,
native records, parent choices/marginals, scores, undefined ratios, rosters,
reader fields, reference masses, summary values, population and input bindings.
Source admission requires isolated controls, native/portable routing and measured
synthetic tractability within the original inclusive 1,800 CPU-second D card.

Freeze checker source, plan, environment and every input. Preserve both complete
producer replays and export unchanged public witnesses separately from scientific
forecasts, reports, targets and scores. Execution and replay acceptance precede
numerical acceptance. This is a constructed-method tradeoff under stipulated
costs, not historical goal correspondence, human intent or a user-value claim.
