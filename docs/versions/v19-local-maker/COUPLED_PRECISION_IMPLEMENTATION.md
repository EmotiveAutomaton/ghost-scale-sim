# Coupled arithmetic interpolation: executable contract

Retain the eight supplied laws, 81 precision assignments, three byte budgets,
four count vertices and lengths 16/64/128. Use one shared weight in [0,1] to
interpolate all twelve coordinates between the producer and scalar range arrays.
This line segment is contained in the accepted independent-coordinate arithmetic
box. Neither model certifies containment of exact real-valued ranges.

Convert both binary floating endpoints to exact rational numbers with a common
integer denominator. Each assignment has four affine vertex objectives. Enumerate
every vertex-line intersection in the interval, including inactive intersections.
Within each resulting open segment, identify the active objective lines using an
exact rational midpoint. Enumerate every feasible assignment-pair objective crossing
inside that segment. Retain all these events and every boundary's full objective
vector and complete optimum set, plus the optimum set on each final open segment.
The midpoint determines ordering on an analytically partitioned interval; this is
not a sampled-weight estimate. Coincident lines retain all assignment ties.

The maximum regret of each original computed-float choice occurs at a retained
boundary because its regret is affine on every final segment. Retain exact rational
regret at all three scales, every possible optimum and the intersection of optima
over the whole interval. Compare with the containing box's unresolved set,
guaranteed set and conservative regret bound. Keep original choices unchanged.

Known controls include an optimizer reversal, an interior-only optimum, exact ties,
common affine translation, feasibility, context permutation and containment in the
box. Complete-support synthetic timing must exercise all assignments, pairs and
segments before freezing source. Original, adjacent and extracted-source replay,
independent review and all failed work share an inclusive 1,800 CPU-second cap.
No fits, observations, protected lineages or reader inputs are added.

Independent review must reconstruct the rational line coefficients, byte charges,
all intersection events, boundary values, interval ties and extrema, parent input
identities, all 72 strata and nine equal-law summaries. Execution alone does not
establish numerical acceptance.
