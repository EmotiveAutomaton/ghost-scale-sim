# Process versus goal uncertainty: prepared independent diagnostic

Does the primary joint reader lose probability on the operation sequence, or on
goals conditional on that sequence? This prepared, unimplemented A diagnostic
uses the original frozen 5,832-label distributions, four tiers and budgets, two
draws, five feature seeds and sixteen development laws. No fit, new episode,
support restriction or protected lineage is needed. It can run regardless of
whether public-support decoding helps. Initial ceiling: 1,800 CPU seconds,
inclusive of tests, original, both full replays and independent review, within A.

Preserve the full unknown tail and derive the 216-operation-path marginal by
summing all 27 goal assignments. Decompose true expected joint logarithmic loss
exactly into operation loss and conditional-goal loss. Independently decompose
the supplied posterior entropy, so excess loss has the same additive split.
Report each original reader including same-budget frequencies, all tiers and
budgets, per-position localization and existing abstention. Do not call a lower
operation score goal recovery. Full witnesses supply operations explicitly.

Controls: identical operations with distinct goals; identical goals with distinct
operations; uniform distributions; a perfect joint posterior; unknown-only truth;
and deliberately swapped marginal axes. Reproduce every original joint loss,
check the chain rule to 1e-10 and reconstruct scalar sums independently. Average
draws/seeds within lineage before 10,000 paired resamples, seed 191004. Retain all
components and draw/seed variation separately. Preserve scientific and reader
roles and native weights. This is a constructed-method error decomposition,
not causal access, unique historical recovery or human intent.
