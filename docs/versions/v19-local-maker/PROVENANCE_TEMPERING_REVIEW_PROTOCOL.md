# Independent fixed-tempering reconstruction

Can the fixed-power comparison survive an independently implemented likelihood
calculation and paired regroup? This verifier consumes the frozen result and its
two previously verified supplied-law parents. It imports no producer filter,
projection, score or regroup function. It does not claim a new native-law audit:
the endpoint laws remain hash-bound references from the completed parent reviews.

The existing serial scientific worker first runs the frozen known-answer,
placebo, live-effect, scalar-product, pairing, complete native-fixture and
corruption tests. This is a mandatory gate, not a claim that queued tests passed.
Only then may it read and reconstruct the campaign result. Source, tests, inputs,
plan, environment and accounting are frozen before dispatch. No second worker.

Independently reconstruct all streams from the supplied laws and fixed random
draws; apply the source-identity omission alone. Build the full unchanged
unknown-time/type roster and prior. Multiply each distinct source likelihood
raised to powers 1, 0.75 and 0.5, rescaling after each source. Source time determines
its likelihood; query time determines current-maker projection. Verify every
joint weight, row map, posterior, endpoint forecast and all five scores. Verify
the exponent-one parent arrays and independent-source aliases exactly; retain
stationary reference-family aliases without counting them as replication.

Average sixteen makers equally within each observation draw, then draws within
eight paired coefficient lineages. Keep every length, change family, actual-change,
copy, checkpoint, evidence and fixed-power stratum. Recompute all means, both draw
means, lineage values and 10,000-resample intervals with seed 190501. Pair each
power with one in the same evidence condition, omitted with aware at equal power,
and each omitted power with aware power one. Preserve identity contrasts.

Use the frozen 0.02-nat practical margin for losses. Current-state inclusion and
set size remain a tradeoff. No selected exponent, new fit, observations, protected
lineages or tiny-model settings. The checker shares the original two-hour card
ceiling, within the initial B-family allowance and global exploration allocation.
Reader evidence remains the unchanged parent packets. All joint hypotheses, laws,
truth, scores and reconstruction exports are scientific/evaluator material.

Bounded primitive feedback and fixed-capacity mixture compression remain prepared
independent alternatives. Neither requires tempering to win; their separate
implementation/admission requirements remain open.
