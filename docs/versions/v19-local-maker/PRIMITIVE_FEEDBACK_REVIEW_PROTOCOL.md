# Independent fixed primitive-feedback review

Reconstruct both legal input orders, each truthful and bit-flipped observation,
all three table updates, deduplication and the single saved prior without importing
the feedback producer. Reuse the previously validated support reviewer's explicit
three-step path sum and independent native executor. Check every updated table,
640-query forecast, proper score, population mass and support/change stratum.
Parent count fitting and native path probabilities inherit their bound independent
reviews; reconstruct query endpoints and masses from the retained paths.

Check all anonymous reader budget slices, their content hashes and evaluator-only
pairing maps; unused feedback, rule identities, weights and query truth cannot
enter reader inputs. Verify zero-feedback identity, unchanged non-observed rows,
wrong feedback, undo/stay controls, conflicting duplicates, empty strata, and
deliberate table/forecast/score/weight corruption. A complete native fixture must
pass in an isolated checkout before admission. Absolute tolerance is 1e-12.

Regroup both training draws within each of the eight paired coefficient lineages.
Report each budget, update, order, feedback condition, tool, support mode,
changed/stay and old support stratum, with native and equal-query populations
separate. Retain all means and method-minus-unchanged, wrong-minus-true and
reverse-minus-forward loss contrasts. Use 10,000 paired lineage bootstrap draws
with fixed seed 190971 and the existing 0.02-nat margin. Intervals condition on
the two saved fits and deterministic feedback sets. Equal-query forecasts are
identical across lineages; their degenerate interval does not quantify training
or population uncertainty. No seed is a new scientific episode or model fit.

Run through the existing serial owner under the original four-hour feedback-card
and F-family/global ceilings. No additional tiny setting, protected lineage or
environment change. Numerical review and complete original replays have separate
acceptance records. Retrospective evidence updating and primitive alias pooling
remain independent prepared alternatives; neither depends on this result winning.
