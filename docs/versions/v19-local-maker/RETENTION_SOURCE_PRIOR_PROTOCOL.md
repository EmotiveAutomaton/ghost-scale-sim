# Source-prior sensitivity of fixed retention: prepared alternative

Does a fixed retention policy preserve useful past information when the later
report is more likely to concern earlier or later sources? Uniform source choice
does not answer this question. This B-family alternative crosses the existing
full, recent half/quarter and evenly spaced half/quarter source rosters with
uniform, earlier-weighted and later-weighted source probabilities already defined
in RETROSPECTIVE_SOURCE_PRIOR_PROTOCOL.md. Do not tune retention or source weights
from outcomes.

Use the complete saved posterior/source roster, eight laws, two evidence conditions
and both draws. Keep all report endpoints, copy probabilities and future coordinates.
Selection remains outcome-blind and counts remain matched. Each source's original
nonuniform probability contributes either its exact joint state or the explicit
uniform-maker-prior remainder. Copy histograms use the same source probabilities.
Compare each compact update with full retention under that same true source prior.
This isolates retention from source-prior misspecification, which was tested earlier.

Require direct scalar Bayes, normalized weighted remainder, constant-law/full/copy
identities, equal-source-prior reduction to the uniform experiment, source-order
permutation with identities fixed, and a deliberately dropped weighted remainder.
Report exact support and undefined errors separately from proper regret, future
probability errors and storage. Keep both draws inside equally weighted laws and
all source-prior/retention contrasts paired. No new fit, observation or protected
lineage. No learned provenance or historical correspondence claim is admitted.

Prepared only: implement and freeze exact source weights and complete-support
synthetic timing before native outcomes. The inclusive prospective cap is 3,600
CPU seconds within existing family/global ceilings, including controls, failures,
all three executions and independent reconstruction. No sampling to fit the cap.
Two-report dependence remains an independent alternative if this is blocked.
