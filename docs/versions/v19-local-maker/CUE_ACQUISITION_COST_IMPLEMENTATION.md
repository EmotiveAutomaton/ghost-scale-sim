# Cue acquisition cost: frozen implementation contract

This implements the prepared fee protocol within G19-B. Preserve 28 finite
storage libraries, 15 denominator-four source mixtures, cue correctness
1/3, 2/3 and 1, and fees 0, 1/64, 1/16 and 1/4. Fees are hypothetical utility
in source-probability units; they are distinct from measured CPU or storage bytes.

Before the cue, choose a feasible constant mask without payment or a complete
three-label policy paying once. Maximize expected retained mass minus fee.
Retain every paid policy and gross score, every optimal paid policy, every
optimal acquire/no-acquire choice at each fee, and the exact break-even fee.
Preserve all exact ties; the reported representative avoids acquisition on a
tie, then chooses the lexicographically greatest policy. It cannot change the
optimum value. Every realized mask satisfies the original byte capacity.

The known-channel integration reuses the accepted noisy-disclosure apparatus.
Independent test integration enumerates complete policies and paid/free choices.
Controls cover free-access identity, a known break-even tie, a positive net
benefit, uninformative/identical-prior nulls, zero-probability labels, monotone
acquisition with fees, corrupt byte charges and negative-fee rejection.

Capacity: 28 libraries, 1,260 channel problems, 5,040 fee problems; all 57,344
roster/budget rows join to 180 mixture/channel/fee comparisons, or 10,321,920
evaluations. Preserve both evidence conditions, eight lineages, two draws and
equal-law summaries. No fit, new observation, seed or protected lineage is used.
Teacher access is evaluator-only supplied priors and channel, with no reader
inputs. Architecture is exact enumeration; schedule is the complete frozen grid.

Require isolated controls, full-support synthetic timing, source/input/plan and
environment hashes, original plus adjacent and extracted-source replays through
the existing one-worker queue. Inclusive cap: 1,800 CPU seconds. Independent
numerical acceptance must reconstruct ancestors, all policies and fee choices,
costs and ties, all roster joins, paired/equal-law summaries and both replays.
This is a finite supplied-law decision study, not forecast or process recovery.
