# Independent review of retrospective full-simplex witnesses

Reconstruct all ten structural horizon/checkpoint cells, all eight development
laws and every retained original pair, third hypothesis and past-time membership.
Use direct bit transitions, Python tuple classifications and integer counters,
without importing the producer's structure, kernels or summary implementation.
Compare every structural array, class mapping and multiplicity with the original.

Compute all 131,072 scalar Bayes-kernel cells per law, including report
probabilities, both updated group weights, feasibility and quotient differences.
Check an independently rearranged Bayes formula. Reconstruct future-coordinate
maxima from scalar endpoint differences over every state in each future mask.
Group equal computations only after verifying their original identities; retain
every Cartesian-product multiplicity. Recompute all 80 law/checkpoint rows,
including impossible, binary64-nonzero and greater-than-1e-12 counts and maxima.
Binary64 equality is not rational-arithmetic certification.

Admission requires direct exhaustive small-roster Bayes and future-forecast
controls, uniform/stationary/same-time identities, zero/disjoint laws, permuted
identities and deliberate corruption of every factor and summary field. Execute
the complete synthetic producer/checker chain. Measure full native structural
enumeration and a synthetic full-law kernel/summary workload, charging tests,
failures, review and exports within the original 3,600-second card ceiling.

The checker retains independently reconstructed evaluator factors and summaries.
No new reader input, fit, seed, protected lineage or missing posterior is created.
Original and both complete replay sources remain unchanged. Independent checking
is required in addition to complete replay, and event-owned regrouping of all
law/checkpoint rows is required before final numerical acceptance. There is no
sampling confidence interval for an exhaustive finite full-simplex certificate.
Reachable-posterior updating and joint retrospective likelihood state remain
independent prepared alternatives. Historical correspondence and human intent
are outside this constructed-method test.
