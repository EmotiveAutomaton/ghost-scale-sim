# Aggregate state for source-context disclosure: prepared alternative

Does revealing a report's query context require retaining separate source tables,
or can a joint future-group/context/endpoint table preserve the full update?
Freeze the original uniform source prior and supplied endpoint law. Compare the
eight-outcome aggregate state with a 32-outcome joint context/endpoint state and
full-hypothesis Bayes. Unknown source identity remains marginalized. A context
with no eligible source has zero probability, not an invented observation.

Use all saved posteriors, eight laws, two evidence conditions, both paired draws,
all five copying probabilities and all future coordinates. Report the extra
memory and construction cost of retaining context, supported report mass,
forecast error against the exact context-disclosed reference, and future squared
loss reduction relative to the endpoint-only aggregate. Preserve context-specific
copy counts. No test outcome selects which contexts to retain.

Prepared, unimplemented and unadmitted. Require scalar joint Bayes, constant-
context identity, informative-context positive, source/context permutation,
zero-context support and corrupted-table/histogram controls. Complete-support
synthetic timing and frozen source must precede native outcomes. The inclusive
prospective cap is 3,600 CPU seconds, including checks, failures, three executions,
independent reconstruction and original-row regroup. No fit, new observation,
protected lineage or occupancy sampling. This tests one fixed source/report law;
source disclosure, arbitrary reweighting and dependent report pairs remain
separate. No minimality, optimized latency or historical correspondence claim.
