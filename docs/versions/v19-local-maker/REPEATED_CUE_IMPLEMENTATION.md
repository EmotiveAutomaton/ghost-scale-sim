# Repeated cue dependence: frozen implementation contract

Does a second source cue add value when its errors can copy the first cue?
This implements the prepared repeated-cue protocol on the same 28 storage
libraries, 15 supplied source mixtures and three known symmetric channels.
For each true source, the first label follows that channel. The second copies
the first with probability 0, 1/2 or 1; otherwise it is an independent draw.

Retain all nine ordered pairs, exact rational joint and conditional source
probabilities, every feasible mask score, all ties and the greatest-mask
representative. Each pair can independently select any feasible mask; the
Cartesian product of retained tie sets defines all optimal complete policies.
There is no coupling constraint across observed pairs. Zero-probability pairs
retain all score ties and explicitly undefined conditional probabilities.

The exact expected coverage gain over one cue is the break-even second-cue
fee. Also choose each mask assuming independence, then score it under the true
dependence. Preserve gross coverage, signed gain, regret, expected bytes and
maximum bytes in positive-probability pairs. Hypothetical source-probability
utility remains distinct from measured CPU and bytes. No training, teacher
labels for a reader, observations, new lineages or hidden truth in reader input.
The law, mixture and structural costs are supplied evaluator information.

Preserve 57,344 roster/budget rows joining to 135 mixture/channel/dependence
conditions (7,741,440 evaluations), 3,780 distinct library problems, original
pairing, both training draws and equal weight for each of eight laws. Prospective
inclusive cap: 1,800 CPU seconds, including setup, failures, all three executions,
independent reconstruction, regroup, documentary work and publication. Admission
requires isolated known-answer controls and complete synthetic-support timing.
Freeze source, input, plan and environment before original execution. The existing
serial queue owns original, adjacent replay and extracted-source replay.

Independent review must rebuild ancestor costs, all libraries and per-pair
calculations, validate all ties and undefined cases, reconstruct original rows
and paired/equal-law populations, and verify both complete replay outputs.
Controls include exact independent gain, copied/perfect/uninformative identities,
marginal preservation, normalization, source-label permutation, a harmful wrong
independence fixture and corrupt byte charges. This is finite supplied-law
information value, not attained forecast accuracy or process correspondence.
