# Independent witnessed-goal review

Does removing dependence among the remaining goals improve frozen forecasts after
the actual operation sequence is supplied? Execution and complete replay are
separate from independent numerical acceptance of this constructed-method test.

Bind the unchanged producer plan, all source and input hashes, environment,
deterministic outputs and separate timing receipts before inspecting results.
The checker consumes every retained complete-witness packet, sixteen development
laws, two training draws, five feature seeds, four budgets and four readouts.
No fit, new sample or protected lineage is used. The previously accepted joint
and public-support scores must reproduce; their inherited fits and native targets
are not regenerated by this checker.

Decode the three public operation witnesses directly in their declared order.
Their integer code must select exactly the 27 goal labels in the accepted support
mask. Accurate scalar sums reconstruct the restriction normalizer and each of the
three three-way goal marginals, using integer quotient/remainder membership rather
than producer tensor axes. Reconstruct the restricted distribution and the product
of goal marginals independently. Check every saved normalizer, marginal and
resulting probability within 1e-12. Comparing normalized probabilities as well as
normalizers protects very small conditioning masses from absolute-tolerance errors.

After those checks, score the saved binary64 normalization and marginal products
to retain the executed tie decisions. The existing independent checker supplies
forecast expansion, sparse native-reference scoring and candidate ranking; no
producer probability, marginal, scoring or regroup routine is imported. Candidate
sets stop at the first 90% cumulative crossing. Alphabet-first order resolves
learned ties; full-label order resolves native ties. Verify every proper score,
compatible mass, candidate measure, unknown mass, abstention and per-step accuracy
within 1e-10. All three native arms remain separate evaluator rulers.

Reconstruct all 7,680 learned strata, 48 native strata and 112,640 learned frame
forecasts over 704 public packets. Separately regroup original and reconstructed
rows with scalar sums and a bootstrap multiplicity matrix. Average draws and
seeds within each of sixteen paired lineages; use 10,000 resamples with seed
191008. Retain all sixteen budget contrasts, four normalized log-budget areas,
48 metric means and separate draw/seed summaries. These intervals condition on
the retained fits and do not quantify general training-population uncertainty.

The isolated controls cover dependent and independent goal paths, equal marginals
with different paths, axis reversal, fixed operations, unknown tails, point
forecasts, candidate ties, zero and tiny conditioning mass, malformed inputs and
complete synthetic execution. Deliberate score, localization, coverage, marginal,
normalizer, operation-code, support-mask, support-score, target, reader-field,
hash, summary and missing/duplicate-row corruption must fail. Native runtime and
portable-dispatch coverage are part of admission.

Source admission requires those controls and a conservative complete-work timing
screen. The unchanged 1,800 CPU-second card cap includes producer preparation,
failures, execution, both replays and this review. Use the existing serial queue,
one below-normal CPU worker and one numerical thread. The 96-hour total and
16-hour reserve remain unchanged. Reader inputs contain only the 704 anonymous
public evidence packets; targets, forecasts, marginals and scores remain separate
scientific/evaluator material.

Retrospective updating and temporal alphabet-mass matching remain independently
prepared alternatives. The full within-step comparison retains its recorded
timing blocker. Numerical acceptance does not establish historical goals, causal
access, confirmation or human intent.
