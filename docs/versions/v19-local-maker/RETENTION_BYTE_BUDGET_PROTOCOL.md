# Fixed byte budget for source retention: prepared alternative

Do comparisons at the same source count conceal unequal memory costs? Freeze a
byte budget separately for each structural cell and source roster, before using
posterior weights, endpoints or forecast errors. Use the lesser byte cost of the
existing recent and evenly spaced half-retention states as the half budget, and
the corresponding lesser quarter cost as the quarter budget. Structural joint
group/past-state support determines costs, independent of measured effects.

Under each budget compare recency priority and chronological midpoint priority.
For the latter, first visit the exact midpoint ranks of the original matched-count
roster in increasing time order, then all remaining sources in decreasing time
order. Recency visits all sources in decreasing time order. Timestamps are unique.
Traverse the complete fixed priority order, retaining a source if its structural
cost fits the remaining budget, otherwise continue. Always charge the group mass,
full copy histogram, omitted-context counts, source metadata and structural
indices; share the original prior table once. Report unused bytes and retained
counts separately. This tests fixed-budget allocation, not global optimization.

Preserve all saved posteriors, source candidates, eight laws, paired draws, both
evidence conditions, all copying probabilities, reports and future coordinates.
Every omitted source keeps its original probability in the uniform-maker-prior
remainder. Pair all comparisons within the same posterior and budget. Empty
retention is valid when overhead exhausts the budget; negative usable budgets
are explicit inadmissible settings, never silently omitted.

Prepared, unimplemented and unadmitted. Require independent structural byte
counts, deterministic priority/tie/permutation controls, scalar Bayes, exact
budget compliance, zero-budget and all-retained controls, support masks,
complete-support timing, and source freeze before native outcomes. The inclusive
prospective cap is 3,600 CPU seconds within family/global limits, including failed
checks, three executions, independent reconstruction and original-row regroup.
No fitted model, observation, protected lineage or occupancy sampling is used.
No practical margin or confirmatory interval is frozen. Scope is a supplied-law
constructed method; learned provenance and historical intent remain unestablished.
