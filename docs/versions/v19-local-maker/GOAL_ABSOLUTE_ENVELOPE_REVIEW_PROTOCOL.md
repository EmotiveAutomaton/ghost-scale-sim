# Absolute residual envelope: independent numerical review

Does bin averaging conceal frame-level absolute probability errors? Numerical
acceptance requires an independent scalar reconstruction of the complete frozen
producer output, after original, adjacent and extracted-source replays agree.
This checker imports no producer score or aggregation function.

Reconstruct native goal marginals with scalar base-three sums. Recreate and
scalar-check the executed matrix projection to preserve its binary64 bin
boundaries; this does not audit the numerical library independently. Rebuild
all fixed five-, ten- and twenty-bin weights, forecast and target sums and empty
means. Independently sum signed residuals inside each bin and check the same
absolute discrepancy. Sum absolute residuals before bin averaging; verify every
saved signed residual and weighted absolute contribution, the nonnegative
envelope gaps and their nesting, retaining roundoff within 1e-12.

Check every inherited accepted bin score, the full parent decision roster,
fine forecast loss and coordinate goal accuracy. Preserve all sixteen laws,
two training draws, five fit seeds, four budgets, four readouts, two forecasts,
three positions and three goals. Native and equal-frame weighting are separate.
The expected evidence comprises 92,160 rows, 320 group/forecast/raw bundles,
225,280 frame forecasts, 704 reader packets and 5,120 parent score identities.

Regroup original and reconstructed rows separately, using scalar means and
bootstrap multiplicities. Seed 191021 fixes 10,000 paired sixteen-law resamples,
with both draws and all five fit seeds kept inside each law. Verify 576 means,
1,728 envelope estimates, 3,024 budget contrasts and 756 normalized log-budget
areas. A third direct-index regroup is required at final numerical acceptance.
Conditional intervals and draw/seed variation remain distinct; no practical
threshold or winner is selected after results.

Before admission, controls must cover known opposing and constant residuals,
unequal and zero weights, native self, frame/class permutations, every exact
and adjacent boundary, probability one, undefined empty means, synthetic full
execution, missing/duplicated rosters and deliberately corrupted raw arrays,
forecasts, parent scores, summaries and schemas. Validate native and portable
dispatch in an isolated source copy. A full-size synthetic benchmark must fit
the inclusive 1,800 CPU-second D card with all prior work and operating costs.

Reader inputs remain unchanged and separate from evaluator targets, arrays,
scores and reconstructions. No new fit, sample or protected lineage is used.
This is an exploratory constructed-method diagnostic; historical local-goal
correspondence and human intent remain unestablished. Retrospective updating
and exact forecast-collision uncertainty remain independent prepared alternatives.
