# Partial prior disclosure: frozen implementation

Implement the prepared partial-disclosure protocol on the same 28 finite mask
libraries, three source priors, 15 mixtures in quarters, eight development laws,
two source draws, seven checkpoints and paired evidence roles. Use all five
partitions of three identities: none, each binary split, and full disclosure.
No new observation, fit, protected lineage or tiny setting is used.

Within each disclosed cell, maximize conditional expected retained mass exactly
with rational arithmetic. Preserve every mask score and tie; choose the largest
mask among ties. A zero-probability cell has undefined conditional probabilities
and mass, zero contribution, and all masks tied. Its deterministic tie choice is
retained but does not contribute to expected or maximum realized byte charges.
Every candidate mask individually meets the frozen byte budget. Preserve exact
gross value over no disclosure and the remaining gap from full disclosure.

Architecture is finite enumeration with no fitted capacity or optimization
schedule. Teacher access is supplied prior identity or its partition, declared
mixture weights and evaluator source costs. No acquisition fee, future outcome
or forecast label is provided. No reader inputs are added.

Raw evidence has 57,344 roster/budget rows joined by selection identity to all
15 mixtures and five partitions: 4,300,800 joined evaluations and 2,100 distinct
structural disclosure problems. Repeated structural answers are not independent
law replications. Retain selections, cell probabilities, all ties, rational
information values and byte charges. Independent review must reconstruct all
parent rosters and costs, exhaustively enumerate cell-to-mask policies and check
the complete paired and equal-law joins. Original and two full replays are required.

Known-answer controls include selective partition value, identical-prior and
point-mixture nulls, zero-probability handling, permutation, refinement monotonicity,
individual byte feasibility and malformed inputs. Full-support synthetic timing
precedes source admission. The inclusive cap is 1,800 CPU seconds, including
tests, failures, review, replays and write-through. Family/global limits and the
confirmation reserve are unchanged. Precision-choice stability and noisy prior
disclosure remain independent prepared alternatives.
