# V18.1 active progress

The first G0–G4 wave is executed and independently checked; the campaign continues.
Read the [interim results](RESULTS.md), [branch ledger](../../../../results/v18/continuation/BRANCH_LEDGER.json),
[bridge](BRIDGE.md) and [eight selected illustrations](ILLUSTRATIONS.md).

The larger dependency-transfer run and paired existing-Stitch comparison have
passed their first-block checks and are being resumed sequentially by one native
supervisor. Event-only review is configured separately; an active process is not
completion, and a configured event delivery is not proof that its review occurred.

The preserved G3 runtime failure was resumed from identical frozen source and
inputs. G4's 18 sampling clusters represent 16 distinct physical contexts; the
72 blind cases are unchanged. Full first-wave reaggregation and bounded extracted
source replay now pass. No final confirmation has been frozen or claimed.

The current integrated suite passes all 41 checks with zero failures or skips,
including the existing native Stitch executable and a real subprocess delivery
fixture. The latter verifies event plumbing without invoking a model; this
campaign's first actual model review has not yet been delivered.

A cheap structural compiler is admitted on 25,408 reused input cases for 72,960
matched budget comparisons after the active queue. It uses the public topological
part order to remove supports safely and rebuild in order. Its result is pending;
any final learned-representation contrast must first address this stronger rival.
See the [validation receipt](../../../../results/v18/continuation/VALIDATION.json),
[target checks](../../../../results/v18/continuation/TARGET_VERIFICATION.json),
[direct plan](../../../../results/v18/continuation/structural-direct/PLAN.json), and
[interim time account](../../../../results/v18/continuation/TIME_ACCOUNT.json).
