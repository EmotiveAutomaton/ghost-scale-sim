# First evidence review and next decisions

19 September 2026. P1 and the timing pilot are independently replayed. The
campaign remains active; the next finished batch triggers another review.

P1 separates purpose alignment from generic code diversity. Highest-entropy
old-task ties improve core transfer but hurt under the lexicographic rule.
Prospective portfolios improve the four-symbol comparison under both boards,
with an old-task cost. The current decoder knows the law, so retained information
and practical learned access remain separate.

The next compression design should hold portfolio size and supervision quantity
fixed while varying alignment: useful independent purpose questions, irrelevant
questions, and a misleading law. Its known-answer gates should make irrelevant
questions identical under both possible histories and include an exactly
discriminating question. The expected informative outcome is an alignment-dependent
advantage, a reversal, or equivalence; no sign is a validity gate. This is a new
structural comparison, not more seeds for P1. It is a priority candidate for the
next refill, alongside learned observable-state objectives.

The neural pilot measures fitting time without resolving the substantive support
question. Scaling its actual costs led to a complete 64-epoch matched slate,
estimated 5.9–9.1 fitting CPU hours before additional generation/development/replay.
Replace the width-scaling estimate after the first main packet. The earlier
96-epoch plans are preserved as unexecuted. The purpose is to obtain all six
matched regimes and retain room for new diagnostic rounds before morning.

Coverage checks caught query/state aliasing before main execution; rotating the
question menu by 16-history blocks restores equal question exposure within each
latent state. A larger held-out roster now includes all three history lengths.
The initial failed control used only the unchanged first rotation block; its
fixture was expanded across the boundary and the failure record retained.

Next operational transition: the 96-step change-adaptation packet, followed by its
single-worker replay slot and the long learned-support slate. Read actual receipts
instead of treating these estimates as completion evidence. Source investigation,
family revision, intervention composition and preparation economics remain useful
candidate families and have not been relabeled as implemented work.

The 32-step U1 result sharpens the next candidate: role/rate averaging reduces
the stationary penalty but fails to repair a new decision law. It also improves
opportunity-change proper scores without improving independent-observation
artifact matching. The next reviewed expansion should separate a changed latent
state from a changed family and price any source verification or family revision.
These are useful unresolved distinctions; no exhausted-useful-work state applies.
