# V18.4 review 4 — 20 September 2026 UTC

The complementary old-question support packet and its adjacent verification are
complete. The scientific write-through and portable raw/weight export were
published in commit 0116983. This is distinct from the running full-support packet,
the three diverse-question packets, and the admitted matched-prefix diagnostic.
All earlier science and retained failures remain unchanged.

Swapping the trained combinations preserves old-question recurrent gains and the
new-question reversal on shared test histories. It weakens an explanation specific
to one support half, but does not separate missing question labels from inaccessible
or inadequate learned state. The support swap is paired, not a new independent
test-world replication. The exact finding paragraph is in RESULTS and exchange.

## Timing and useful buffer

The completed science used 2,351.984375 charged CPU seconds and 2,416.56 wall seconds;
verification added 4.421875 CPU seconds. The two main packets average 2,390.13 science
CPU seconds and 40.91 wall minutes. Four remaining main regimes project about 2.73
wall hours from the last completion, with a 25% planning margin. U2 adds its prior
600–1,000 CPU-second forecast. These estimates are not runtime fulfillment.

## Explanations and next decisions

| Candidate | Expected distinguishing evidence | Priority and disposition |
|---|---|---|
| Missing maker support | Full support repairs new-question loss | Existing L1 full-support packet running; preserve it |
| Missing question supervision | Diverse training repairs covered compositions, possibly not farther questions | Three matched L1 regimes already queued |
| Decoder fails to use retained information | Added readouts recover prediction from frozen states beyond question-only and shuffled-label rivals | Implement L3 now, on both verified parent supports |
| Linear readout is too restricted | Fixed nonlinear readout improves at declared label budgets | L3 crosses linear/nonlinear access with two nested budgets; all rungs reported |
| State is inadequate for farther questions | Neither readout repairs transfer despite adequate controls | L3 bounds this decoder family only; L2 observable-bank objective remains next |
| More labels alone explain improvement | Raw-history or question-only gains match memory gains | L3 gives rivals the same fresh labels and compares learning curves |
| Evidence age explains skill reversal | Common-trace windows change mixture/static ordering | U2 is already admitted with replay, not completed |
| Wrong explanatory family | Averaging or paid expansion repairs rule change with distinct costs | R1 remains useful after decoder admission; not implemented |
| Provenance or purpose alignment | Noisy paid audits or equal-size purpose portfolios alter tradeoffs | S1/P2 remain useful independent families; no exhaustion claim |
| Intervention reuse or preparation cost | Untaught compositions or accuracy-matched workload changes the benefit | I1/C1 remain useful after the learned diagnostics |

One further sequential Terra research pass addressed the specific decoder-control
decision. The reviewer checked the primary sources and retained their limited role
in the separate research notes. L3 measures accessibility under added supervision,
not original model use, semantic role identification or unrestricted information.
It uses a fixed random-feature nonlinear decoder rather than an open-ended neural
search. A known nonlinear fixture validates that the rung can express an
interaction; success on the scientific cases remains an outcome, never a gate.

The new packet combines both parent supports and shares fresh cases and baseline
fits. Eighteen immutable encoders plus raw-history and question-only features,
two readout families, two label budgets and aligned/permuted supervision give 160
readout fits. Each has five test conditions. Parent fitting is not repeated.
No farther-question labels enter train, development or feature selection.

The detailed admission receipt records measured controls, source hashes and the
science/replay plans. Initial tests encountered only an inaccessible default
temporary directory; both failed attempts remain retained and charged. Workspace
temporary directories resolved that environment issue without changing permissions.
No useful-work-exhausted state applies, and this review does not close the campaign.

## Admission outcome

Fifteen scoped controls passed in isolated source copies, including the optional
CPU Torch fixture: 39.984375 CPU seconds for the final selected checks. Earlier
failed setup attempts and the preceding successful check pass remain separately
charged. Both nested budgets complete whole 384-history cycles so state, query
and history length are jointly balanced; this arithmetic was checked before freeze.

Queue revision 9 retains all previous slots and appends one combined L3 science
packet with its adjacent independent replay. It contains 12,288 training histories,
2,048 development histories and 1,536 test histories per condition, shared across
both parent supports and the two baseline representations. There are 160 readout
fits, not 160 independent experiments. All raw forecasts, copied encoders and
readout parameters are required export material. The generic paired-summary
reference is explicitly the smaller-budget raw linear reader; scientific decoder
comparisons must also compare the matched capacity/budget/control cells retained
in the complete summary rather than conflate additional labels with memory access.

The broad 800–3,600 CPU-second forecast includes the new data count, frozen-state
extraction, full-width raw-history readouts, scoring and replay. It is anchored by
about 218 seconds for the previous main packet's generation/evaluator parent and
3.4375 seconds for the small optional fixture suite; full-size decoder scaling is
uncertain and must be replaced with actual measurements. Together with the existing
matched slate and U2, it adds useful buffer without claiming the minimum exploration
window is already covered. L2, R1, S1, P2 and I1/C1 remain substantive next designs.

[Source-bound admission](../../../../results/v18/exploratory-loop/L3-ADMISSION.json).
The support-swap publication CI failed solely on the retained V15 C11/M01
instruments; the failed log was inspected. Those gates were not weakened.
