# V18.4 review 5 — 20 September 2026 UTC

The full maker-support old-question science packet and adjacent verification are
published in commit 1744ce6. All 90 means and 2,880 fixed forecasts reproduce,
with maximum forecast error zero. Portable raw data, selected weights, source,
plan and proof have complete scientific write-through. Three diverse-question
regimes and U2/L3 remain running or queued. Runtime fulfillment is not claimed.

Full support leaves the new-question reversal. Missing maker support is therefore
insufficient to explain it at the fixed total budget. Fewer examples per combination
leave a coverage-by-density interaction possible. The exact finding paragraph is
in RESULTS, exchange and the user-facing report.

## Timing and useful queue

Science used 2,653.3125 charged CPU seconds and 2,725.0692 wall seconds; verification
added 5.265625 charged CPU seconds. The three main regimes average about 2,477.86
CPU seconds and 42.41 wall minutes. Three remaining regimes project 2.12 hours from
02:25 UTC, with a 25% planning margin. U2's 600–1,000 and L3's 800–3,600 CPU-second
forecasts add useful buffer. Actual measurements must replace these estimates.

The rows name competing explanations, distinguishing evidence and disposition;
unexecuted candidates are not findings.

| Candidate | Distinguishing evidence | Priority and disposition |
|---|---|---|
| Missing maker combinations | Full support alone repairs new-question forecasts | Not supported at this fixed total budget; completed paired comparison |
| Coverage-by-density interaction | Full support plus equal per-combination samples repairs failure | Possible; lower priority than pending question/access tests |
| Missing question supervision | Diverse labels repair covered compositions, perhaps not farther questions | Three frozen L1 regimes running or queued |
| Decoder cannot use retained information | Extra-supervision readouts recover from frozen states | L3 already admitted |
| Old-answer bank already carries sufficient predictions | Supplied-law map improves new answers without new labels | Implement L2a on the verified full-support parent |
| Algebraic closure hides unstable access | Map norms and raw invalidity explain failed conversion | L2a fixed full/truncated/passive maps and raw diagnostics |
| Objective failed to shape useful predictions | Bank training improves matched-label transfer beyond fixed decoder intervention | L2b remains useful after L2a/L3; not implemented |
| Evidence age or omitted law | Common-prefix update or paid family comparison separates them | U2 admitted; R1 next independent-family candidate |
| Provenance, purpose alignment, intervention reuse or preparation | Paid audits, matched purpose portfolios, untaught compositions or accuracy-matched cost crossovers | S1/P2/I1/C1 remain substantive openings |

One further bounded Terra research pass informed bank access and conditioning.
Primary sources and adoption decisions are recorded separately in research notes.
No delegate implemented or audited repository changes.

L2a uses nine full-support parent weight sets, five test conditions and four decoder
arms: 36 reader/decoder/seed arms and 180 forecast files. Fit seeds and questions
remain paired within lineage. Maps use public world coefficients alone, never hidden
test state. Extra supplied intervention-law access is explicit. Raw invalidity cannot
be concealed by the declared floor-and-normalize scoring arm. The finite span and
conditioning results will inform later bank-target training. No useful-work-exhausted
state applies.

The previous decoder admission's CI failed only on retained V15 C11/M01 instruments;
the failed log was inspected. Those tests and records remain unchanged.

## Admission and verification

Thirteen scoped controls passed in an isolated source copy, including the optional
CPU Torch integration fixture, for 50.015625 CPU seconds in the final selected checks.
The first Torch attempt found two implementation defects: a completed resume tried
to rewrite an immutable completion timing record, and the replay helper reimported
an older worker that attempted to reset Torch interop threads twice. The new bank
worker now validates and returns the retained completion, and the new replay driver
uses the current guarded worker. Existing scientific snapshots are unchanged.
Both failures and all earlier successful test costs remain retained and charged.

The final controls cover finite closure, an observational alias, uniform targets,
near-singular truncation, invalid raw outputs, independent forward arithmetic,
parent/capsule binding, unchanged weights, complete resume, corruption rejection,
independent scoring and full raw-forecast replay. Before execution, the initial
bank plans were superseded by a second source freeze whose proof also independently
reconstructs all 75 diagnostic means and explicitly records full raw-output replay.
Both unexecuted original plans and sources remain retained in the plan ledger.
Queue revision 11 preserves the preceding 24 slots and admits the revised bank
science and adjacent replay. The new diagnostic has not yet executed.

Its broad 300–1,800 CPU-second forecast includes science and replay. The approximately
22-second integrated small fixture has one selected weight and one twelfth of the
full history count; the full packet has nine weights, shared law-map generation and
no fitting. Scaling is uncertain and must be replaced by actual measurements.
This is useful buffer, not a duration requirement. L2b and independent R1/S1/P2
designs remain actionable on future events, even if this queue finishes early.

[Source-bound admission](../../../../results/v18/exploratory-loop/L2a-ADMISSION.json).
