# Event review 12: restored exposure and a conditional-target test

20 September 2026 UTC. The earlier undershoot remains acknowledged. L4 restored
exposure completed science and adjacent replay. Restoring the original-question
label count retains covered improvement and farther deterioration in every
learned reader, including the no-history control. This rejects dilution as the
sole explanation in this contrast. Changed training volume and updates remain
unresolved until the matched-total arm completes. No partial effects are read.

All 105 means and 3,840 fixed forecasts reconstruct exactly; source, input,
selected-weight and retained-file hashes match. Portable reassembly passes.
Science plus replay used 4,801.859375 CPU seconds and 4,953.802823 wall seconds
(82.56 minutes), within the admitted science forecast. Ten isolated result
controls pass. The result batch has full scientific write-through and is published.

The table lists candidate explanations, separating observations and priority.
Desired scientific rankings are outcomes, never validity gates.

| Candidate explanation | Separating test and possible outcomes | Priority |
|---|---|---|
| Training volume or optimization explains the restored-exposure contrast | Original-menu arm with the same total histories and updates may retain or remove deterioration | Already running L4 matched-total |
| Conditional uncertainty in training targets impairs fitting useful banks | Same sampled histories and initialization; realized-state versus exact conditional old-answer targets. Old-only improvement, transfer, tie and harm are all informative | Implement and validate L2b this review |
| Old-bank decoding is unstable rather than information-poor | Feasible projection versus fixed truncation, prior-only and history posterior | L2c admitted, pending |
| Distinct-family mixtures behave differently from aliases | Distinct supplied laws and explicit predictive-class prior mass | R2 admitted, pending |
| A learned decoder extrapolates badly even with accurate old predictions | After L2b, compare direct farther forecasts with feasible supplied-law decoding of the same bank | Useful follow-on conditional on L2b/L2c |
| Audit symmetry or graph aliases remove useful inquiry | Asymmetric source reliability and a separating audit; alternatively a persistent shared paid-channel error | S2 remains useful, below the now-identified learning contrast |
| Intervention mappings fail beyond supplied combinations | Untaught joint/ordered interventions and equally informed alternative organizations | I1 remains useful; requires separate implementation |
| Preparation pays only at matched accuracy | Fit/update/query workloads after matching held-out loss | C1 after current fits |
| Near-tied purpose shifts preserve the same partitions | Exact retained-code partition equivalence | Lower-cost P2 diagnostic remains useful |

One sequential bounded Terra pass challenged conditional-target assumptions.
The relevant population loss identity is checked locally, with no claimed
transfer guarantee. The return's suggested uniform average after conditioning
was corrected to posterior weighting; a suggested random balance gate was rejected.
Fresh sampled-state training avoids assigning the old balanced-index allocation
an unsupported teacher prior. Common realized-state development targets prevent
arm-specific selection objectives. Test allocation and the exact reference's
stipulated prior remain explicit limitations. No implementation or audit was
delegated. Research sources and scope are retained separately in the research notes.

The new design uses two paired 768-history arms, not repeat seeds of completed
science. The observed restored-exposure cost suggests 4,300–5,900 CPU seconds per
arm, with additional bounded target-construction and audit overhead checked in
fixtures. Remaining L4, R2 and L2c plus the proposed pair supply several useful
hours if admission succeeds. Estimates are forecasts and do not change the
12:00 UTC minimum exploration target, 13:00 science cutoff, 15:00 delivery or
64,800 cumulative CPU-second ceiling. Queue drain remains a refill request.
Neither useful-work exhaustion nor minimum-window fulfillment is claimed.

The first full replay-interface test caught a path error: the target auditor was
given the packet root instead of its data directory. This was an integration
failure before admission, not a scientific null. The failing receipt is retained;
the caller is corrected without changing any numerical acceptance threshold.
The separate tiny training fixture passed both target capsules, paired tensor
inputs and shared development targets, 20 fitting steps per arm, checkpoint resume
and zero-discrepancy forecast replay. It used 29.343750 CPU seconds; its warm/cold
generation times are not interpreted as a target-speed comparison. The fixed
replay entry point is rechecked before admission.

The repaired replay entry point now passes all seven conditional-target controls;
the wider unchanged generation/exposure/runtime checks supply ten further controls.
Queue revision 18 appends two science packets and their adjacent verification
slots, preserving all preceding 42 slots. The new pair comprises 24 fits and
12,288 training histories with 61,440 target rows per arm. Both source snapshots
and plans are frozen. Admission estimates total 8,600–11,800 CPU seconds; actual
consumption and absolute cutoff still govern execution. The retained failed
control run is charged alongside successful checks and integration work.
[Admission](../../../../results/v18/exploratory-loop/L2b-ADMISSION.json) and
[integration proof](../../../../results/v18/exploratory-loop/L2b-INTEGRATION.json)
are receipts for admitted work, not scientific findings. At admission the
cumulative charged snapshot was 39,021.393322 CPU seconds, including the active
worker's latest receipt; further computation continues to accrue.
