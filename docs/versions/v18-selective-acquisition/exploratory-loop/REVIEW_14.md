# Review 14 — 20 September 2026: target baseline and bank access

The earlier campaign undershot the owner's requested exploration. This event
review continues the commissioned adaptive loop; the completed packet is neither
campaign closure nor a finding that useful experiments are exhausted.

The realized-target L2b arm and its adjacent replay are now verified. Recurrent
memory helps on the original menu, while direct, flat and split history readers
lose to the question/world-only rival on compositions and farther questions.
The new independently sampled states and old-only development change several
features relative to L4, so cross-study scores do not identify a sampling or
selection effect. The conditional-target arm is running and supplies the actual
controlled target comparison. No partial effects from that fit are inspected.

Science charged 4,285.937500 CPU seconds; its adjacent replay charged 19.421875.
Their combined wall time is 75.84 minutes. The charge uses the larger native
parent receipt plus recorded child work. All 105 means and 3,840 fixed forecasts
reconstruct exactly; the independent scalar target audit checks 160 rows with
maximum discrepancy 3.33e-16. Twenty-one isolated conditional-target, generation,
bank and runtime controls pass. This is bounded replay, not full retraining.

The table records competing explanations, the comparison that separates them,
and its current priority. Rows are designs or completed evidence, not claims
that every family has run.

| Remaining explanation | Decisive comparison and possible outcome | Priority and disposition |
|---|---|---|
| Realized-state targets add finite-sample uncertainty | Paired conditional-target fit improves old fitting, transfer, both or neither under the same population objective | Highest; already running with adjacent replay |
| Direct extrapolation hides useful old-bank information | Common supplied-law decoder on both L2b arms; compare history, no-history and prior forecasts, then the paired target difference | L2e realized bank admitted now; conditional bank waits for its parent replay and exact pairing |
| Diverse allocation damages old-bank forecasts as well as direct extrapolation | Already admitted L2d matched-total banks; equal or better diverse bank retains decoder extrapolation, worse bank retains bank quality as a contributor | Two science/replay pairs already queued |
| Old-only versus diverse development changes selection | Replay epoch-selection alternatives with identical trained checkpoints and declared development access, or new fully paired fits if checkpoints insufficient | Useful, but current retained best/checkpoint inventory may not support every epoch; no fabricated reconstruction |
| Raw surprise responds to predictive randomness rather than missing law | R3 whole-horizon calibration on separate worlds, matched calibration purchase rates, fresh evaluation and retained timing/refit costs | Concrete researched design remains useful; implementation and calibration required |
| Audit benefit depends on symmetric graphs and independent paid channels | S2 asymmetric graph priors or persistent shared audit/evidence errors with independent enumeration | Useful distinct family; new likelihood and decision controls required |
| Separately taught intervention laws fail when composed | I1 joint or ordered interventions against equally informed role organizations | Useful distinct family; implementation required |
| Preparation costs are repaid only at matched accuracy | C1 held-out accuracy-matched fitting, update and query workloads | Useful after current fits; raw speed alone cannot answer it |
| Shifted supplemental purposes retain useful partitions | P2 exact partition-equivalence diagnosis on retained codes | Useful low-cost alternative; shifts alone are not a test of lost geometry |
| Finite old-bank fitting lacks predictive closure | Explicit finite predictive-bank objective and recursive update controls | Useful architectural extension; not implemented by externally decoding a bank |

L2e reuses the validated L2c instrument without changing scores, tolerances or
source algorithms. Its first 1,536-history input roster binds to the newly
verified realized-target parent and twelve selected banks per history. The
conditional counterpart is a concrete next design, not prematurely admitted
science. Its event must first verify the parent, bind forecasts, prove roster
identity, freeze source/plan and append its own replay. Losses cannot select
histories or admission. Direct farther tests and bank cases have different state
allocation, precluding a paired decoder-effect claim from their overall means.

L2c measured 376.828125 CPU seconds and 6.42 wall minutes for science plus replay.
L2e retains a broad 180–500 CPU-second forecast per arm. The conditional fit's
baseline-derived completion estimate is around 12:02 UTC, with a provisional
11:55–12:15 range for target/audit overhead and native scheduling. The two L2d
packets add 360–1,000 forecast seconds, and the newly admitted realized bank adds
180–500. These forecasts do not authorize running past 13:00 or imply fulfillment
of the 12:00 minimum. With about two hours to the science cutoff, adding hours
of new fits would risk the cutoff; bounded diagnostics are the useful next work.

Prior Terra research on conditional targets and certified convex decoding already
informs this specific diagnostic. No additional literature claim or delegated
implementation is needed. A new search would be justified by a new unresolved
design question, not by the need to fill time.

The result received portable raw/weight/source export, exact reassembly and full
scientific write-through before its ordinary push. The three previously pending
CI runs fail only the unchanged historical V15 C11/M01 instrument test. New push
CI is inspected separately; failed instruments remain failures, not scientific
nulls. Source snapshots in use, old campaign records and environments are unchanged.

[Verified baseline](../../../exchange/v18-4-L2b-realized-response.md) and
[L2e protocol](PROTOCOLS.md#l2e-conditional-target-comparison-through-a-common-bank-decoder)
separate completed evidence from new diagnostic design.
