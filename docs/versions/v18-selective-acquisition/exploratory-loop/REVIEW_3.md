# V18.4 review 3 — 20 September 2026 UTC

The first full learned-reader packet is complete, independently verified and ready
for publication. Scientific completion, replay, export and publication are separate
states. Its nine fits preserve recurrent gains on held-out maker combinations and
reverse them on new questions. The exact finding is in RESULTS and the exchange.

The measured science cost is 2,428.28125 CPU seconds and 2,492.94 wall seconds;
verification adds 4.515625 CPU seconds. The remaining five matched packets provide
about 3.5 hours of useful work at this rate (plan with 25% margin). This is shorter
than the remaining minimum exploration window, so subsequent rounds are needed.
The immutable cumulative ceiling and minimum/cutoff/delivery times are unchanged.

## Competing explanations and next decisions

| Candidate | Distinguishing result | Decision |
|---|---|---|
| Missing maker-combination support | Full support repairs question transfer | Existing matched slate remains admitted; old-question transfer already survives the combination holdout |
| Missing question supervision | Diverse supervision repairs covered compositions but not the farther holdout | Existing matched slate runs next; avoid calling covered questions novel |
| Objective learns an inadequate state | A finite observable predictive bank repairs farther transfer without farther labels | L2 remains high priority after coverage contrast; needs explicit bank closure and access controls |
| Decoder cannot use retained information | A separately supervised nonlinear decoder succeeds on frozen states where a linear decoder fails, with randomized-target controls | L3 remains high priority; better decoding alone does not prove semantic roles |
| Skill adaptation needs older evidence | The mixture/static difference changes across nested prefixes after the same absolute change time | Implement U2 now, using fresh common trajectories and equal post-change windows |
| Different prior traces caused the U32/U96 reversal | Nested-prefix ordering does not reproduce the earlier reversal | U2 is informative either way; preserve both completed horizon packets |
| Purpose alignment rather than generic diversity | Equal-size aligned/irrelevant/misleading portfolios differ | P2 remains useful, lower immediate priority than the measured skill reversal |
| Wrong explanatory family | Family mixtures versus paid revision distinguish averaging from commitment costs | R1 remains useful; role mixtures cannot express this change |
| Uncertain evidence independence | Paid audits beat new evidence only for some audit accuracy/stakes | S1 remains useful; requires an actual noisy audit and its price |
| Supplied interventions or workload economics explain gains | Untaught intervention compositions, or accuracy-matched reuse, change rankings | I1/C1 remain useful after learned coverage/objective diagnostics |

No exhausted-useful-work state applies. Three existing Terra research returns
already provide concrete objective/decoder/source designs; another search is not
needed to implement the matched-prefix diagnostic. U2 will use fresh 96-step
streams, absolute switches at 12 and 28, prefixes 32/64/96, and equal 16-step
post-switch windows to separate evidence age from averaging-window composition.
Static, fixed-role and role/hazard-mixture settings remain unchanged. Null controls
require prefix identity and pre-change identity; no desired method ranking gates
validity. All full traces and native actions remain portable evidence.

The initial roster has 19,200 verified finite evaluations, one pilot and one main
neural packet verified, and five main neural packets still running/queued. U2 is
a new diagnostic, not repetition of the completed horizon packets. Admission and
measured control timings will be recorded before appending its adjacent science
and verification slots. This review is not campaign closeout.
