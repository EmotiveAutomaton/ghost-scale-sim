# Review 16 — 20 September 2026: target-bank access and partition diagnosis

The minimum 05:00 PDT checkpoint has passed. The inherited 06:00 PDT science
cutoff and 08:00 PDT delivery remain fixed. Useful work is not exhausted. This
review publishes both target-bank arms, then prepares a bounded structural
diagnostic derived from the verified purpose-alignment result. It adds no fits,
seeds or repetition of finished science. The queue drained while design and
publication work proceeded; elapsed-window fulfillment is not uninterrupted
occupancy or complete experimental coverage.

Both target banks contain useful history under the common supplied-law decoder.
Conditional targets improve farther mean logarithmic loss slightly: flat memory
0.78259 to 0.77983 and split memory 0.77849 to 0.77717; direct history improves too.
All three improve on compositions as well and beat both no-history references.
The matched banks share all 1,536 selected world/history/state identities. Their
direct farther answers had worsened, so that behavior does not exhaust the useful
information accessible through the original-question banks. Decoder access,
optimization and selection remain contributors; different direct-test allocation
prevents a paired decoder-effect interpretation. Supplied finite law and the
stipulated twenty-four-state decoder prior remain explicit privileges.

Each bank verifies 3,200 independent means and eight whole-unit replays; all
18,432 certificates per arm pass. Parent/input/source hashes and portable raw
reassembly agree. The unchanged instrument and tests match the previously passed
21-control isolated source byte-for-byte. Science plus replay costs are
378.234375 CPU seconds and 6.47 wall minutes for the realized arm, and 370.312500
CPU seconds and 6.35 wall minutes for the conditional arm. Those are completed
costs, not forecasts. Retained failures and the original carry-forward remain
charged. No full retraining or architecture severity is claimed.

The next question is why several misaligned purpose assignments nearly tied the
aligned portfolio in P2. The candidate explanations are: identical selected
partitions, distinct partitions with equivalent future forecasts, and distinct
forecasts whose aggregate losses happen to be close. P3 compares all seven shifts
and the marginal control with the aligned code at every cardinality across all
640 verified parent worlds. It reuses selected codes; future answers do not
reselect them. Raw bindings and exact parent-score reproduction precede diagnosis.

Partition equivalence is measured without symbol names. Disagreement counts how
many of the 28 unordered history pairs change their grouping. Forecast comparison
retains the largest probability difference across five future tasks and its fixed
1e-12 tolerance, beside both losses. Identical partitions imply identical forecasts
under this decoder, but neither converse follows. All three cases have known-answer
fixtures; a scalar membership/decoder/score implementation checks retained units.
The resulting frequencies and conditional losses are descriptive, not a randomized
causal effect or new independent-world replication.

Eleven isolated partition, purpose-alignment and runtime controls pass. Four fixed
retained cases take 0.640625 CPU seconds including computation and independent
unit verification, projecting 102.5 seconds across 640 worlds before aggregation,
input/output and replay. Admission reserves 400 CPU seconds for science and replay
and additional administration allowance, checks the actual ledger and time to cutoff,
freezes all sources/inputs and provides the adjacent replay. The frozen plans and
admission receipt distinguish readiness from execution. Earlier source snapshots
are unchanged. No additional literature search is needed for these finite
partition identities; the sequential Terra purpose-alignment research already
motivated the retained parent experiment.

The table gives remaining useful candidates, what they would distinguish and
their priority at this boundary. Unimplemented candidates are not executed coverage.

| Candidate | Decisive outcome or unresolved explanation | Disposition |
|---|---|---|
| P3 partition access | Same code versus equivalent predictions versus close aggregate scores | Implemented, validated and selected for bounded admission now |
| R3 calibrated purchases | Calibration of the whole purchase process separates excess buying from useful missing-law detection | Useful; needs separate calibration/evaluation worlds and timing, not a threshold fitted to R2 |
| S2 paid provenance | Asymmetric truth priors or persistent coupled audit/evidence errors change actual decision value | Useful; new latent likelihood and known-answer controls required |
| I1 joint interventions | Untaught compositions distinguish mapping interpolation from reusable intervention organization | Useful; new data, equally informed rivals and fits do not have an admitted cutoff-safe estimate |
| C1 accuracy-matched costs | Preparation repayment at matched prediction quality, including updates and fitting | Useful; existing speed records alone cannot establish the comparison |
| Selection interactions | Shared checkpoints selected under distinct declared development targets | Useful; existing checkpoint inventory lacks all epochs, so it cannot answer this by relabeling |
| Predictive-state learning | Explicit bank objectives and recursive updates versus supplied-law access | Useful architectural extension; bank decoding is not its implementation |

After P3 verifies, land its result and assess whether a further bounded diagnostic
can be completed and independently checked before cutoff. A drained roster is not
an exhaustion finding. If the remaining useful designs cannot fit, retain their
concrete next-design disposition as cutoff-limited. Final reporting must separate
the elapsed minimum, actual work/costs, published evidence and missing coverage.

The prior conditional-target and conditional-bank admission CI runs fail only the
retained V15 C11/M01 instrument verdicts. Other recent CI runs remain separately
tracked until their final status is inspected; no green repository CI is claimed.
