# Event review 11: useful history and feasible decoder access

20 September 2026 UTC. The earlier undershoot remains acknowledged. L4's first
matched-exposure control has completed science and adjacent replay; the two
paired exposure arms remain running or queued. No partial fit effects are used.
The first control retains original-question recurrent gains but loses to the
question/world-only rival on compositions and farther forms. It does not yet
resolve exposure dilution. Full scientific write-through and portable evidence
are published in the named L4 result batch.

Science plus replay charged 3,055.843750 CPU seconds and took 3,148.495390 wall
seconds (52.47 minutes), within the 2,700–3,700 CPU-second science forecast.
All 105 means and 3,840 fixed forecasts reconstruct, with zero replay discrepancy.
Ten isolated result controls pass. The two remaining L4 arms retain their larger
768-history budgets; they are substantive paired contrasts, not repeat seeds.

The table lists unresolved explanations, the next observation that separates them
and the current priority. Numerical validity never depends on the desired ordering.

| Explanation | Discriminating observation and possible outcomes | Priority |
|---|---|---|
| Diverse training only harmed farther performance by diluting old-question exposure | Restored-exposure and matched-total controls can repair, preserve or reverse the boundary | Existing L4 arms executing/queued |
| Clipping masks avoidable bank-geometry error | Feasible hull forecasts versus frozen truncated maps on the same learned banks; lower residual need not improve loss | Implement L2c this review |
| Supplied-law bank gains are mostly world/question information | Same-world uniform prior and no-history neural banks may tie or beat history banks | Include in L2c |
| Exact history contains useful distinctions that learned banks omit | Exact-history posterior versus projected learned banks under the same stipulated prior | Include in L2c; not proof of optimality under deterministic allocation |
| Better conditional training targets repair the learned bank | Actual-allocation conditional teacher versus realized-state targets, matched data and updates | Next learning design after L2c; teacher prior still requires explicit treatment |
| Distinct law averaging differs from aliased label averaging | R2 varies distinct law catalogs and class prior mass | Already source-bound with adjacent replay |
| Audit symmetry or graph alias removes decision value | Asymmetric source reliability or a graph-separating third audit | Useful S2 candidates after current result |
| Purchased channels share an unmodeled error | Persistent shared error in audit and evidence changes paid inquiry value | Separate useful S2 candidate |
| Interchange teaching fails on composition | Untaught joint/ordered interventions with equally informed alternative organizations | I1 remains useful |
| Preparation costs are repaid only at equal accuracy | Held-out loss-matched workloads including fitting, updating and query costs | C1 after exposure contrasts |
| Cyclic purpose shifts preserve useful partitions | Exact code-partition equivalence across retained P2 shifts | Useful lower-cost P2 diagnostic |

L2c uses 1,536 fixed retained histories and all twelve selected fit banks, with
fit seeds averaged inside history. It creates a decoder intervention rather than
a new neural training run. Selected indices balance eligible states and history
length; the law's full 24-state prior is stated, not equated to the fixed sample
allocation. A bounded Terra return informed certificates but its incorrect
five-dimensional reading was rejected. No implementation or audit was delegated.

The bank projection supplies eighty old-answer coordinates. Global squared-error
suboptimality is bounded by an independently reconstructed simplex duality gap.
This checks an optimizer, not the scientific claim. Residuals, rank, raw invalidity,
proper scores and no-history references remain reportable whether projection wins
or loses. A near-optimal old bank need not fix a poorly conditioned future answer.

The queue remains useful and the campaign remains active. Minimum exploration is
12:00 UTC, science cutoff 13:00 UTC and delivery 15:00 UTC. Neither experimental
coverage nor minimum-window fulfillment is claimed; no useful-work exhaustion
review is applicable. Actual fixture timing and source-bound admission follow
isolated validation and are retained with the admission receipt.

The initial inconsistent-bank timing fixture failed its independent certificate:
SLSQP reported success while one gap was 1.46766e-7, above the fixed 1e-7 bound.
The retained failure is numerical, not a scientific null. The optimizer now uses
a tighter internal stop, a feasible active-face least-squares polish and bounded
exact-line-search refinements. The independent acceptance threshold is unchanged.
Eleven isolated controls pass, including that failure pattern. Eight disjoint
full numerical fixtures, each with twelve banks and independent reconstruction,
cost 0.703125 CPU seconds and 0.689851 wall seconds. Their largest gap is below
3e-15. Scaling generation/verification gives roughly 135 seconds before summary,
input parsing and replay overhead; the conservative admitted forecast is 180–500
CPU seconds. This narrow diagnostic is not presented as hours of queue coverage.
The remaining long L4 arms provide that coverage while upcoming events inform
further conditional-target and intervention designs.

L2c is now source-bound in queue revision 17 with its adjacent independent replay,
leaving the previous forty slots unchanged. The frozen integration fixture also
passes dispatch, raw retention/reload, aggregation, forty independent means and
one exact whole-unit replay. This fixture is not execution of the scientific packet.
[L2c admission](../../../../results/v18/exploratory-loop/L2c-ADMISSION.json) and
[integration proof](../../../../results/v18/exploratory-loop/L2c-INTEGRATION.json)
retain the distinction between admitted and verified science.
