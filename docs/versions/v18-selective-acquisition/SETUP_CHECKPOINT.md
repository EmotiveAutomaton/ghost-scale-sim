# V18 setup: implementation admitted, scientific endpoint decision pending

**Historical checkpoint, superseded by [the pre-run clarification](PRERUN_CLARIFICATION.md)
and [completed scientific study](RESULTS.md).** The renewed user instruction commissioned
completion and filing; all core/extension blocks subsequently executed. The original
pause and engineering outcomes below are retained as their dated record.

18 September 2026. Can focused study preserve useful craft, and when does paid
checking protect later construction from interference? This is the commissioned
constructed mechanism question; no new scientific finding is claimed here.

The new V18 adapter and finite runner are implemented. The original handoff is
preserved byte-for-byte in [CODING_PACKAGE.md](CODING_PACKAGE.md). The design and
ownership record is [README.md](README.md). No V15–V17 scientific source or result
was changed, and no historical campaign was launched.

Fourteen targeted tests passed with zero skips in an isolated checkout. They cover
the offered-only and acquired-record interfaces, paired realized offers, fixed
allocation, feedback eligibility, empty libraries, ties, untrained target rules,
independent execution and search costs, checking inside the envelope, corruption
refusal, native ownership, literal resume, full replay and actual extracted archive
execution. The native GUI interpreter and consoleless launcher also completed the
actual V18 supervisor/worker path. Its output was independently verified and fully
replayed: 1,344 transfer executions from the same discarded eight-constructor,
two-history development population at two budgets. Engineering repetitions are
not new independent observations or scientific replication.

The first admission failed when canonical JSON serialization changed stratum-key
order and hence replay row order. Enumeration is now explicitly sorted. The failed
attempt is retained locally; all scientific source remains unfrozen pending the
decision below. The earlier worktree setup hit Windows long-path limits; a complete
isolated checkout with command-local long-path support replaced that setup attempt.

## Endpoint conflict requiring a curator decision

The handoff preserves actual execution errors and fixes transfer goals to cross-pair
compositions. It also says transfer endpoints must never equal acquisition endpoints.
These constraints conflict if “acquisition endpoint” means every observed endpoint:
failed two-action trials sometimes accidentally make a later changed-target object.
The discarded development population contains 48 such arm-level exposures; offers
shared across broad/focused arms can appear in both counts. None is a successful
acquisition of that cross-pair, and the learner excludes their failed feedback.

The curator has been asked whether to preserve the specified errors and targets,
retain/count those exposures, and describe the transfer goal as untrained rather
than the endpoint as never seen. The alternative needs an explicit design revision.
No outcome-dependent exclusions, substitutions or target replacements were made.
The independent report already counts accidental exposures without changing the
experimental policy. **The scored 64-constructor cohort has not started.**

## Ready next action

Resolve that interpretation, record it before scientific outcomes, freeze the tested
source and 64-by-four case list, and run the first complete eight-constructor block.
Then continue the finite core and admit budget 128 solely from the recorded runtime
and resource allowance. Development timing suggests this tiny study can finish well
before the weekend ceiling; no time-filling repetitions are authorized.

One worker, standard-library science, no GPU or environment synchronization. The
local acceptance record conservatively anchors the clock at 14:00 UTC on September
18, before the first recorded startup timestamp at 14:29:18 UTC; the exact first
read was not timed. Its fixed admission cutoff is September 20 at 12:00 UTC and
delivery ceiling 14:00 UTC (05:00 and 07:00 Pacific). This earlier conservative
clock does not extend the supplied Sunday schedule. Codex in the commissioning
conversation owns the final independent analysis and documentary write-through.
