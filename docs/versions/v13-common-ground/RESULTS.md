# V13 — Common Ground. Results

**Skeleton written before any scientific number was read (2026-08-25), alongside the launch of
the discovery run.** Everything below the placeholder lines is filled in the pass that lands the
run's numbers in `FINDINGS.md` and `docs/theory/READING_INTENT.md` — the same pass, per the
standing rule. The frame is fixed now so that reading the results cannot quietly reshape it.
Spec: [`V13_SPEC.md`](../../../V13_SPEC.md). Implementation translation and the four judgment
calls: [`SPEC.md`](SPEC.md).

## Pass A — read first

*(To be written after the run. The section order below is the spec's §21 order and is frozen.)*

1. **Where the project world moved.** *(pending — at most two pages)*
2. **The five strongest results or boundaries**, in global language. *(pending)*
3. **Which competing explanations gained or lost probability.** *(pending)*
4. **What should enter Sounding Line next**, with exact controls. *(pending; the bridge ledger
   lands in [`BRIDGE_PACKET.md`](BRIDGE_PACKET.md))*
5. **What remains unmeasured**, especially every human and neural gap. *(pending — this section
   may not be shortened to make room for results)*
6. **Questions for the curator**, only where answers change the next branch. *(pending)*
7. **Recommendation:** continue Ghost, pause it, or retain only selected rulers. *(pending)*

> **STOP READING HERE**

## Pass B — analyst appendix

*(Generated by `runners/report_v13.py` from the committed verdicts; assembled here after the
confirmation lane closes. Contents per spec §21: per-card plain-language paragraphs before
metrics, conditional matrices and intervals, the seven-gate batteries, matching residuals,
cost-cause identifiability maps, trust and uptake factorization checks, the central/shared-brief
equivalence audit, exact–PyMDP discrepancies, attacks and repairs, lane separation, power and
multiplicity, runtime forecast versus actual, file hashes and the fresh-clone receipt, and the
Sounding Line bridge ledger.)*

## Deviations after data

*(Empty at launch. Any post-lock change lands here with the original beside the replacement.
Pre-lock instrument corrections — the smoke-pass gate redefinitions — are visible in the git
history of `ghostscale/validation/soundingline/v13/cards/` and are summarized in the analyst
appendix, not here, because they preceded every lock and every scientific read.)*
