# V17 bounded review: empty pooled assembly acquisition

Does a pooled procedure library help construction when donors obey different
assembly dependencies? This event review identifies an apparatus failure in that
comparison. It makes no new claim about the compared mechanisms or human readers.

At 11:52:28 UTC on 13 September, the retained robustness constructor numbered
72,451 failed with `ValueError: pooled acquisition is empty`. The exact event and
failure identities are preserved in the [review proof](../../../results/v17/pool-failure-1/REVIEW.json).
The original worker caught the exception and halted further sampling of the pooled
assembly branch for this stage, as its frozen branch-failure rule specifies. Other
branches continued. This is branch attrition, not just one skipped observation.

The separate checkout reproduced the failure in the fifth of eight histories.
All four donor worlds allowed the second part to stand independently; the receiver
required it to depend on the first part. Of 64 donated training examples, 56 tried
to attach the second part without its support, and eight tried to remove the first
part while its dependent remained attached. All were correctly rejected. The
fixed four-donor rule therefore supplied no legal acquisition examples. No
prediction or construction score exists for the failed atomic constructor.

The branch retains its completed initial expansion of 32 constructors and 256
histories, plus the robustness prefix of 4,025 constructors and 32,200 histories.
These are execution denominators, not effectiveness estimates. The preceding
valid robustness constructor, numbered 72,435, reproduces all eight retained cases
exactly. The isolated test of the literal runner guard confirms that a recorded
branch failure prevents another unit from entering execution. The 37 archived
source members, plan and controller bindings matched admission; the original source
and immutable window remained unchanged. The [diagnostic script](../../../results/v17/pool-failure-1/review.py)
uses a read-only database connection and does not invoke a scientific stage or
the optional procedure learner.

At 12:01:19 UTC, worker **277512**, supervisor **504436** and notification process
**454700** were alive with their original creation times. The worker heartbeat was
less than one second old and recovery still recorded one original launch. The
worker's error log remained empty because the constructor exception was handled
and retained in the failure ledger. Zero apparatus-issue *rows* does not imply
zero apparatus failures: this failure occurred before any rows could commit.

The retained failed-unit dependency cache contains 56 files, totaling 97,492 bytes.
Its bounded local archive and native process snapshot are recorded in
[OPERATIONS.json](../../../results/v17/pool-failure-1/OPERATIONS.json).
The notification stream records an independent thread and a started model turn for
this event. That establishes actual model invocation; successful delivery closure
is still the notifier's responsibility after this review exits.

Disposition: retain the failure and curtailed branch, and leave the healthy worker
to service the remaining eligible schedule. Replacing donors or changing acquisition
rules would define a different experiment and requires separate source, namespace
and admission. It cannot repair the original comparison by silently replacing data.
The earlier admission suite tested valid pooled examples but did not establish
that every four-donor draw supplies a legal pool.

Final closeout must report the failed constructor and curtailed robustness prefix,
keep initial expansion and robustness denominators separate, and preserve this
diagnosis and the failure cache in its archive. The frozen closeout code will mark
commissioned executable scope incomplete when a failed unit exists; a successful
process exit must not erase that qualification. Whether the retained prefix is
useful is for completed analysis, with its actual selection and stopping limits.

Sampling still ends **17 September 2026 at 18:09:19 Pacific**. Expected products
remain the raw database, frozen branch and confirmation receipts, independently
verified comparisons and results, time account, archive proof, and separate reader
and evaluator bundles. Full scientific verification, theory/FINDINGS/exchange
write-through and the authorized ordinary push belong to the completion review.
This bounded review checked case generation, source identity and branch containment;
it did not verify all database rows or assess unfinished scientific effects.
