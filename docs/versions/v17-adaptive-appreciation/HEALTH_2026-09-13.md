# V17 health check, 13 September 2026

The scientific queue is healthy and fully populated. This is an operational report, not a new interpretation of scientific effects.

At the 03:43 Pacific inspection, the original worker and supervisor were alive with recent heartbeats. The worker had retained 111,376 constructor units, 706,760 history cases and 10,175,024 method rows. There were zero failed units, zero apparatus-issue rows and one original worker launch with no recovery restart. The scientific source hashes and controller hash all matched admission.

All 24 expansion branches completed their 32 constructor draws / 256 histories. All three fixed confirmation runs completed their planned 46,062 constructor draws and 184,248 histories. Robustness sampling was active across the declared selected-region and coverage schedule. Completion of these runs does not itself announce that their scientific criteria held.

The queue still has its finite, predeclared backlog through the immutable deadline. The database occupied about 3.0 GB, with about 1.1 TB free. Allocation remains one scientific CPU worker, one numerical/Stitch thread, no GPU. Native Python launcher parents account for the additional matching process entries; they are not duplicate scientific workers. The original worker's stderr log was empty.

## Tests and source integrity

The retained V17 admission and compatibility suites passed all 110 tests. Source hashes still match that admitted implementation. The latest completed full CI at 817b6a3, run 34729888247, failed only the existing V15 C11/M01 instrument gate test. No new V17 failure appeared. There was no need to rerun valid scientific work or synchronize its environment.

Seven additional notification-specific tests passed for the operational repair below. They exercise a real harmless subprocess, once-only delivery, the correct scientific root, acknowledgement of an already-reviewed event, bounded failure, restart ownership, missing/changed executable refusal and refusal to reopen an app-owned session.

## Notification failure and isolated repair

The scientific supervisor attempted six deliveries across three completed checkpoints. Every CLI attempt failed before a model turn because the app still owned the requested conversation's writer lock. The earlier read-only protocol check established that the conversation existed; it did not prove it could be resumed by a second process. Those failed attempts remain on record.

A separate notification-only native service now opens an independent CLI review session for future event batches. This follows the fresh-session use of `codex exec` in the [official OpenAI documentation](https://learn.chatgpt.com/docs/non-interactive-mode). It does not take over the app's conversation, restart scientific processes or change the frozen study.

The original broken delivery trigger is archived and disabled. The new service has its own ownership lock, durable delivery receipts, at most three attempts per event and no heartbeat/work-remains wake. The three existing checkpoint notifications were acknowledged by this user-requested operational review; scientific interpretation and final write-through remain owed at closeout. A notification-sidecar restart preserves a live review rather than launching a duplicate.

The native independent-session probe successfully acquired a new thread, found saved authentication and retained workspace-write containment with automatic review. It started no model turn. Therefore **configuration, process launch and protocol were verified; actual future model delivery remains to be established by its delivery ledger**.

One nonblocking checkpoint-label issue is retained: the event called “robustness 32,768 constructor units” fired against the campaign-wide count, so it fired at the start of robustness after confirmation. Robustness itself now exceeds that count. This affects the notification label/timing, not samples, scores, selection or the deadline; the live frozen source was preserved.

Repair source, test and native launch receipts are in [notification-repair-1](../../../results/v17/notification-repair-1/REPAIR.json). The original scientific worker is **277512**, original supervisor **504436**, and new notification-only process **454700**. Their native launch check confirmed the original scientific creation times were unchanged.

## ETA and next agent action

Sampling ends **Thursday 17 September 2026 at 18:09:19 Pacific**, unchanged. My current planning estimate for the verified results, archive proof and final public filing is **Friday 18 September in the morning**. That final handoff estimate allows independent verification, compression/archival and result review; it is not an exact measured completion time.

Expected outputs remain the raw compressed database, frozen branch/confirmation receipts, final comparison tables, RESULTS.md, time account and separate reader/evaluator bundles. Native supervision continues the queue. The next agent invocation is only for an actual new failure, disappearance, resource checkpoint or completion event; healthy external computation does not keep an agent turn open.
