# V17 continuation: remaining scope and five-day execution

Can inferred procedures, concrete memories, recipient models and selective computation improve useful prediction? The completed screens compare these mechanisms in constructed miniatures. They do not test the human theory.

The screens identify several useful follow-ups: recipient inference improves predictions at substantial cost, some adaptive methods struggle with misleading evidence, and Stitch's compressed procedures often cost more search than simpler methods.

This assessment uses the seven retained packets (1,832 cases and 37,256 rows, including discarded pilots); the amended revision packet replaces its original trace provenance without adding independent observations. All proper scores, counted-cost identities and aggregate means were independently checked. Constructor and structural support remain limited. The [original checkpoint](SETUP_CHECKPOINT_2026-09-12.md) and its raw/source archives remain intact.

## Explicit timing amendment and allocation

The user's subsequent instruction commissions every remaining implementation, including the optional partial-sharing comparison, and a **new 120-hour continuously serviced scientific allocation after setup**. This is an explicit amendment to the coding package's original soft implementation-through-analysis target. The original implementation start and target remain recorded unchanged. The new worker assigns its execution start/end once in WINDOW.json; resumes inherit them.

One scientific CPU worker, one thread per numerical library and one Stitch thread, no GPU, no environment synchronization. Sounding Line's processes and services are outside this queue's ownership. The allocation stops admitting new units at its deadline; the active atomic unit and full independent closeout then finish. The final analysis, archive review and public filing follow completion and may add time beyond the 120-hour sampling window.

## Scientific tree and frozen confirmation

The first continuation cohort has 24 targeted branches, each with 32 constructor draws and eight histories per draw. It covers construction and exceptions in both worlds, personal/pooled acquisition, graphic/assembly recipients, adaptive inference, revision/feedback, all four observer families and expert/unfamiliar partial sharing. Construction budgets and storage caps are explicit in each target/method identifier; the earlier three-cap storage curve remains available.

After that complete cohort, a deterministic rule selects two regions per family by the largest unsigned method separation plus twice the constructor standard error, with ties by identifier. Reversals remain eligible. Three selected-region passes and one all-region coverage pass then sample new independent namespaces until the deadline. The maximum index is finite and far beyond the available allocation. This is a program of new scientific draws, with transactional resume, not repeated execution of completed packets.

The following primaries were selected from the completed initial screens for practical recipient gain, a model-misspecification reversal, and transfer-relevant value of feedback. They are frozen separately before their reserved fresh namespaces are used. No later branch ranking changes them.

The table defines each paired gain, its minimum practically relevant gain and fixed number of constructor draws. Four histories are averaged within each constructor draw; aliases and repeated actions do not enlarge that count.

| Comparison | Gain being tested | Minimum gain | Constructor draws |
|---|---|---:|---:|
| Changed recipient | Retrieval minus inferred-recipient Brier score | 0.02 | 20,472 |
| Accidentally correct belief | Fixed-inverse minus empirical Brier score after 12 observations | 0.02 | 20,472 |
| Stale purpose | Feedback minus matched-cost internal revision success | 0.05 | 5,118 |

The [frozen plan](../../../results/v17/continuation-1/PLAN.json) retains discovery paired means, between-constructor variances, observed costs and nominal attainable intervals. Sample sizes take the greater of a discovery-variance normal planning calculation and a conservative bounded Hoeffding requirement. The simultaneous one-sided radius is at most 0.04; the family uses Holm correction. Normal planning intervals are descriptive, not the confirmatory test. A failed superiority test is not equivalence. Incomplete primaries keep probability one in the same family and receive no replacement or significance-driven extension.

These are independent constructor *draws with replacement*, not a claim of that many distinct structures. Public problems, private constructions and structural families are hashed and their unique counts reported separately. All architectural conclusions retain **miniature — architecture untested**.

## Remaining implementation now supplied

[The coverage matrix](SPEC_COVERAGE.md) maps the commissioned scope to code and retained evidence. The additions supply pooled acquisition with target exclusion and equal example counts; actual dependency-bearing assembly recipients; shared/separate/partial performer-observer learning with prediction and personal execution separated; fixed fresh confirmation; deterministic experiment selection; compressed transactional raw storage; final comparisons, illustrative selection and an extracted reader consumer.

The implementation preserves the original A–E science and the recorded D trace amendment. It does not tune an old scoring rule to improve a result. Unavailable Stitch produces explicit apparatus-failure rows while native representations continue; an unverified replacement binary is never run. Storage uses one compressed SQLite database and temporary per-unit dependency caches that are archived before cleanup. The allocation is capped at 256 GiB of database and a 64 GiB free-disk floor; reaching either emits an intervention event.

Final illustrations are the lowest case hash within each actually observed advantage, reversal or failure type on the completed expansion cohort. The five specified counterexample types are included only when present. Reader-compatible examples and evaluator examples are distinguished; absent reader types remain explicitly absent. E remains a finite A–D policy reconstruction task, rather than a claim to recover every historical stochastic row from all earlier screens.

## Verification and event-only operation

The frozen admission passed **71 tests, zero skips**, including actual Stitch, assembly physics, projection boundaries, no-information controls, partial sharing, proper scores, counted costs, confirmation/Holm known answers, corruption refusal, literal resume, full phase progression, extracted reader predictions and private-file denial. A separate compatibility pass also executed all 39 existing foundation/queue tests successfully (110 V17 tests across the two disjoint suites). The previous full CI at 5607288 failed only the retained V15 C11/M01 gate test. Those failures were preserved.

Development failures remain recorded: an early SQL insertion placeholder mismatch was repaired before scientific use; one test invocation omitted the isolated cache environment variable and failed two dependency tests; a PowerShell wildcard invocation collected zero tests. The corrected explicit-file invocation passed. No failed scientific result was reclassified as a pass.

A native supervisor holds OS ownership locks, preserves an already-running worker and resumes missing work with a bounded recovery budget. A uniquely named local scheduler task supervises this process. The worker/checkpoint ledgers retain interruptions; no claim of uninterrupted occupancy follows merely from configuration.

**Healthy external computation ends the agent turn.** Only completion, failure/disappearance, resource intervention, or the three named scientific checkpoints may request an agent. No periodic model checks, Stop-hook continuation, recursive queue instruction or “work remains” wake is used. Agent delivery is batched, deduplicated and bounded; a supervisor restart preserves a live delivery. Native scheduling and actual model delivery are separate verification states.

The local protocol check found the exact existing conversation and saved login without starting a model turn. The installed CLI's explicit-session resume interface supplies transition delivery, following [the official non-interactive interface](https://learn.chatgpt.com/docs/non-interactive-mode). Actual future model delivery is established only by its delivery ledger. Neither a configured task nor this protocol check alone proves it.

## Closeout and public products

Expected outputs: records.sqlite; frozen branch selection and confirmation receipts; closeout/RESULTS.md, COMPARISONS.json, ILLUSTRATIONS.json and TIME_ACCOUNT.json; separate reader/evaluator ZIPs and a portable-consumer proof. Full independent row verification runs before the completion event.

The large raw database and dependency logs remain machine-local pending archival/publication review; source snapshots, admission, plan, compact comparisons, proofs and bounded examples are public products. The completion event requests one agent review to finish honest analysis, theory/FINDINGS/exchange write-through, transfer documentation, archival checks and the already-authorized ordinary push. Campaign completion requires those products, not merely process exit.
